---
phase: 04-user-experience-progress-output
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/output/output.go
  - internal/output/ndjson.go
  - internal/output/speed.go
  - internal/output/output_test.go
  - main.go
  - internal/mux/mux.go
  - internal/api/client.go
  - internal/api/episode.go
  - internal/api/manifest.go
  - go.mod
  - go.sum
autonomous: false
requirements: [UX-04, UX-08]
user_setup:
  - service: "golang.org/x/term"
    why: "TTY detection for ANSI color output"
    env_vars: []
    dashboard_config: []

must_haves:
  truths:
    - "output package compiles and passes tests"
    - "main.go compiles with --quiet and --json flags"
    - "main.go uses output.Global.Info/Warn/Error instead of fmt.Printf"
    - "all fmt.Printf calls in mux.go, api/client.go, api/episode.go, api/manifest.go replaced with output.Global calls"
    - "`go build .` succeeds with no errors"
    - "go test ./internal/output/... -count=1 -v passes"
  artifacts:
    - internal/output/output.go
    - internal/output/ndjson.go
    - internal/output/speed.go
    - internal/output/output_test.go
    - modified main.go
    - modified internal/mux/mux.go
    - modified internal/api/client.go
    - modified internal/api/episode.go
    - modified internal/api/manifest.go
  key_links:
    - "output.Global.Info/Warn/Error/Debug/Progress used in main.go replaces 23 fmt.Printf calls"
    - "output.Global.Warn replaces fmt.Printf in mux.go cleanup warnings"
    - "output.Global.Debug replaces fmt.Printf in api/*.go debug dumps"
    - "output.Init() called after flag.Parse() in main.go sets the mode"
---

<objective>
Create the `internal/output/` abstraction package and wire it into main.go and all non-download internal packages — establishing the output infrastructure that Plans 4.1 and 4.2 build on.

**Purpose:** Provide a single global output sink (per D-03) so all three output modes (human, JSON, quiet) are available system-wide. Eliminate raw `fmt.Printf` from main.go, mux.go, and api/*.go — 28 of the 43 total calls.

**Output:** `internal/output/` package (output.go, ndjson.go, speed.go, output_test.go), modified main.go/mux.go/api files, go.mod updated with `golang.org/x/term`.
</objective>

<execution_context>
@/home/fmoura/Documents/Applications/crunchyroll-downloader/.opencode/gsd-core/workflows/execute-plan.md
@/home/fmoura/Documents/Applications/crunchyroll-downloader/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-user-experience-progress-output/04-CONTEXT.md
@.planning/phases/04-user-experience-progress-output/04-RESEARCH.md
@.planning/phases/04-user-experience-progress-output/04-UI-SPEC.md
@.planning/phases/03-usability-configuration-validation/03-CONTEXT.md
@.planning/phases/01-foundation-error-handling-http-memory/01-CONTEXT.md

# Source files being modified
@/home/fmoura/Documents/Applications/crunchyroll-downloader/internal/config/config.go
@/home/fmoura/Documents/Applications/crunchyroll-downloader/main.go
@/home/fmoura/Documents/Applications/crunchyroll-downloader/internal/mux/mux.go
@/home/fmoura/Documents/Applications/crunchyroll-downloader/internal/api/client.go
@/home/fmoura/Documents/Applications/crunchyroll-downloader/internal/api/episode.go
@/home/fmoura/Documents/Applications/crunchyroll-downloader/internal/api/manifest.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create internal/output/ package with interface, modes, SpeedTracker, NDJSON events, and tests</name>
  <files>
    internal/output/output.go
    internal/output/speed.go
    internal/output/ndjson.go
    internal/output/output_test.go
    go.mod
    go.sum
  </files>

  <read_first>
    @/home/fmoura/Documents/Applications/crunchyroll-downloader/internal/config/config.go
    @/home/fmoura/Documents/Applications/crunchyroll-downloader/.planning/phases/04-user-experience-progress-output/04-CONTEXT.md
    @/home/fmoura/Documents/Applications/crunchyroll-downloader/.planning/phases/04-user-experience-progress-output/04-UI-SPEC.md
    @/home/fmoura/Documents/Applications/crunchyroll-downloader/.planning/phases/04-user-experience-progress-output/04-RESEARCH.md
  </read_first>

  <action>
    Create the `internal/output/` directory and three source files:

    **1. internal/output/output.go:**
    - Define `Mode` type as `int` with constants `ModeHuman`, `ModeJSON`, `ModeQuiet` (iota)
    - Define ANSI color constants: `ANSIReset="\033[0m"`, `ANSIBold="\033[1m"`, `ANSIDim="\033[90m"`, `ANSIRed="\033[31m"`, `ANSIGreen="\033[32m"`, `ANSIYellow="\033[33m"`, `ANSICyan="\033[36m"` (per UI-SPEC.md color palette)
    - Define `Outputter` interface with five methods: `Info(format string, args ...any)`, `Warn(...)`, `Error(...)`, `Debug(...)`, `Progress(...)`
    - Declare `var Global Outputter = &humanOutput{}` — default before Init is called (per D-03 pattern matching internal/config)
    - Implement `Init(m Mode)` that sets Global to the correct implementation, performing TTY detection via `golang.org/x/term.IsTerminal(int(os.Stdout.Fd()))` — if not a TTY, override ANSI constants to empty strings to suppress color codes
    - **humanOutput struct:** embeds `mu sync.Mutex`, `speedTracker *SpeedTracker`. Methods:
      - `Info`: mutex-lock, print `"\033[K"` (clear line), `fmt.Fprintf(os.Stdout, ...)`, `\n`, unlock (per Pitfall 1: clear line remnants)
      - `Warn`: mutex-lock, print `ANSIYellow + "⚠ " + ANSIReset`, then the message + `\n`, unlock
      - `Error`: mutex-lock, print `ANSIRed + "✗ " + ANSIReset`, then the message + `\n`, unlock. Write to `os.Stderr` (standard error channel)
      - `Debug`: mutex-lock, print `ANSIDim + "[debug] " + ANSIReset`, then message + `\n`, unlock. Write to `os.Stderr`
      - `Progress`: mutex-lock, print `"\r\033[K"` (CR + clear line), `fmt.Fprintf(os.Stdout, format, args...)`, unlock. No trailing newline (carriage return overwrites on next call)
    - **jsonOutput struct:** embeds `mu sync.Mutex`, `enc *json.Encoder` (initialized with `os.Stdout`), `speedTracker *SpeedTracker`. Methods:
      - `Info`: marshal `event{Type: "info", ...}` via `emitEvent()` (defined in ndjson.go)
      - `Warn`: marshal `event{Type: "warn", ...}`
      - `Error`: marshal `event{Type: "error", Message: ...}` (per D-16: errors emit as JSON events in JSON mode)
      - `Debug`: marshal `event{Type: "debug", ...}`
      - `Progress`: marshal appropriate NDJSON event type (`segment_progress`) — caller passes pre-formatted data; the JSON encoder handles the marshaling
    - **quietOutput struct:** Embeds no mutex (no shared mutable state). Methods:
      - `Info`, `Debug`, `Progress` — no-op (per D-16)
      - `Warn`: `fmt.Fprintf(os.Stderr, "⚠ %s\n", ...)` — warnings always show
      - `Error`: `fmt.Fprintf(os.Stderr, "✗ %s\n", ...)` — errors always show
    - Add exported package-level `RecordBytes(n int64)`, `SpeedBps() float64`, and `ETASeconds(remaining int64) int` functions that delegate to the global `SpeedTracker`. These MUST be exported (capital first letter) because they are called from `internal/media/segment.go` in Plan 4.2. Add a package-level `speedTrackerMu sync.Mutex` and `var globalSpeedTracker *SpeedTracker` for lazy initialization:
    ```go
    var (
        speedTrackerMu    sync.Mutex
        globalSpeedTracker *SpeedTracker
    )
    func RecordBytes(n int64) {
        speedTrackerMu.Lock()
        defer speedTrackerMu.Unlock()
        if globalSpeedTracker == nil {
            globalSpeedTracker = NewSpeedTracker()
        }
        globalSpeedTracker.Record(n)
    }
    func SpeedBps() float64 {
        speedTrackerMu.Lock()
        defer speedTrackerMu.Unlock()
        if globalSpeedTracker == nil { return 0 }
        return globalSpeedTracker.Bps()
    }
    func ETASeconds(remaining int64) int {
        speedTrackerMu.Lock()
        defer speedTrackerMu.Unlock()
        if globalSpeedTracker == nil { return 0 }
        return int(globalSpeedTracker.ETA(remaining).Seconds())
    }
    ```

    **2. internal/output/speed.go:**
    - Define `sample` struct (`at time.Time`, `bytes int64`), unexported
    - Define `SpeedTracker` struct with `mu sync.Mutex`, `buf [10]sample` (ring buffer, per D-10: 10-second window), `pos int`, `count int`
    - `NewSpeedTracker() *SpeedTracker` constructor
    - `Record(bytes int64)`: mutex-lock, write `sample{time.Now(), bytes}` at current position, advance pos circularly, increment count (max 10)
    - `Bps() float64`: mutex-lock, read valid samples within a 10-second cutoff window, compute total bytes / window duration. Return 0 if fewer than 2 samples
    - `ETA(remaining int64) time.Duration`: call Bps(), if <= 0 return 0, return `time.Duration(float64(remaining)/bps) * time.Second`
    - Implement Pitfall 5 mitigation: clamp ETA so it never goes up — track `lastETA` field and use `min(currentETA, lastETA)` in `ETA()`; if remaining <= 2 segments worth, return 0

    **3. internal/output/ndjson.go:**
    - Define event types per UI-SPEC.md NDJSON schema:
      ```go
      type event struct {
          Type      string `json:"type"`
          Timestamp string `json:"timestamp"`
          // Optional fields matching UI-SPEC.md:
          EpisodeNumber int     `json:"episode_number,omitempty"`
          SeasonNumber  int     `json:"season_number,omitempty"`
          TotalEpisodes int     `json:"total_episodes,omitempty"`
          Title         string  `json:"title,omitempty"`
          SeriesTitle   string  `json:"series_title,omitempty"`
          Downloaded    int     `json:"downloaded,omitempty"`
          Total         int     `json:"total,omitempty"`
          Percent       float64 `json:"percent,omitempty"`
          BytesPerSec   int64   `json:"bytes_per_sec,omitempty"`
          ETASecs       int     `json:"eta_secs,omitempty"`
          Stream        string  `json:"stream,omitempty"`
          Locale        string  `json:"locale,omitempty"`
          Message       string  `json:"message,omitempty"`
          Success       bool    `json:"success,omitempty"`
          Fatal         bool    `json:"fatal,omitempty"`
          DurationSecs  float64 `json:"duration_secs,omitempty"`
          SizeBytes     int64   `json:"size_bytes,omitempty"`
          Successful    int     `json:"successful,omitempty"`
          Failed        int     `json:"failed,omitempty"`
          Errors        []eventError `json:"errors,omitempty"`
      }
      type eventError struct {
          EpisodeNumber int    `json:"episode_number"`
          Title         string `json:"title"`
          Message       string `json:"message"`
      }
      ```
    - Helper `emitEvent(evt event)`: sets `evt.Timestamp = time.Now().UTC().Format(time.RFC3339)`, json.Marshal, write to os.Stdout, write `'\n'` (per D-13: NDJSON format)
    - Note: The `emitEvent` function writes directly to os.Stdout (not through the json.Encoder) to ensure raw NDJSON output — consistent with RESEARCH.md section "NDJSON Emitter"

    **4. internal/output/output_test.go:**
    - Create tests using `os.Pipe()` to capture stdout/stderr (per RESEARCH.md "Wave 0 Gaps")
    - `TestHumanOutputInfo`: capture stdout, call Global.Info("hello %s", "world"), verify output contains "hello world" and ANSI clear
    - `TestHumanOutputError`: capture stderr, call Global.Error("fail"), verify output contains "✗" on stderr
    - `TestHumanOutputWarn`: capture stdout, call Global.Warn("caution"), verify output contains "⚠"
    - `TestHumanOutputProgress`: capture stdout, call Global.Progress("test"), verify output contains carriage return `\r` and no trailing newline
    - `TestJSONOutputInfo`: set ModeJSON via Init, capture stdout, call Global.Info("test"), verify output is valid JSON with `"type":"info"` field
    - `TestJSONOutputError`: in ModeJSON, capture stdout, call Global.Error("boom"), verify JSON contains `"type":"error"` and `"message":"boom"`
    - `TestQuietModeInfo`: set ModeQuiet, capture stdout, call Global.Info("silent"), verify stdout is empty
    - `TestQuietModeError`: in ModeQuiet, capture stderr, call Global.Error("err"), verify output contains "✗ err"
    - `TestSpeedTrackerRecord`: 10 sequential Record calls, verify Bps() is non-zero
    - `TestSpeedTrackerEmpty`: zero records, verify Bps() returns 0
    - `TestSpeedTrackerETA`: record 100 bytes each for 10 samples with 1-second spacing (use time manipulation), verify ETA returns correct value. Acceptable for test to use `time.Sleep` with short intervals or restructure to inject time
    - For time-dependent tests, use `time.Now()` as-is but keep windows short; tests should complete in under 3 seconds

    **5. Dependencies:**
    - Run `go get golang.org/x/term` to add the dependency (per RESEARCH.md Package Legitimacy Audit — [ASSUMED] verdict, Go team package)
    - Run `go mod tidy` to clean up

    Imports needed in output.go:
    ```go
    import (
        "encoding/json"
        "fmt"
        "os"
        "sync"
        "time"
        "golang.org/x/term"
    )
    ```
  </action>

  <verify>
    <automated>go build ./internal/output/... && go test ./internal/output/... -count=1 -v</automated>
  </verify>

  <done>
    - `go build ./internal/output/...` succeeds
    - `go test ./internal/output/... -count=1 -v` passes all 10+ tests
    - `golang.org/x/term` is in go.mod
    - Outputter interface defined with 5 methods
    - All three output implementations compile
    - SpeedTracker compiles with Record/Bps/ETA methods
    - NDJSON event structs defined with snake_case tags
  </done>

  <acceptance_criteria>
    - `go build ./internal/output/...` — no compile errors
    - `go test ./internal/output/... -count=1 -v` — all tests pass
    - `grep -c 'golang.org/x/term' go.mod` — dependency exists
    - `grep -c 'type Outputter interface' internal/output/output.go` — interface defined
    - `grep -c 'var Global Outputter' internal/output/output.go` — global declared
    - `grep -c 'func Init' internal/output/output.go` — Init function exists
    - `grep -c 'type humanOutput' internal/output/output.go` — human mode exists
    - `grep -c 'type jsonOutput' internal/output/output.go` — JSON mode exists
    - `grep -c 'type quietOutput' internal/output/output.go` — quiet mode exists
    - `grep -c 'type SpeedTracker' internal/output/speed.go` — SpeedTracker exists
    - `grep -c 'type event struct' internal/output/ndjson.go` — event type exists
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add --quiet/--json flags to main.go, wire output.Init(), replace 23 fmt.Printf calls with output.Global</name>
  <files>
    main.go
  </files>

  <read_first>
    @/home/fmoura/Documents/Applications/crunchyroll-downloader/main.go
    @/home/fmoura/Documents/Applications/crunchyroll-downloader/.planning/phases/04-user-experience-progress-output/04-UI-SPEC.md
    @/home/fmoura/Documents/Applications/crunchyroll-downloader/.planning/phases/04-user-experience-progress-output/04-RESEARCH.md
  </read_first>

  <action>
    Modify `main.go` to:
    
    **1. Add imports:** `"crunchyroll-downloader/internal/output"` — add BEFORE `"crunchyroll-downloader/internal/config"` import block (alphabetical by import path, "config" vs "output": **o** comes after **c**, so add after config line)

    **2. Add flag variables** (after existing flag definitions around line 30):
    ```go
    jsonMode  = flag.Bool("json", false, "Output progress as NDJSON")
    quietMode = flag.Bool("quiet", false, "Suppress progress output (errors still print)")
    ```

    **3. Resolve output mode** after `flag.Parse()` and the `explicitFlags` tracking block, but BEFORE config path resolution (around line 265 in current code). Insert this block:
    ```go
    // Resolve output mode (after flag.Parse)
    switch {
    case *jsonMode:
        output.Init(output.ModeJSON)
    case *quietMode:
        output.Init(output.ModeQuiet)
    default:
        output.Init(output.ModeHuman)
    }
    ```
    This follows the pattern from RESEARCH.md section "main.go Initialization Pattern"

    **4. Replace ALL 23 `fmt.Printf`/`fmt.Println`/`fmt.Fprintf(os.Stderr,...)` calls with `output.Global` calls — per D-01, eliminating all raw output from main.go.**

    **Replacement map (current → replacement):**

    | Current | Replacement | Reasoning |
    |---------|-------------|-----------|
    | `fmt.Fprintf(os.Stderr, "Warning: cannot determine config path: %v — skipping config file\n", err)` | `output.Global.Warn("Warning: cannot determine config path: %v ...skipping config file", err)` | D-02: Info/Warn/Error methods; "Warning:" prefix is now implicit via Warn method |
    | `fmt.Fprintf(os.Stderr, "Warning: %v — using defaults\n", err)` | `output.Global.Warn("%v ...using defaults", err)` | Same pattern |
    | `fmt.Fprintf(os.Stderr, "Warning: could not create default config: %v\n", err)` | `output.Global.Warn("Could not create default config: %v", err)` | Implicit "Warning:" prefix dropped — Warn method adds '⚠' |
    | `fmt.Printf("Created default config at %s\n", cfgPath)` | `output.Global.Info("Created default config at %s", cfgPath)` | D-02: Info for status messages |
    | `fmt.Printf("Error: %v\n", err)` | `output.Global.Error("%v", err)` | D-02: Error method |
    | `fmt.Printf("Invalid URL: %v\n", err)` | `output.Global.Error("Invalid URL: %v", err)` | D-07: errors shown immediately |
    | `fmt.Printf("Invalid URL format: %s\n", rawURL)` (lines 113, 119) | `output.Global.Error("Invalid URL format: %s", rawURL)` | Same pattern |
    | `fmt.Printf("Invalid URL (must be /watch/ or /series/): %s\n", rawURL)` | `output.Global.Error("Invalid URL (must be /watch/ or /series/): %s", rawURL)` | Same |
    | `fmt.Printf("Error fetching episode info: %v\n", err)` | `output.Global.Error("Error fetching episode info: %v", err)` | Same |
    | `fmt.Printf("Error downloading episode: %v\n", err)` | `output.Global.Error("Error downloading episode: %v", err)` | Same |
    | `fmt.Printf("Error fetching seasons: %v\n", err)` | `output.Global.Error("Error fetching seasons: %v", err)` | Same |
    | `fmt.Printf("This anime has no season %v!\n", *seasonNumber)` | `output.Global.Warn("This anime has no season %v!", *seasonNumber)` | Warning, not error — user input issue |
    | `fmt.Printf("Error fetching episodes: %v\n", err)` (lines 168, 180) | `output.Global.Error("Error fetching episodes: %v", err)` | Same |
    | `fmt.Printf("Season completed with errors: %v\n", err)` (lines 172, 184) | `output.Global.Warn("Season completed with errors: %v", err)` | Non-fatal; season continues |
    | `fmt.Print("No season number specified, downloading all seasons...\n")` | `output.Global.Info("No season number specified, downloading all seasons...")` | D-02: Info |
    | `fmt.Printf("Failed to initialize API client: %v\n", err)` | `output.Global.Error("Failed to initialize API client: %v", err)` | Error — exit follows |
    | `fmt.Println(errMsg)` | `output.Global.Error("%s", errMsg)` | Error — exit follows |
    | `fmt.Printf("Failed to open URLs file: %s\n", err)` | `output.Global.Error("Failed to open URLs file: %s", err)` | Error — exit follows |
    | `fmt.Println("Invalid URLs found:")` | `output.Global.Info("Invalid URLs found:")` | Info — list header |
    | `fmt.Printf("  %s — %s\n", inv.URL, inv.Error)` | `output.Global.Info("  %s ... %s", inv.URL, inv.Error)` | Part of validation report |
    | `fmt.Printf("Found %d URLs to download\n\n", len(urls))` | `output.Global.Info("Found %d URLs to download\\n", len(urls))` | Note: use literal `\n` in format string for blank line |
    | `fmt.Printf("=== [%d/%d] %s ===\n", i+1, len(urls), u)` | `output.Global.Info("=== [%d/%d] %s ===", i+1, len(urls), u)` | Batch header |
    | `fmt.Println()` (line 357, between batch items) | `output.Global.Info("")` | Empty info line for spacing |
    | `fmt.Printf("Invalid URL: %v\n", err)` (line 107, processURL) | `output.Global.Error("Invalid URL: %v", err)` | Already covered |
    | `fmt.Println(errMsg)` (line 323) | Already covered above | |

    **5. CRITICAL — remove `"fmt"` from import if no raw fmt calls remain.** Since all fmt.Printf/Fprintf/Println calls are replaced, the `"fmt"` import must be removed to prevent compilation error. The imports that remain: `"bufio"`, `"context"`, `"flag"`, `"net/url"`, `"os"`, `"os/exec"`, `"os/signal"`, `"strings"`, `"syscall"`, `"crunchyroll-downloader/internal/api"`, `"crunchyroll-downloader/internal/config"`, `"crunchyroll-downloader/internal/download"`, `"crunchyroll-downloader/internal/drm"`, `"crunchyroll-downloader/internal/output"`.

    **6. After changes, run `go build .` to verify compilation.**

    **DO NOT** modify main.go's logic flow — only replace output calls and add the new flag variables + output.Init() block.
  </action>

  <verify>
    <automated>go build .</automated>
  </verify>

  <done>
    - `go build .` succeeds
    - `--json` and `--quiet` flags accepted by the CLI (no "flag provided but not defined" error)
    - `output.Init()` called before any output
    - No `fmt.Printf`, `fmt.Println`, or `fmt.Fprintf` calls remain in main.go (except possibly `flag.Usage()` and `os.Exit()`)
    - `grep -c 'fmt\.Printf\|fmt\.Fprintf\|fmt\.Println' main.go` returns 0
  </done>

  <acceptance_criteria>
    - `go build .` — no compile errors
    - `go run . --help` shows `--json` and `--quiet` in flag list
    - `grep -v '^import\|^$' main.go | grep -c 'output\.Global\.'` — output.Global used at least 20 times
    - `grep -c '"fmt"' main.go` — returns 0 (no raw fmt import) UNLESS flag.Usage() requires it — in which case keep fmt and verify no Printf/Println calls
    - `grep -c 'output.Init(' main.go` — Init called exactly once
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Replace fmt.Printf in mux.go and api/*.go with output.Global calls</name>
  <files>
    internal/mux/mux.go
    internal/api/client.go
    internal/api/episode.go
    internal/api/manifest.go
  </files>

  <read_first>
    @/home/fmoura/Documents/Applications/crunchyroll-downloader/internal/mux/mux.go
    @/home/fmoura/Documents/Applications/crunchyroll-downloader/internal/api/client.go
    @/home/fmoura/Documents/Applications/crunchyroll-downloader/internal/api/episode.go
    @/home/fmoura/Documents/Applications/crunchyroll-downloader/internal/api/manifest.go
  </read_first>

  <action>
    **1. internal/mux/mux.go (2 replacements):**
    - Add import: `"crunchyroll-downloader/internal/output"` — alphabetical order
    - Replace `fmt.Printf("\nDownload finished! Output file: %s\n\n", outputFile)` (line 109): → `output.Global.Info("\\nDownload finished! Output file: %s\\n", outputFile)` (use escaped `\n` for literal newlines in the format string)
    - Replace `fmt.Printf("Warning: failed to remove temporary file %s: %v\n", path, err)` (line 115 in `warnRemove`): → `output.Global.Warn("Failed to remove temporary file %s: %v", path, err)` — implicit "Warning:" dropped since Warn adds its own prefix
    - Remove `"fmt"` import if it's the only fmt usage. Check: `fmt.Sprintf` calls remain (in args/map construction). So keep `"fmt"` import but remove the `fmt.Printf` usage patterns. The file still needs `"fmt"` for Sprintf in metadata tags and map arguments.

    **2. internal/api/client.go (1 replacement):**
    - Add import: `"crunchyroll-downloader/internal/output"`
    - Replace `fmt.Println("Access token expired. Refetching one...")` (line 101 in `Do` method): → `output.Global.Debug("Access token expired. Refetching one...")` — this is an internal infrastructure message, debug-level is appropriate (D-02: Debug for internal details)
    - Keep `"fmt"` import if `fmt.Errorf` remains (it does — several Errorf calls exist)

    **3. internal/api/episode.go (1 replacement):**
    - Add import: `"crunchyroll-downloader/internal/output"`
    - Replace `fmt.Printf("\n%s\n", string(body))` (line 39 in `GetEpisode`, inside `if c.Debug` guard): → `output.Global.Debug("\\n%s", string(body))` — debug-level since this is behind the `c.Debug` flag
    - Keep `"fmt"` import for Errorf and URL construction

    **4. internal/api/manifest.go (1 replacement):**
    - Add import: `"crunchyroll-downloader/internal/output"`
    - Replace `fmt.Printf("\n%s\n", string(body))` (line 28 in `FetchManifest`, inside `if c.Debug` guard): → `output.Global.Debug("\\n%s", string(body))` — same reasoning as episode.go
    - Keep `"fmt"` import for Errorf

    **5. After all 4 files modified, run `go build ./...` to verify everything compiles together.**

    IMPORTANT: Do NOT change any logic, conditionals, or error returns. Only replace the print calls with output.Global equivalents.
  </action>

  <verify>
    <automated>go build ./internal/mux/... && go build ./internal/api/... && go build ./...</automated>
  </verify>

  <done>
    - `go build ./...` succeeds
    - No `fmt.Printf` calls remain in any of the 4 files (fmt.Sprint-based calls like Sprintf/Errorf are fine and remain)
    - `grep -c 'fmt\.Printf\|fmt\.Println' internal/mux/mux.go internal/api/client.go internal/api/episode.go internal/api/manifest.go` returns 0 total
    - Each file imports `"crunchyroll-downloader/internal/output"`
  </done>

  <acceptance_criteria>
    - `go build ./...` — no compile errors
    - `grep -c '"crunchyroll-downloader/internal/output"' internal/mux/mux.go` — mux imports output
    - `grep -c '"crunchyroll-downloader/internal/output"' internal/api/client.go` — client imports output
    - `grep -c '"crunchyroll-downloader/internal/output"' internal/api/episode.go` — episode.go imports output
    - `grep -c '"crunchyroll-downloader/internal/output"' internal/api/manifest.go` — manifest.go imports output
    - `rg "fmt\.(Printf|Println)" internal/mux/mux.go internal/api/client.go internal/api/episode.go internal/api/manifest.go | wc -l` — returns 0 (no raw print calls remain)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stdout/stderr output | The output package writes to process standard streams. No untrusted input crosses this boundary — the output package does not parse user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | main.go --json flag | low | accept | NDJSON events contain episode titles and series names (already visible in human mode). No credentials or tokens are written to NDJSON output |
| T-04-02 | Tampering | go get golang.org/x/term | high | mitigate | Performed in RESEARCH.md Package Legitimacy Audit: [ASSUMED] verdict, Go team-maintained package at go.googlesource.com |
| T-04-03 | Denial of Service | humanOutput mu sync.Mutex | low | accept | Mutex contention on Global output is negligible at 1 event/sec. No external attacker can trigger high-frequency writes |
</threat_model>

<verification>
- `go build ./...` succeeds (all packages compile with new output package)
- `go test ./internal/output/... -count=1 -v` passes all tests
- `go vet ./...` passes with no issues
- No raw `fmt.Printf`/`fmt.Println`/`fmt.Fprintf` calls remain in main.go, mux.go, or api/*.go
</verification>

<success_criteria>
- `internal/output/` package exists with exported Outputter interface, Global variable, Init function
- Three mode implementations (human, JSON, quiet) all functional
- `--quiet` and `--json` CLI flags operational
- main.go uses output.Global exclusively for all output
- mux.go and api/*.go use output.Global exclusively for all output
- 28 of 43 total fmt.Printf calls replaced (Plan 4.1 replaces 13 more, Plan 4.2 replaces 2 more)
</success_criteria>

<output>
Create `.planning/phases/04-user-experience-progress-output/04-03-SUMMARY.md` when done.
</output>
