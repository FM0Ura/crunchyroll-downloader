---
phase: "01-foundation-error-handling-http-memory"
plan: "01-05"
type: "test"
wave: 4
depends_on:
  - "01-01"
  - "01-02"
  - "01-03"
  - "01-04"
files_modified:
  - main.go
  - internal/download/episode.go
  - internal/mux/mux.go
  - main_test.go
  - internal/api/client_test.go
  - internal/download/episode_test.go
  - internal/media/segment_test.go
  - internal/drm/drm_test.go
  - internal/mux/mux_test.go
autonomous: true
requirements:
  - UX-07
  - QOL-01
must_haves:
  truths:
    - "D-18: SIGINT and SIGTERM cancel active work instead of leaving partial state behind."
    - "D-19: ffmpeg is terminated on interruption and partial output is removed."
    - "D-20: Temporary files are always removed on interruption."
    - "D-21: DeleteStream failures do not block local shutdown."
    - "D-22: Any partial episode artifacts are removed after interruption."
    - "D-23: Test investment focuses on the most fragile points in this phase."
    - "D-24: The first test batch covers error contract, HTTP retry, DownloadParts, Widevine discovery, cleanup/interruption, and URL parsing."
    - "D-25: The phase sets a minimal useful coverage bar, not a strict high target."
  artifacts:
    - path: main_test.go
      provides: "CLI and URL parsing regression coverage"
    - path: internal/api/client_test.go
      provides: "HTTP retry and cancellation coverage"
    - path: internal/download/episode_test.go
      provides: "Episode cleanup and continuation coverage"
    - path: internal/media/segment_test.go
      provides: "Segment streaming and temp-file cleanup coverage"
    - path: internal/drm/drm_test.go
      provides: "Widevine discovery precedence coverage"
    - path: internal/mux/mux_test.go
      provides: "Mux cleanup and ffmpeg failure coverage"
  key_links:
    - from: main.go
      to: internal/download/episode.go
      via: "signal cancellation must reach the episode loop"
    - from: internal/download/episode.go
      to: internal/mux/mux.go
      via: "cancelled runs must stop muxing and clean partial files"
---

<objective>
Lock in the phase's fragile behavior with focused tests and complete the interruption-cleanup path so a Ctrl+C leaves no partial episode artifacts behind.
</objective>

<tasks>
### Task 1
`type`: auto
`files`: main.go, internal/download/episode.go, internal/mux/mux.go
`action`: Add SIGINT/SIGTERM handling that cancels the active run, stops worker activity, kills ffmpeg, and removes temporary or partial outputs without letting DeleteStream failures block shutdown.
`verify`: Trigger cancellation during a mocked download and confirm the process exits cleanly with no partial output left behind.
`acceptance_criteria`: Interrupts leave the workspace clean and do not crash the process.

### Task 2
`type`: auto
`files`: main_test.go, internal/api/client_test.go, internal/download/episode_test.go, internal/media/segment_test.go, internal/drm/drm_test.go, internal/mux/mux_test.go
`action`: Build the first useful test batch for the phase: error contract, HTTP retry, segment assembly, Widevine discovery, and cleanup/interruption regression coverage.
`verify`: `go test ./...`
`acceptance_criteria`: The highest-risk paths in the phase are covered by tests instead of just by plan text.

### Task 3
`type`: auto
`files`: main_test.go
`action`: Add a small regression check for the existing URL parsing entrypoint so the test batch includes the CLI path that the phase decision called out, even though the fix itself is deferred.
`verify`: `go test ./... -run TestProcessURL`
`acceptance_criteria`: The first test batch includes the CLI parsing path called out in phase discussion.
</tasks>

<verification>
- `go test ./...`
- Confirm cancelation leaves no partial artifacts.
- Confirm the new tests cover the highest-risk phase behavior.
</verification>

<success_criteria>
- Ctrl+C and SIGTERM cancel active work cleanly.
- ffmpeg and temporary files are cleaned up on interruption.
- DeleteStream failures do not block local shutdown.
- The phase has focused tests for error handling, HTTP retry, segment assembly, Widevine discovery, cleanup, and CLI parsing.
- The plan covers UX-07 and QOL-01.
</success_criteria>
