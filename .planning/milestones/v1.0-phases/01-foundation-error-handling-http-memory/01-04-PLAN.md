---
phase: "01-foundation-error-handling-http-memory"
plan: "01-04"
type: "refactor"
wave: 3
depends_on:
  - "01-02"
files_modified:
  - internal/drm/drm.go
autonomous: true
requirements:
  - PERF-05
  - QOL-13
must_haves:
  truths:
    - "D-13: The Widevine device is loaded once per process and reused for license requests."
    - "D-14: Device paths come from environment variables loaded from .env for this phase."
    - "D-15: client_id.bin and private_key.pem are valid only as a pair."
    - "D-16: A packaged .wvd file wins over the raw pair when both are present."
    - "D-17: Missing device configuration returns one explicit error that names the accepted formats."
  artifacts:
    - path: internal/drm/drm.go
      provides: "Widevine device discovery, loading, and one-time reuse"
  key_links:
    - from: internal/drm/drm.go
      to: internal/api/license.go
      via: "license acquisition depends on the cached device and its keys"
---

<objective>
Cache the Widevine device for the process lifetime and make device discovery explicit, deterministic, and fail-fast when the user has not provided a usable device configuration.
</objective>

<tasks>
### Task 1
`type`: auto
`files`: internal/drm/drm.go
`action`: Add a one-time device loader that caches the loaded Widevine device for the process and prefers `.wvd` when both `.wvd` and the raw pair are present.
`verify`: Run a license-flow unit path twice and confirm the loader only reads the device once.
`acceptance_criteria`: The device is cached and reused instead of being re-read on every license request.

### Task 2
`type`: auto
`files`: internal/drm/drm.go
`action`: Add `.env`-backed environment-variable lookup for device paths, require `client_id.bin` and `private_key.pem` to exist together, and fail with one clear error when config is missing or incomplete.
`verify`: Use a temp directory with and without the pair present and confirm the precedence and error messages.
`acceptance_criteria`: Device discovery is deterministic and does not silently fall back when configuration is incomplete.

### Task 3
`type`: auto
`files`: internal/drm/drm_test.go
`action`: Add tests for the `.wvd` precedence, raw-pair all-or-nothing behavior, and the missing-device error path.
`verify`: `go test ./internal/drm`
`acceptance_criteria`: Device discovery behavior is locked in before the download flow depends on it.
</tasks>

<verification>
- `go test ./internal/drm`
- Confirm `.wvd` beats the raw pair.
- Confirm the raw pair only works when both files exist.
- Confirm missing config fails with one explicit error.
</verification>

<success_criteria>
- Widevine devices are loaded once per process.
- `.env`-backed env vars are used for device discovery in this phase.
- Discovery is explicit about accepted formats and does not silently limp forward.
- The plan covers PERF-05 and QOL-13.
</success_criteria>
