---
phase: "01-foundation-error-handling-http-memory"
plan: "01-01"
type: "refactor"
wave: 1
depends_on: []
files_modified:
  - internal/download/episode.go
  - internal/download/season.go
  - internal/mux/mux.go
autonomous: true
requirements:
  - UX-01
  - UX-05
  - QOL-10
  - QOL-11
must_haves:
  truths:
    - "D-01: Episode failures return errors instead of panicking."
    - "D-01: Season runs continue after a single episode failure once cleanup finishes."
    - "D-02: User-facing errors stay short, clear, and actionable."
    - "D-03: Cleanup failures are warnings when the main work succeeded."
    - "D-04: Expected control flow does not use panic()."
  artifacts:
    - path: internal/download/episode.go
      provides: "Episode-level error propagation and cleanup continuation"
    - path: internal/download/season.go
      provides: "Season loop that keeps moving after a failed episode"
    - path: internal/mux/mux.go
      provides: "ffmpeg merge error handling and cleanup warnings"
  key_links:
    - from: internal/download/season.go
      to: internal/download/episode.go
      via: "per-episode call returns error and the season loop decides whether to continue"
    - from: internal/download/episode.go
      to: internal/mux/mux.go
      via: "merge step consumes downloaded tracks and must not panic on ffmpeg failure"
---

<objective>
Make the episode and season flow return explicit errors, preserve continue-on-error behavior for batch downloads, and replace mux panic paths with errors plus cleanup warnings.
</objective>

<tasks>
### Task 1
`type`: auto
`files`: internal/download/episode.go, internal/download/season.go
`action`: Convert episode orchestration and the season loop to return errors to the caller, keep per-episode cleanup in place, and continue the season after one episode fails.
`verify`: Run a season-mode unit path that fails one episode and confirms the next one still runs; confirm the returned error is logged once and the loop continues.
`acceptance_criteria`: Episode failures no longer crash the process; season downloads keep going after cleanup.

### Task 2
`type`: auto
`files`: internal/mux/mux.go
`action`: Replace ffmpeg panic handling with returned errors, preserve partial-output cleanup, and turn cleanup-removal failures into warnings instead of hard failures when muxing succeeded.
`verify`: Force an ffmpeg failure in a unit test or local harness and confirm the function returns an error and removes partial output.
`acceptance_criteria`: Mux failures are explicit errors, and successful downloads are not downgraded by cleanup noise.

### Task 3
`type`: auto
`files`: internal/download/episode_test.go, internal/download/season_test.go, internal/mux/mux_test.go
`action`: Add focused tests for the episode-failure continuation path, the season loop, and the mux cleanup/error contract.
`verify`: `go test ./internal/download ./internal/mux`
`acceptance_criteria`: The phase has test coverage for the new error contract and cleanup semantics.
</tasks>

<verification>
- `go test ./internal/download ./internal/mux`
- Confirm one failed episode does not stop a season run.
- Confirm mux cleanup warnings do not turn a successful run into a failure.
</verification>

<success_criteria>
- Episode and season flows return errors instead of panicking.
- Season downloads continue after one episode failure.
- ffmpeg failures are surfaced cleanly and partial output is removed.
- Cleanup warnings do not override a successful main result.
- The plan maps directly to UX-01, UX-05, QOL-10, and QOL-11.
</success_criteria>
