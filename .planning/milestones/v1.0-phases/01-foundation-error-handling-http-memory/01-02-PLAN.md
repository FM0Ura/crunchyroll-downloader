---
phase: "01-foundation-error-handling-http-memory"
plan: "01-02"
type: "refactor"
wave: 2
depends_on:
  - "01-01"
files_modified:
  - main.go
  - internal/api/client.go
  - internal/api/episode.go
  - internal/api/season.go
  - internal/api/license.go
  - internal/download/episode.go
autonomous: true
requirements:
  - PERF-02
  - PERF-03
  - PERF-07
  - QOL-06
must_haves:
  truths:
    - "D-05: The shared HTTP client uses a configured transport with keep-alive and idle connection reuse."
    - "D-05: HTTP calls carry context and timeouts from main down through API calls."
    - "D-06: 401 refresh happens once and only once before returning an error."
    - "D-08: Cancellation propagates from main through API and download entrypoints."
    - "D-04: The retry path remains explicit and bounded instead of relying on panic or recursion."
  artifacts:
    - path: main.go
      provides: "Root context and orchestration wiring for the shared client"
    - path: internal/api/client.go
      provides: "Configured transport, timeout policy, and bounded auth refresh"
    - path: internal/api/episode.go
      provides: "Episode fetches that honor the shared client and context"
    - path: internal/api/season.go
      provides: "Season and episode listing calls that honor the shared client and context"
    - path: internal/api/license.go
      provides: "License challenge requests that honor the shared client and context"
  key_links:
    - from: main.go
      to: internal/api/client.go
      via: "client creation carries the transport and timeout policy"
    - from: internal/api/client.go
      to: internal/api/episode.go
      via: "newRequest/Do path injects auth and enforces one retry"
---

<objective>
Wire a single configured HTTP client and a root cancellation context through the API and download entrypoints so network work has consistent timeouts, one-shot auth refresh, and clean cancellation.
</objective>

<tasks>
### Task 1
`type`: auto
`files`: main.go, internal/download/episode.go
`action`: Introduce a root context in `main`, pass it into the episode orchestration path, and keep the user-facing error path short and actionable when the root command fails.
`verify`: Run the CLI against a mocked failure and confirm the top-level error is printed once without a panic trace.
`acceptance_criteria`: `main` owns the root context and the download path can be cancelled from above.

### Task 2
`type`: auto
`files`: internal/api/client.go, internal/api/episode.go, internal/api/season.go, internal/api/license.go
`action`: Configure the shared transport with keep-alive and idle connection reuse, keep timeout behavior explicit, and enforce exactly one token refresh plus retry on 401.
`verify`: Use an httptest server to return 401 once and confirm the second request succeeds, then return an error if it fails again.
`acceptance_criteria`: The API layer has one client path, one retry budget, and deterministic timeout behavior.

### Task 3
`type`: auto
`files`: internal/api/client_test.go, internal/api/episode_test.go, internal/api/season_test.go, internal/api/license_test.go
`action`: Add tests for the 401 refresh budget, request cancellation, and the client configuration path that the API methods now use.
`verify`: `go test ./internal/api ./internal/download`
`acceptance_criteria`: HTTP retry and cancellation behavior are covered before the next phase depends on them.
</tasks>

<verification>
- `go test ./internal/api ./internal/download`
- Confirm one 401 retry only.
- Confirm request cancellation propagates through API calls.
- Confirm the configured transport is exercised instead of the zero-value default client.
</verification>

<success_criteria>
- The project uses one shared configured HTTP client instead of ad hoc client behavior.
- HTTP requests have explicit timeout and cancellation behavior.
- A single auth refresh retry is the maximum for 401 handling.
- The main entrypoint passes cancellation downward instead of letting requests hang.
- The plan covers PERF-02, PERF-03, PERF-07, and QOL-06.
</success_criteria>
