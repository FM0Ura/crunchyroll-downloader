---
phase: 05-testing-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - testutil/factories.go
  - internal/media/testdata/mpd/simple-video-audio.mpd
  - internal/media/testdata/mpd/multi-audio.mpd
  - internal/media/testdata/mpd/with-content-protection.mpd
  - internal/media/testdata/mpd/no-content-protection.mpd
  - internal/api/testdata/api/episode-info-response.json
  - internal/api/testdata/api/season-list-response.json
  - internal/api/testdata/api/episode-playback-response.json
  - internal/api/testdata/api/license-response.json
  - internal/locale/locale_test.go
  - main_test.go
  - internal/config/config_test.go
  - internal/download/episode_test.go
  - internal/media/segment_test.go
  - internal/media/manifest_test.go
  - internal/drm/drm_test.go
  - internal/output/output_test.go
  - internal/mux/mux_test.go
  - internal/download/season_test.go
autonomous: true
requirements:
  - QOL-01

must_haves:
  truths:
    - "All pure functions (parseLangs, BuildUrl, ExpandTimeline, parseDotenv, formatFileSize, formatDuration, formatSpeed, formatETAShort, resolveString, isAllNilConfig) have table-driven test coverage following Go stdlib patterns"
    - "internal/locale/ package achieves >=90% coverage — LanguageNames and LanguageCodes maps verified for all 26 entries"
    - "main.go pure functions (parseLangs, resolveString, isAllNilConfig) achieve >=60% coverage"
    - "internal/config/ dotenv functions (LoadDotenv, parseDotenv) achieve >=90% coverage"
    - "internal/media/ functions (BuildUrl, ExpandTimeline, ParseManifest, formatSpeed, formatETAShort) achieve >=90% coverage"
    - "internal/download/ format functions (formatFileSize, formatDuration) achieve 100% coverage"
    - "Integration tests use httptest.NewServer to mock Crunchyroll API endpoints (auth, playback, CMS) for processURL and DoRequest retry paths"
    - "testutil package provides factory functions for constructing EpisodeInfo, MPD, and SeasonEpisode structs programmatically"
    - "testdata/ directories exist at internal/media/testdata/mpd/ and internal/api/testdata/api/ with fixture files"
  artifacts:
    - testutil/factories.go
    - internal/locale/locale_test.go
    - internal/media/testdata/mpd/simple-video-audio.mpd
    - internal/media/testdata/mpd/multi-audio.mpd
    - internal/media/testdata/mpd/with-content-protection.mpd
    - internal/media/testdata/mpd/no-content-protection.mpd
    - internal/api/testdata/api/episode-info-response.json
    - internal/api/testdata/api/season-list-response.json
    - internal/api/testdata/api/episode-playback-response.json
    - internal/api/testdata/api/license-response.json
  key_links:
    - "locale_test.go must verify all 26 locale entries in both LanguageNames and LanguageCodes maps — any missing entry is a coverage gap"
    - "BuildUrl tests must exercise \$Number\$, \$Number%05d\$, and \$RepresentationID\$ substitution patterns"
    - "ExpandTimeline tests must cover R=0, R>0, nil R, empty timeline, and multiple S elements"
    - "ParseManifest tests must load real MPD XML from testdata/ and verify decoded struct fields"
    - "httptest tests must mock auth/v1/token for token refresh, playback/v3 for manifest, and content/v2/cms for episode/season data"
    - "processURL integration test must exercise /watch/ path with httptest mock for GetEpisodeInfo"
---

<objective>
Write comprehensive test suite covering all pure functions, integration paths, and edge cases across the entire codebase.

Purpose: Raise aggregate code coverage from 47.5% to 60%+ by closing the highest-ROI gaps: internal/locale (0%), main.go parseLangs (0%), internal/config dotenv (42.7%), internal/media (55%), internal/download (30.8%). Deliver test infrastructure (testutil factories, testdata fixtures) that enables maintainable testing.
Output: 9 testdata fixture files, 1 factory file, 1 new test file, 9 extended test files with table-driven tests and httptest integration tests.
</objective>

<execution_context>
@/home/fmoura/Documents/Applications/crunchyroll-downloader/.opencode/gsd-core/workflows/execute-plan.md
@/home/fmoura/Documents/Applications/crunchyroll-downloader/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-testing-quality/05-CONTEXT.md
@.planning/phases/05-testing-quality/05-RESEARCH.md

# Source files under test
@main.go
@internal/locale/locale.go
@internal/config/config.go
@internal/config/dotenv.go
@internal/media/manifest.go
@internal/media/segment.go
@internal/download/episode.go
@internal/download/season.go
@internal/drm/drm.go
@internal/output/output.go
@internal/output/speed.go
@internal/mux/mux.go

# Existing test patterns to follow
@main_test.go
@internal/api/client_test.go
@internal/media/segment_test.go
@internal/media/manifest_test.go
@internal/mux/mux_test.go
@internal/download/episode_test.go
@internal/download/season_test.go
@internal/drm/drm_test.go
@internal/config/config_test.go
@internal/output/output_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test infrastructure — testutil factories and testdata fixtures</name>

  <files>
    testutil/factories.go
    internal/media/testdata/mpd/simple-video-audio.mpd
    internal/media/testdata/mpd/multi-audio.mpd
    internal/media/testdata/mpd/with-content-protection.mpd
    internal/media/testdata/mpd/no-content-protection.mpd
    internal/api/testdata/api/episode-info-response.json
    internal/api/testdata/api/season-list-response.json
    internal/api/testdata/api/episode-playback-response.json
    internal/api/testdata/api/license-response.json
  </files>

  <read_first>
    @internal/api/episode.go — EpisodeInfo, EpisodeMetadata, DubVersion struct definitions
    @internal/api/season.go — SeasonEpisode, Season struct definitions
    @internal/media/manifest.go — GetVideoBaseUrl, GetAudioBaseUrl, ParseManifest signatures
    @internal/media/segment.go — BuildUrl, DownloadPart, DownloadParts signatures
    @internal/media/manifest_test.go — existing MPD struct usage patterns (go-mpd types)
    @internal/mux/mux_test.go — testEpisodeInfo() factory pattern for reference
  </read_first>

  <action>
    Create testutil/factories.go with exported factory functions per D-09. The package name is testutil, located at project root (not internal/). Functions to expose:
    - EpisodeInfo() *api.EpisodeInfo — returns a default EpisodeInfo with SeriesTitle "Test Series", SeasonNumber 1, EpisodeNumber 1, AudioLocale "ja-JP", Title "Test Episode". No Versions slice.
    - EpisodeInfoWithVersions(locales ...string) *api.EpisodeInfo — returns EpisodeInfo with Versions populated: each locale gets a DubVersion with GUID = locale + "-guid".
    - SeasonEpisode(episodeNumber int, seriesTitle string, seasonNumber int) api.SeasonEpisode — returns a minimal SeasonEpisode with ID "episode-N", given episodeNumber, seriesTitle, seasonNumber, AudioLocale "ja-JP", Title "Test Episode N".
    - DummyMPD() *mpd.MPD — returns a minimal go-mpd MPD struct with one Period containing one video AdaptationSet (Height: 1080, Bandwidth: 5000000, BaseURL: "https://example.com/video/1080p/") and one audio AdaptationSet (Bandwidth: 192000, BaseURL: "https://example.com/audio/192k/"). Both sets have SegmentTemplate with Initialization="init-\$RepresentationID$.mp4" and Media="seg-\$Number%05d\$-\$RepresentationID$.m4s", SegmentTimeline with one S element {D: 1}.

    Create internal/media/testdata/mpd/ with 4 MPD XML fixtures per D-07/D-08:
    - simple-video-audio.mpd: Valid MPD with one video AdaptationSet (mimeType video/mp4, height 1080, bandwidth 5000000, codecs avc1.640028, BaseURL "https://example.com/video/1080p/") and one audio AdaptationSet (mimeType audio/mp4, bandwidth 192000, codecs mp4a.40.2, BaseURL "https://example.com/audio/192k/"). Both use SegmentTemplate with initialization/segment URL patterns. Must include MPD xmlns, profiles, type, minBufferTime, publishTime, availabilityStartTime attributes.
    - multi-audio.mpd: Like simple-video-audio.mpd but with 3 audio Representations (bandwidths 192000, 128000, 96000, with IDs "audio-192", "audio-128", "audio-96").
    - with-content-protection.mpd: Single video AdaptationSet with a ContentProtection element containing schemeIdUri="urn:uuid:edef8ba9-79d6-4ace-a3c8-27dcd51d21ed" and a cenc:pssh element with a valid-looking base64 PSSH box inside. The PSSH data should be parseable by go-mpd (the library preserves the raw XML).
    - no-content-protection.mpd: Same as simple-video-audio.mpd structure but explicitly without any ContentProtection elements.

    Create internal/api/testdata/api/ with 4 JSON fixture files per D-08:
    - episode-info-response.json: Full Crunchyroll GetEpisodeInfo JSON response. Structure: top-level object with "data" key containing EpisodeInfo-like fields: id, series_title, season_number, episode_number, title, audio_locale, slug, description, episode_air_date, season_slug_id, series_slug_id, available_offline, versions (array with one dub version: audio_locale "en-US", guid "en-us-guid", original false).
    - season-list-response.json: Full GetSeasons JSON response. Array of season objects with id, season_number, slug_title, series_title, hide_season_in_steins.
    - episode-playback-response.json: Full GetEpisode playback JSON. Top-level "data" with "manifest_url" (string URL to MPD), "token" (string stream token), "subtitles" (object with locale keys, each with "url", "locale", "format", "title"), and "versions" array.
    - license-response.json: Full SendChallenge license response. Top-level "data" with "token" (string), "cid" (content ID), "license" (base64-encoded license data blob — use a placeholder base64 string like "AAAAbGljZW5zZURhdGFIZXJlCg==").
  </action>

  <verify>
    <automated>go build ./testutil/ && for f in $(find internal/media/testdata internal/api/testdata -type f); do [[ -s "$f" ]] || { echo "Empty fixture: $f"; exit 1; }; done; echo "All fixtures valid"</automated>
  </verify>

  <acceptance_criteria>
    - testutil/factories.go compiles without errors: `go build ./testutil/` exits 0
    - testutil/factories.go can be imported from root-package tests (package main) — verify with `go vet ./testutil/`
    - EpisodeInfo() returns non-nil *api.EpisodeInfo with SeriesTitle, SeasonNumber, EpisodeNumber, AudioLocale, Title populated
    - EpisodeInfoWithVersions("en-US", "pt-BR") returns an EpisodeInfo with 2 DubVersion entries
    - DummyMPD() returns a non-nil *mpd.MPD with at least one Period containing 2 AdaptationSets
    - All 8 fixture files are non-empty and syntactically valid (MPD XML or JSON as appropriate)
    - MPD XML files can be parsed by go-mpd: `mpd.Decode(data)` on the file contents
    - JSON fixture files can be unmarshaled to appropriate Go types
  </acceptance_criteria>
  <done>testutil/factories.go compiles (go build), all 8 fixture files exist and are non-empty, factory functions return non-nil structs with expected default fields</done>
</task>

<task type="auto">
  <name>Task 2: Add pure function unit tests for high-priority coverage gaps</name>

  <files>
    internal/locale/locale_test.go
    main_test.go
    internal/config/config_test.go
    internal/download/episode_test.go
    internal/media/segment_test.go
  </files>

  <read_first>
    @internal/locale/locale.go — LanguageNames and LanguageCodes map contents (26 entries each)
    @main.go — parseLangs(), resolveString(), isAllNilConfig() function signatures and behavior
    @internal/config/dotenv.go — LoadDotenv() and parseDotenv() function signatures
    @internal/config/config.go — Config struct definition, Merge behavior
    @internal/download/episode.go — formatFileSize(), formatDuration() function signatures
    @internal/media/segment.go — formatSpeed(), formatETAShort() function signatures
    @main_test.go — existing test patterns: captureMainStdout(), captureMainStderr(), t.Run with subtests
    @internal/config/config_test.go — existing table-driven test patterns, t.TempDir(), os.WriteFile usage
    @internal/download/episode_test.go — existing t.Chdir(), table-driven sanitizeFilename test patterns
    @internal/media/segment_test.go — existing fakeDoer pattern, func okResponse(), func TestDownloadPartUsesInjectedClient
    @internal/media/manifest_test.go — existing TestMPDCacheMiss, TestMPDCacheHit patterns
  </read_first>

  <action>
    Per D-03 (table-driven tests with Go stdlib, no testify), add the following test functions to the specified files.

    **Create internal/locale/locale_test.go (NEW)** — package locale:
    - TestLanguageNames: Table-driven test covering ALL 26 entries in the LanguageNames map. Each test case: {name string, locale string, want string}. Verify that LanguageNames[locale] returns want for every entry. Add edge case: lookup of missing locale returns empty string.
    - TestLanguageCodes: Table-driven test covering ALL 26 entries in the LanguageCodes map. Same structure. Verify that LanguageCodes[locale] returns want for every entry. Add edge case: lookup of missing locale returns empty string.

    **Extend main_test.go** — package main:
    - TestParseLangs: Table-driven, test cases: "ja-JP" → [ja-JP], "ja-JP,en-US" → [ja-JP en-US], " ja-JP , en-US " (with whitespace) → [ja-JP en-US], "" → nil/empty, single locale with whitespace, multiple locales with mixed whitespace. Use reflect.DeepEqual for slice comparison.
    - TestResolveString: Table-driven for the precedence hierarchy. Build explicitFlags map to simulate flag.Visit(). Test cases: (1) explicit flag wins over everything, (2) env var wins over config val when flag not set, (3) config val wins over default when flag not set and env not set, (4) default returned when nothing is set.
    - TestIsAllNilConfig: Table-driven. Test cases: (1) all-nil Config → true, (2) Config with only AudioLang set → false, (3) Config with only Workers set → false, (4) Config with all fields populated → false.

    **Extend internal/config/config_test.go** — package config:
    - TestParseDotenvEmptyFile: Create empty .env file, call parseDotenv, verify no error.
    - TestParseDotenvCommentsAndBlanks: Create .env with comments (# lines) and blank lines, verify they are skipped.
    - TestParseDotenvKeyValue: Create .env with "KEY=value", call parseDotenv, verify os.Getenv("KEY") == "value".
    - TestParseDotenvQuotedValues: Create .env with KEY="quoted value" and KEY2='single quoted', verify values have quotes stripped (per existing dotenv.go behavior).
    - TestParseDotenvNoEquals: Create .env with a line without "=", verify it's silently skipped.
    - TestLoadDotenvFound: Write a .env file in a temp directory, chdir there (use t.Chdir), call LoadDotenv, verify it returns the path.
    - TestLoadDotenvNotFound: t.Chdir to a temp directory with no .env file, call LoadDotenv, verify it returns empty string with nil error.

    **Extend internal/download/episode_test.go** — package download:
    - TestFormatFileSize: Table-driven. Test cases at byte boundaries: 500 B → "500 B", 1024 B → "1.0 KB", 1536 B → "1.5 KB", 5242880 (5 MB) → "5.0 MB", 1073741824 (1 GB) → "1.0 GB", 0 → "0 B", negative → default format.
    - TestFormatDuration: Table-driven. Test cases: 0s → "0s", 5s → "5s", 60s → "1m 0s", 150s → "2m 30s", 3600s → "60m 0s", -1s → "0s" (negative clamped by time.Duration.Round).

    **Extend internal/media/segment_test.go** — package media:
    - TestFormatSpeed: Table-driven. Test cases at byte boundaries: 500 Bps → "500 B/s", 2048 Bps → "2.0 KB/s", 5242880 Bps → "5.0 MB/s", 1073741824 Bps → "1.0 GB/s", 0 → "0 B/s", negative → "0 B/s".
    - TestFormatETAShort: Table-driven. Test cases: 0 → "0s", 30 → "30s", 60 → "1m 0s", 150 → "2m 30s", -1 → "0s".
    - TestGetFilename: Table-driven for getFilename(). Test nil set → creates temp file with crdl-subs-*.ass pattern. Test set with video representation (Height non-nil) → creates crdl-video-*.mp4. Test set with audio representation (Bandwidth non-nil) → creates crdl-audio-*.mp3. Test empty set (no representations) → returns error.
  </action>

  <verify>
    <automated>go test -count=1 -run "TestParseLangs|TestResolveString|TestIsAllNilConfig|TestLanguageNames|TestLanguageCodes|TestParseDotenv|TestLoadDotenv|TestFormatFileSize|TestFormatDuration|TestFormatSpeed|TestFormatETAShort|TestGetFilename" . ./internal/locale/ ./internal/config/ ./internal/download/ ./internal/media/ -v 2>&1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
    - internal/locale/locale_test.go exists, compiles, and all 26 entries in both maps are verified
    - parseLangs tests pass for single, multiple, whitespace, and empty input cases
    - resolveString tests verify the 4-level precedence (flag > env > config > default)
    - isAllNilConfig tests verify true for all-nil and false if any single field is set
    - parseDotenv tests verify comments, blanks, key=value, quoted values, and missing-equals handling
    - LoadDotenv tests verify found and not-found paths
    - formatFileSize tests verify B/KB/MB/GB boundary values and edge cases
    - formatDuration tests verify seconds, minutes, zero, and negative inputs
    - formatSpeed tests verify B/s, KB/s, MB/s, GB/s boundary values
    - formatETAShort tests verify seconds, minutes, zero, and negative inputs
    - GetFilename tests verify all 4 paths (nil, video, audio, empty)
    - All tests pass when run in isolation: `go test -count=1 -run "TestParseLangs|TestResolveString|..." -v`
  </acceptance_criteria>
  <done>All new test functions compile, run, and pass; locale_test.go verifies all 26 map entries; parseLangs/resolveString/isAllNilConfig/formatFileSize/formatDuration/formatSpeed/formatETAShort/GetFilename tests cover edge cases</done>
</task>

<task type="auto">
  <name>Task 3: Add integration tests and extended unit tests for media, DRM, API, and orchestrator packages</name>

  <files>
    internal/media/manifest_test.go
    main_test.go
    internal/drm/drm_test.go
    internal/output/output_test.go
    internal/mux/mux_test.go
    internal/download/season_test.go
  </files>

  <read_first>
    @internal/media/manifest.go — ExpandTimeline(), ParseManifest() function signatures
    @internal/media/segment.go — BuildUrl() function signature
    @main.go — processURL() function signature, validateURL(), validateOutputDir()
    @internal/drm/drm.go — GetPssh() function signature
    @internal/output/output.go — humanOutput, jsonOutput, quietOutput implementations
    @internal/output/speed.go — SpeedTracker data structures
    @internal/mux/mux.go — TrackTitle() function, MergeEverything() signature
    @internal/download/season.go — runSeason() signature, SeasonError type, episodeDownloader type
    @internal/api/client_test.go — newTestClient() helper, httptest patterns for auth/token
    @internal/media/manifest_test.go — existing tests for GetVideoBaseUrl, GetAudioBaseUrl, MPD cache
    @internal/drm/drm_test.go — existing patterns: resetWidevineDeviceCache, clearWidevineEnv, chdirTemp, writeFile helpers
    @internal/output/output_test.go — existing captureStdout, captureStderr patterns
    @internal/media/testdata/mpd/ — fixture files from Task 1 (now exist)
    @internal/api/testdata/api/ — fixture files from Task 1 (now exist)
  </read_first>

  <action>
    Extend the following test files. Per D-03, use table-driven tests with Go stdlib. Per D-04, use httptest for HTTP mocking. Do not add testify dependency.

    **Extend internal/media/manifest_test.go** — package media:
    - TestBuildUrl: Table-driven, test BuildUrl with different substitution patterns. Cases: no part number (nil), with part number (int64 pointer) for $Number%05d$, $RepresentationID$ substitution, $Number$ (without %05d), edge case empty base URL. Use the same test signature pattern as the RESEARCH.md examples.
    - TestExpandTimeline: Table-driven. Cases: single S element with D=1 (startNumber 1 → [1]), S element with R=2 (repeat count 3 total: [1,2,3]), S element with nil R (treated as R=0: [1]), empty timeline → [], multiple S elements [S{D:1}, S{D:1,R:&two}] → [1,2,3], zero D value.
    - TestParseManifest: Load simple-video-audio.mpd from testdata using os.ReadFile (relative path from test file: "testdata/mpd/simple-video-audio.mpd"). Call ParseManifest, verify no error, verify mpd.Period[0].AdaptationSets has length 2, verify first AdaptationSet has video mimeType and second has audio mimeType.
    - TestParseManifestInvalidXML: Call ParseManifest with []byte("invalid"), verify error is non-nil.

    **Extend main_test.go** — package main:
    - Add processURL integration test using httptest.NewServer. Create a test server that handles:
      - /auth/v1/token → returns 200 with `{"access_token":"test-token"}`
      - /content/v2/cms/objects/G123456789 — for GetEpisodeInfo: returns 200 with valid episode JSON
      - /playback/v3/... — returns 200 with playback JSON containing manifest_url pointing to MPD and a token
    - TestProcessURLWatchPath: Call processURL with the httptest server URL, a /watch/ URL, audioLangs ["ja-JP"], subsLangs ["en-US"]. Since processURL internally calls client.GetEpisodeInfo which hits the mock server, the test should verify the flow proceeds to download.Episode. Use a cancelled context so the test completes quickly (download.Episode fails fast on cancelled context).
    - Use captureMainStderr or captureMainStdout to capture output for verification.

    **Extend internal/drm/drm_test.go** — package drm:
    - TestGetPsshWithProtection: Build a minimal MPD struct (via go-mpd types or testutil.DummyMPD) that includes a ContentProtection element on the first AdaptationSet. Call GetPssh(manifest), verify the returned PSSH data is non-nil.
    - TestGetPsshWithoutProtection: Build a minimal MPD struct without ContentProtection. Call GetPssh, verify it returns nil.

    **Extend internal/output/output_test.go** — package output:
    - Note: TrackTitle is in internal/mux/ not internal/output/. Skip TrackTitle here.
    - TestHumanOutputDebug: Already exists — no addition needed.
    - TestQuietModeWarn: Already exists — no addition needed.
    - Focus on edge cases already identified in research: clampETA/min edge cases for SpeedTracker.

    **Extend internal/mux/mux_test.go** — package mux:
    - TestTrackTitle: Table-driven. Test known locale "ja-JP" → "日本語", "en-US" → "English", unknown locale "xx-XX" → "xx-XX" (returns the code itself per TrackTitle implementation).
    - TestTrackTitleAllLocales: Verify that every key in LanguageNames map returns a non-empty string via TrackTitle.

    **Extend internal/download/season_test.go** — package download:
    - TestRunSeasonEmptyEpisodes: Call runSeason with empty episodes slice, verify it returns nil immediately (no downloads attempted). Use a mock download func that t.Fatals if called.
    - TestSeasonErrorFormatting: Create a SeasonError with Failed=2, Total=5, err=someError. Verify Error() returns "2 of 5 episode(s) failed". Verify Unwrap() returns the underlying error.
    - TestFormatFailedList: Create a slice of episodeError entries, call formatFailedList, verify it produces a semicolon-joined string.
  </action>

  <verify>
    <automated>go test -count=1 -run "TestBuildUrl|TestExpandTimeline|TestParseManifest|TestProcessURL|TestGetPssh|TestTrackTitle|TestRunSeasonEmpty|TestSeasonError" . ./internal/media/ ./internal/drm/ ./internal/mux/ ./internal/download/ -v 2>&1 | tail -40</automated>
  </verify>

  <acceptance_criteria>
    - BuildUrl tests pass for all substitution patterns ($Number$, $Number%05d$, $RepresentationID$, nil partNum)
    - ExpandTimeline tests pass for R=0, R>0, nil R, empty timeline, multiple S elements
    - ParseManifest loads and decodes real MPD XML fixtures from testdata/
    - ParseManifest returns error for invalid XML input
    - processURL integration test uses httptest and captures output via captureMainStderr
    - GetPssh tests verify both with-protection (non-nil) and without-protection (nil) paths
    - TrackTitle tests verify known locales return display names, unknown return the code itself
    - runSeason with empty episodes returns nil without calling the download function
    - SeasonError.Error() and Unwrap() behave correctly
    - All extended test files compile without errors
  </acceptance_criteria>
  <done>BuildUrl/ExpandTimeline/ParseManifest tests pass with table-driven edge cases; processURL httptest verifies watch-path flow; GetPssh tests cover both content-protection and no-protection; TrackTitle verifies known+unknown locales; season edge cases (empty, error formatting) pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| testdata→test | Test data fixtures are read from disk by unit tests — no untrusted data |
| httptest→test | Mock HTTP responses are crafted by tests — no real API calls made |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | testdata/ MPD/JSON fixtures | low | mitigate | All fixture data is hand-crafted mock data — no real credentials, cookies, or tokens. Verify no real etp_rt value accidentally committed via grep before commit. |
| T-05-02 | Information Disclosure | httptest test output | low | accept | Test output is only visible in CI logs. No real credentials are used — all tokens are "initial-token" or "test-token" string literals. |
</threat_model>

<verification>
- All test files compile: `go build ./internal/... ./testutil/`
- All tests pass: `go test -count=1 -race ./internal/... ./testutil/`
- Root package tests pass: `go test -count=1 -race .`
- Per-package coverage targets verified: `go test -coverprofile=coverage.out ./... && go tool cover -func=coverage.out`
- Testdata fixtures are valid: MPD XML can be parsed by go-mpd, JSON can be unmarshaled
</verification>

<success_criteria>
- 9 testdata fixture files created and validated (4 MPD + 4 API JSON + testutil package OK)
- All 3 tasks produce test functions that pass
- Aggregate coverage reaches target ranges per D-01 risk-based model
- `go test -count=1 -race ./...` exits 0
- All new tests follow established patterns: table-driven, no testify, httptest for HTTP, t.Cleanup for package-level state
</success_criteria>

## Artifacts This Phase Produces

| File | Symbol | Type |
|------|--------|------|
| `testutil/factories.go` | `func EpisodeInfo()` | Factory function |
| `testutil/factories.go` | `func EpisodeInfoWithVersions(...)` | Factory function |
| `testutil/factories.go` | `func SeasonEpisode(...)` | Factory function |
| `testutil/factories.go` | `func DummyMPD()` | Factory function |
| `internal/locale/locale_test.go` | `TestLanguageNames` | Test function |
| `internal/locale/locale_test.go` | `TestLanguageCodes` | Test function |
| `main_test.go` | `TestParseLangs` | Test function |
| `main_test.go` | `TestResolveString` | Test function |
| `main_test.go` | `TestIsAllNilConfig` | Test function |
| `main_test.go` | `TestProcessURLWatchPath` | Test function |
| `internal/config/config_test.go` | `TestParseDotenv*` (5 tests) | Test functions |
| `internal/config/config_test.go` | `TestLoadDotenv*` (2 tests) | Test functions |
| `internal/download/episode_test.go` | `TestFormatFileSize` | Test function |
| `internal/download/episode_test.go` | `TestFormatDuration` | Test function |
| `internal/media/segment_test.go` | `TestFormatSpeed` | Test function |
| `internal/media/segment_test.go` | `TestFormatETAShort` | Test function |
| `internal/media/segment_test.go` | `TestGetFilename` | Test function |
| `internal/media/manifest_test.go` | `TestBuildUrl` | Test function |
| `internal/media/manifest_test.go` | `TestExpandTimeline` | Test function |
| `internal/media/manifest_test.go` | `TestParseManifest` | Test function |
| `internal/media/manifest_test.go` | `TestParseManifestInvalidXML` | Test function |
| `internal/drm/drm_test.go` | `TestGetPsshWithProtection` | Test function |
| `internal/drm/drm_test.go` | `TestGetPsshWithoutProtection` | Test function |
| `internal/mux/mux_test.go` | `TestTrackTitle` | Test function |
| `internal/mux/mux_test.go` | `TestTrackTitleAllLocales` | Test function |
| `internal/download/season_test.go` | `TestRunSeasonEmptyEpisodes` | Test function |
| `internal/download/season_test.go` | `TestSeasonErrorFormatting` | Test function |
| `internal/download/season_test.go` | `TestFormatFailedList` | Test function |
| `internal/media/testdata/mpd/` | 4 MPD XML fixture files | Testdata |
| `internal/api/testdata/api/` | 4 JSON API fixture files | Testdata |

<output>
Create `.planning/phases/05-testing-quality/05-01-SUMMARY.md` when done
</output>
