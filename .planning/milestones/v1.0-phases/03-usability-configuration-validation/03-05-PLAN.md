---
phase: 03-usability-configuration-validation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/api/auth.go
  - internal/api/auth_test.go
autonomous: true
requirements: [USAB-07]
must_haves:
  truths:
    - "User can set CRUNCHYROLL_CLIENT_AUTH env var to override the hardcoded Basic Auth credential"
    - "When CRUNCHYROLL_CLIENT_AUTH is unset, the existing hardcoded credential is used as fallback"
    - "The env var is checked at every fetchAccessToken call (not cached once at startup)"
    - "The hardcoded credential is documented as a public client ID with its source annotated"
  artifacts:
    - path: "internal/api/auth.go"
      provides: "Updated fetchAccessToken with getClientAuth() function"
      contains: "getClientAuth"
      min_lines: 55
  key_links:
    - from: "internal/api/auth.go"
      to: "os.LookupEnv"
      via: "getClientAuth() calls os.LookupEnv(\"CRUNCHYROLL_CLIENT_AUTH\") — matches established env var pattern from internal/drm/drm.go:126-131"
      pattern: "LookupEnv.*CRUNCHYROLL_CLIENT_AUTH"
---

<objective>
Add CRUNCHYROLL_CLIENT_AUTH env var override for the hardcoded Basic Auth credential in auth.go.

**Purpose:** Crunchyroll can rotate the client credential at any time, breaking the downloader. The `CRUNCHYROLL_CLIENT_AUTH` env var allows users to override the default without recompiling. The hardcoded value is documented as the public default.

**Output:**
- `internal/api/auth.go` — Updated with getClientAuth() function and env var override
</objective>

<execution_context>
@/home/fmoura/Documents/Applications/crunchyroll-downloader/.opencode/gsd-core/workflows/execute-plan.md
@/home/fmoura/Documents/Applications/crunchyroll-downloader/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-usability-configuration-validation/03-CONTEXT.md
@.planning/phases/03-usability-configuration-validation/03-RESEARCH.md
@.planning/phases/03-usability-configuration-validation/03-PATTERNS.md
@internal/api/auth.go
@internal/drm/drm.go:126-132 (established os.LookupEnv pattern)
</context>

<tasks>

<task type="auto">
<name>Task 1: Add getClientAuth() function and CRUNCHYROLL_CLIENT_AUTH env var support to auth.go</name>
<files>internal/api/auth.go</files>

<read_first>
internal/api/auth.go:1-51 (entire file — current fetchAccessToken with hardcoded auth at line 28)
03-RESEARCH.md:528-544 (getClientAuth code example)
03-PATTERNS.md:314-336 (auth.go modification pattern)
</read_first>

<action>
Modify internal/api/auth.go:

1. Add `import "os"` to the imports block.
2. Add a package-level constant `defaultClientAuth` with value `"Basic bm9haWhkZXZtXzZpeWcwYThsMHE6"` and a comment documenting: "Public Crunchyroll client identifier — documented here so users know the default value. If Crunchyroll rotates this credential, set CRUNCHYROLL_CLIENT_AUTH env var to the new value."
3. Add `getClientAuth() string` function:
   - Check `os.LookupEnv("CRUNCHYROLL_CLIENT_AUTH")` — if found and not empty, return the env value.
   - Otherwise return `defaultClientAuth`.
   - Per D-08 and RESEARCH.md code example at lines 528-544.
4. On line 28, replace the hardcoded `"Basic bm9haWhkZXZtXzZpeWcwYThsMHE6"` string with `getClientAuth()` call.
5. Add a test file `internal/api/auth_test.go`:
   - `TestGetClientAuthReturnsDefault` — when env var is unset, returns defaultClientAuth.
   - `TestGetClientAuthPrefersEnv` — when CRUNCHYROLL_CLIENT_AUTH is set, returns env value.
   - `TestGetClientAuthEmptyEnvFallsBack` — when CRUNCHYROLL_CLIENT_AUTH is set to empty string, returns default.
   - Use `t.Setenv()` for env var manipulation (cleans up automatically).

Use the exact function signature from RESEARCH.md code example. The env value must be checked every call (not cached) so credential rotation takes effect without restart.
</action>

<verify>
<automated>go test ./internal/api/... -run TestGetClientAuth -count=1 -v 2>&1</automated>
</verify>

<acceptance_criteria>
- getClientAuth() returns defaultClientAuth when CRUNCHYROLL_CLIENT_AUTH is unset
- getClientAuth() returns env var value when CRUNCHYROLL_CLIENT_AUTH is set to non-empty string
- getClientAuth() returns defaultClientAuth when CRUNCHYROLL_CLIENT_AUTH is set to empty string
- existing `go test ./internal/api/...` tests still pass (no regressions)
- constant defaultClientAuth is documented with comment about public client ID status
</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env var → auth.go | CRUNCHYROLL_CLIENT_AUTH env var value used as HTTP Basic Auth header |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.5-01 | Information Disclosure | CRUNCHYROLL_CLIENT_AUTH | low | accept | Env var is per-process; not written to disk or logs; same risk profile as other auth env vars |
| T-03.5-02 | Denial of Service | Hardcoded credential rotation | medium | mitigate | USAB-07 adds env var override; user can update without source change |
| T-03.5-03 | Tampering | defaultClientAuth constant | low | accept | Constant is public compile-time value; env var override is the intended escape hatch |
</threat_model>

<verification>
<automated>go test ./internal/api/... -count=1 -v 2>&1 | tail -10</automated>
</verification>

<success_criteria>
- Hardcoded auth on line 28 replaced with `getClientAuth()` call
- `defaultClientAuth` constant preserves the original value as documented default
- CRUNCHYROLL_CLIENT_AUTH env var correctly overrides when set
- All API package tests pass
- No changes to other files
</success_criteria>

<output>
Create `.planning/phases/03-usability-configuration-validation/03-05-SUMMARY.md` when done
</output>

## Artifacts This Phase Produces

| Symbol | Kind | File | Consumers |
|--------|------|------|-----------|
| `defaultClientAuth` | const | internal/api/auth.go | getClientAuth() |
| `getClientAuth()` | func | internal/api/auth.go | fetchAccessToken() |
| `CRUNCHYROLL_CLIENT_AUTH` | env var | internal/api/auth.go | os.LookupEnv in getClientAuth() |
</output>
