---
phase: 03-usability-configuration-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/config/config.go
  - internal/config/config_test.go
  - main.go
  - main_test.go
autonomous: true
requirements: [USAB-01, USAB-02]
must_haves:
  truths:
    - "User can set CRUNCHYROLL_ETP_RT env var instead of --etp-rt flag"
    - "User has persistent config at ~/.config/animeheaven/config.json with sane defaults"
    - "Config discovery follows XDG spec with fallback to os.UserConfigDir()"
    - "Missing config file auto-generates a skeleton with defaults and prints a notice"
    - "Invalid JSON config warns on stderr and falls through to defaults instead of crashing"
    - "Unset config fields (nil pointers) fall through to defaults — only explicitly-set fields override"
    - "CLI flag > env var > config file > default precedence is enforced for all resolved values"
    - "CRUNCHYROLL_ETP_RT env var is resolved in the precedence chain before api.NewWithContext"
  artifacts:
    - path: "internal/config/config.go"
      provides: "Config struct, Load, Save, ConfigDir, Merge, WriteSkeleton"
      exports: ["Config", "ConfigDir", "ConfigPath", "Load", "WriteSkeleton", "Merge"]
      min_lines: 120
    - path: "internal/config/config_test.go"
      provides: "Tests for ConfigDir, Load, Merge, WriteSkeleton"
      contains: "func Test*"
      min_lines: 100
  key_links:
    - from: "main.go"
      to: "internal/config/config.go"
      via: "config.Load(cfgPath) called after flag.Parse() — config struct used in precedence resolution"
      pattern: "config\\."
    - from: "main.go"
      to: "internal/api/client.go"
      via: "api.NewWithContext(ctx, resolvedEtpRt) — etpRt comes from precedence chain, not raw flag"
      pattern: "etpRt|NewWithContext"
---

<objective>
Create the persistent configuration infrastructure and env var support for the CLI tool.

**Purpose:** Users can now configure the tool via a JSON config file at `~/.config/animeheaven/config.json` and environment variables, with a strict CLI flag > env var > config file > default precedence hierarchy. The `CRUNCHYROLL_ETP_RT` env var replaces the need to pass `--etp-rt` on every invocation.

**Output:**
- `internal/config/config.go` — Config struct (pointer fields), Load/Save/Merge/ConfigDir/WriteSkeleton
- `internal/config/config_test.go` — Full unit test coverage for config operations
- `main.go` — Updated with config loading, env var resolution, precedence merge, CRUNCHYROLL_ETP_RT support
- `main_test.go` — Updated with test for etpRt resolution
</objective>

<execution_context>
@/home/fmoura/Documents/Applications/crunchyroll-downloader/.opencode/gsd-core/workflows/execute-plan.md
@/home/fmoura/Documents/Applications/crunchyroll-downloader/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md

@.planning/phases/03-usability-configuration-validation/03-CONTEXT.md
@.planning/phases/03-usability-configuration-validation/03-RESEARCH.md
@.planning/phases/03-usability-configuration-validation/03-PATTERNS.md
@.planning/phases/03-usability-configuration-validation/03-VALIDATION.md

@internal/drm/drm.go
@internal/drm/drm_test.go
</context>

<tasks>

<task type="auto">
<name>Task 1: Create internal/config/config.go with pointer-field Config struct, Load, Save, Merge, ConfigDir, WriteSkeleton</name>
<files>internal/config/config.go</files>

<read_first>
internal/drm/drm.go:1-33 (package pattern, stdlib imports, error wrapping)
internal/drm/drm.go:126-132 (os.LookupEnv pattern)
internal/download/episode.go:36-42 (os.MkdirAll pattern)
03-PATTERNS.md:61-103 (config struct, imports, JSON marshal patterns)
03-RESEARCH.md:193-285 (Config struct, ConfigDir, Load, WriteSkeleton code examples)
03-RESEARCH.md:484-525 (Merge function code example)
</read_first>

<action>
Create internal/config/config.go with:
1. Package declaration `package config` — same-package for test access to unexported symbols.
2. Imports: `encoding/json`, `fmt`, `os`, `path/filepath` — stdlib only per D-01.
3. `Config` struct with pointer fields (`*string`, `*int`) and `json:",omitempty"` tags per D-05 and RESEARCH.md pattern. Fields: AudioLang, SubsLang, VideoQuality, AudioQuality, Workers, OutputDir, EtpRt, WidevineDevice.
4. `ConfigDir()` function — checks `$XDG_CONFIG_HOME` first, falls back to `os.UserConfigDir()`, appends `/animeheaven`. Follow Caddy's XDG-first pattern per D-03. Note: on macOS, `os.UserConfigDir()` returns `~/Library/Application Support` — add a comment in the function body documenting this.
5. `ConfigPath()` function — returns `filepath.Join(configDir, "config.json")`.
6. `Load(path string) (*Config, error)` — read JSON file; return `&Config{}` (all nil) on `os.IsNotExist`; on invalid JSON return the partial `&Config{}` with a wrapped error so main.go can warn (per D-04: warn on invalid, never block).
7. `WriteSkeleton(path string) error` — create config directory via `os.MkdirAll(dir, 0755)`, marshal `map[string]interface{}` with defaults `{audio_lang: "ja-JP", subs_lang: "en-US", video_quality: "1080p", audio_quality: "192k", workers: 10}` using `json.MarshalIndent` with 2-space indent, write with `os.WriteFile(path, data, 0644)`. Per D-04: generate skeleton if config file missing.
8. `Merge(base, overlay *Config) *Config` — shallow copy base, then for each pointer field in overlay, if non-nil, assign to result. This implements explicit-only override per D-05.
9. Error wrapping: use `fmt.Errorf("context: %w", err)` consistently — matches the established codebase pattern from internal/drm/drm.go.

Use the exact function signatures from the RESEARCH.md code examples (sections "Pattern 1: Pointer-based Config Struct" and "Code Examples: Config Struct with Explicit-Only Merge"). Do NOT use fenced code blocks in action text — identifiers and signatures are sufficient references.
</action>

<verify>
<automated>go build ./internal/config/...</automated>
</verify>

<acceptance_criteria>
- internal/config/config.go compiles without errors
- Config struct has 8 pointer fields (7 *string, 1 *int) with json tags
- ConfigDir tests: returns `$XDG_CONFIG_HOME/animeheaven` when env set, falls back to `os.UserConfigDir()/animeheaven`
- Load: returns all-nil Config on file not found, returns error on invalid JSON
- WriteSkeleton creates file at given path with valid JSON content
- Merge: non-nil overlay fields override base; nil overlay fields leave base unchanged
</acceptance_criteria>
</task>

<task type="auto">
<name>Task 2: Create internal/config/config_test.go with table-driven tests for all config operations</name>
<files>internal/config/config_test.go</files>

<read_first>
internal/config/config.go (just created)
internal/drm/drm_test.go:199-239 (clearWidevineEnv, chdirTemp, writeFile helpers)
internal/drm/drm_test.go:112-148 (table-driven test pattern)
03-PATTERNS.md:107-164 (test helpers and table-driven test patterns)
</read_first>

<action>
Create internal/config/config_test.go — same-package test (`package config`) per the established convention in internal/drm/ for access to unexported symbols.

Use these test helpers (copy the exact pattern from internal/drm/drm_test.go but adapted for config):
- Helper pattern: `t.Helper()`, `t.TempDir()`, `t.Cleanup()` — same as drm_test.go.

Tests to write:
1. `TestConfigDirUsesXdgConfigHome` — set `XDG_CONFIG_HOME`, verify ConfigDir returns `$XDG_CONFIG_HOME/animeheaven`.
2. `TestConfigDirFallsBackToUserConfigDir` — unset `XDG_CONFIG_HOME`, verify it uses `os.UserConfigDir()` (can only check suffix `/animeheaven` since actual path is platform-dependent).
3. `TestConfigPath` — verify path ends with `/animeheaven/config.json`.
4. `TestLoadFileNotFound` — Load() on nonexistent path returns `&Config{}` with nil error (all pointer fields nil).
5. `TestLoadInvalidJSON` — Load() on file with invalid JSON returns error AND a non-nil Config (partial decode).
6. `TestLoadValidFile` — Load() on valid JSON returns Config with correct field values.
7. `TestWriteSkeleton` — WriteSkeleton to temp path, verify file exists with valid JSON containing all expected keys.
8. `TestWriteSkeletonCreatesDirectory` — WriteSkeleton to nested path, verify directory + file created.
9. `TestMergeNilOverlay` — Merge(base, nil) returns copy of base unchanged.
10. `TestMergeOverlayFields` — Merge with non-nil overlay fields: verify only those fields override.
11. `TestMergeNilFieldsFallThrough` — Merge where overlay has some nil fields, verify base values retained.
12. `TestMergeBothNil` — Merge(nil, nil) returns empty Config with all nil fields.
</action>

<verify>
<automated>go test ./internal/config/... -count=1 -v 2>&1 | tail -30</automated>
</verify>

<acceptance_criteria>
- All 12+ tests pass with zero failures
- TestConfigDirUsesXdgConfigHome: enforces XDG_CONFIG_HOME first
- TestMergeOverlayFields: only non-nil pointer fields override base
- TestLoadFileNotFound: returns config with all nil fields, nil error
- TestWriteSkeleton: file created with all required keys
</acceptance_criteria>
</task>

<task type="auto">
<name>Task 3: Wire config resolution, precedence hierarchy, and CRUNCHYROLL_ETP_RT env var into main.go</name>
<files>main.go, main_test.go</files>

<read_first>
main.go:1-36 (current flag definitions, imports)
main.go:121-167 (current main() function)
internal/drm/drm.go:126-132 (os.LookupEnv pattern for env value reading)
03-RESEARCH.md:291-338 (precedence hierarchy resolution code example)
03-RESEARCH.md:455-461 (flag.Visit vs flag.Lookup — critical pitfall)
03-RESEARCH.md:115-155 (system architecture diagram — shows main.go flow order)
03-CONTEXT.md:23-28 (D-06/D-07 precedence and etp_rt resolution rules)
</read_first>

<action>
Modify main.go to implement config loading and precedence hierarchy. Follow the exact architecture flow from RESEARCH.md "System Architecture Diagram" section.

Changes:
1. Add imports: `internal/config`, `os/exec`, `path/filepath`, `os`.
2. Add `outputDir` and `widevineDev` flag variables following the existing `var (...)` block pattern from lines 17-26. These are defined here but will be fully wired in plans 3.2 and 3.3.
3. After `flag.Parse()` on line 127, add:
   a. `flag.Visit()` to build `explicitFlags map[string]bool` — per RESEARCH.md Pattern 2 and the don't-hand-roll guidance about flag.Visit being the idiomatic Go approach.
   b. Resolve config path via `config.ConfigPath()`. On error, print warning and skip config loading.
   c. Call `config.Load(cfgPath)`. If error (invalid JSON), print "Warning: {error} — using defaults" to stderr and continue (per D-04). If file not found (all-nil Config returned), call `config.WriteSkeleton(cfgPath)` to create default skeleton and print "Created default config at {path}" to stdout.
   d. Define and call `resolveString(flagName string, flagVal string, envName string, configVal *string, defaultVal string) string` helper that checks explicitFlags first, then os.LookupEnv, then configVal, then defaultVal.
   e. Define and call `resolveEtpRt(flagVal string, configVal *string) string`: if `explicitFlags["etp-rt"]` and flagVal != "" use it; else `os.LookupEnv("CRUNCHYROLL_ETP_RT")`; else configVal; else "".
4. Replace `*etpRt` in `api.NewWithContext(ctx, *etpRt)` on line 134 with the result of `resolveEtpRt(*etpRt, cfg.EtpRt)`.
5. Add `resolvedOutputDir` variable from `resolveString("output-dir", *outputDir, "", cfg.OutputDir, "")` — will be wired in Plan 3.2.

For main_test.go, add:
- `TestResolveEtpRtPrefersFlag` — verifies precedence with explicit --etp-rt flag.
- `TestResolveEtpRtFallsBackToEnv` — verifies env var used when flag not set.
- `TestResolveEtpRtPrefersFlagOverEnv` — verifies flag beats env var when both set.
- Verify these compile and pass with the `go test . -run TestResolve -v` command.
</action>

<verify>
<automated>go build . && go test . -run TestResolve -v -count=1 2>&1</automated>
</verify>

<acceptance_criteria>
- `go build .` compiles successfully
- CRUNCHYROLL_ETP_RT env var is read and used when --etp-rt flag is empty (per D-07)
- --etp-rt flag value takes precedence over env var when explicitly provided
- Flag.Visit() correctly detects only explicitly-set flags (not defaults)
- Invalid config file prints warning to stderr and does not block startup
- Missing config file creates skeleton and prints notice
- Config values with nil pointer fields fall through to defaults
</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config file → main.go | Untrusted user-written JSON loaded and parsed by the application |
| env var → main.go | Environment variables read by os.LookupEnv — assumed secure (process-local) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.1-01 | Information Disclosure | Config file (contains etp_rt) | medium | mitigate | Config file created with 0644 permissions; document in README to chmod 600 if secrets stored |
| T-03.1-02 | Tampering | Config file JSON | low | mitigate | Invalid JSON detected at Load time — main.go prints warning to stderr and falls through to defaults per D-04 |
| T-03.1-03 | Tampering | Config file path (XDG) | low | accept | Attacker would need filesystem write access to modify config; user's own config dir is trusted |
| T-03.1-04 | Information Disclosure | CRUNCHYROLL_ETP_RT in process env | medium | accept | Env var is per-process and not logged; flag value visible in `ps` but env var mitigates that (per RESEARCH.md state-of-the-art table) |
</threat_model>

<verification>
<automated>go build . && go test ./internal/config/... -count=1 -v 2>&1 | tail -10</automated>
</verification>

<success_criteria>
- `internal/config/config.go` compiles and all unit tests pass
- `go build .` succeeds with new imports
- `CRUNCHYROLL_ETP_RT` env var resolves correctly through precedence chain
- Config file loading handles missing/invalid/valid states correctly
- All config tests pass: `go test ./internal/config/... -count=1`
- Domain tests pass: `go test . -run TestResolve -count=1`
</success_criteria>

<output>
Create `.planning/phases/03-usability-configuration-validation/03-01-SUMMARY.md` when done
</output>

## Artifacts This Phase Produces

| Symbol | Kind | File | Consumers |
|--------|------|------|-----------|
| `Config` | struct | internal/config/config.go | main.go (precedence resolution) |
| `ConfigDir()` | func | internal/config/config.go | ConfigPath() |
| `ConfigPath()` | func | internal/config/config.go | main.go |
| `Load(path string) (*Config, error)` | func | internal/config/config.go | main.go |
| `WriteSkeleton(path string) error` | func | internal/config/config.go | main.go (auto-gen on missing) |
| `Merge(base, overlay *Config) *Config` | func | internal/config/config.go | main.go (reserved for future layering) |
| `resolveString(...)` | func | main.go (unexported) | main.go precedence resolution |
| `resolveEtpRt(...)` | func | main.go (unexported) | main.go → api.NewWithContext |
| `explicitFlags` | map[string]bool | main.go | main.go all resolve* helpers |
| `CRUNCHYROLL_ETP_RT` | env var | main.go | api.NewWithContext |
| `--output-dir` | CLI flag | main.go | Plan 3.2 (defined but wired later) |
| `--widevine-device` | CLI flag | main.go | Plan 3.3 (defined but wired later) |
</output>
