---
phase: 07-organized-output-folder-metadata
plan: 04
type: execute
wave: 3
depends_on:
  - 07-03
files_modified:
  - internal/api/artwork.go
  - internal/api/artwork_test.go
  - internal/download/season.go
  - internal/download/season_test.go
  - internal/download/episode.go
  - internal/download/episode_test.go
autonomous: true
requirements:
  - META-03
must_haves:
  truths:
    - "When the CMS response (enriched in Plan 03) carries usable poster/backdrop URLs, `poster.jpg` and `backdrop.jpg` are written to the series root `Series Title/` (D-10) — NOT per-season, NOT per-episode"
    - "An HTTP 404 (or any artwork fetch failure) does NOT fail the download: `output.Global.Warn(\"No artwork available for %s: %v\", seriesTitle, err)` AND `diag.APILogger.Warn(\"artwork_fetch_failed\", \"series\", seriesTitle, \"err\", err)` both fire, and the episode/season flow continues (D-12, META-03)"
    - "Artwork URLs come from the same CMS response Plan 03 already fetches (the enriched episode-level response and/or the new series-level GetSeriesInfo response) — no dedicated images endpoint unless neither object response carries usable artwork URLs (D-11)"
    - "Artwork is fetched and written once per series root (os.Stat-guarded like tvshow.nfo), so multi-season invocations do not re-download artwork"
  artifacts:
    - "internal/api/artwork.go with `FetchArtwork(ctx, client, url, destPath) error` (or equivalent method form) returning a sentinel `ErrArtworkNotFound` on HTTP 404"
    - "internal/download/season.go pre-loop artwork fetch+write step (or composed with the tvshow.nfo step from Plan 03) for poster.jpg + backdrop.jpg at series root, non-fatal, os.Stat-guarded"
    - "internal/download/season_test.go::TestRunSeasonWritesArtworkNonFatalOn404 asserting the 404 path warns but does not abort the episode loop"
  key_links:
    - "internal/api/artwork.go (FetchArtwork, GET + io.ReadAll + os.WriteFile) ↔ *api.Client (uses c.newRequest + c.Do for bearer + 401-retry) — method-on-Client form recommended so artwork inherits auto 401 refresh"
    - "internal/download/season.go series-root (same inline 3-line computation as Plan 03) ↔ artwork destination paths `filepath.Join(seriesRoot, \"poster.jpg\")` / `filepath.Join(seriesRoot, \"backdrop.jpg\")`"
    - "internal/api/types.go SeriesInfo (Plan 03) / EpisodeMetadata enrichment → artwork URL source fields (poster_url/backdrop_url OR an images array — whichever the real CMS response carries, confirmed by reading the response Plan 03 already captured)"
---

<objective>
Fetch and write `poster.jpg`/`backdrop.jpg` artwork at the series root, sourced from the CMS response Plan 03 already fetches, with 404 (and any fetch failure) treated as non-fatal so a missing poster never fails a successful download (D-10, D-11, D-12, META-03).

Purpose: Jellyfin/Kodi display a poster + backdrop without the user scraping online databases. A 404 must not abort a 30-minute download — artwork is best-effort; the MKV + NFO are the deliverable.

Output: New `internal/api/artwork.go` helper (+ `ErrArtworkNotFound` sentinel + 404 test); pre-loop artwork fetch+write step in `internal/download/season.go` (composed with the Plan 03 tvshow.nfo step), non-fatal + os.Stat-guarded; a single-episode-flow mirror in `internal/download/episode.go` so the single-episode path also gets series-root artwork (D-03 uniformity). Depends on Plan 03 (the enriched CMS response that carries artwork URLs + the GetSeriesInfo call + the season.go pre-loop series-root/metadata seam established there).
</objective>

<execution_context>
@/home/fmoura/Documents/Applications/crunchyroll-downloader/.opencode/gsd-core/workflows/execute-plan.md
@/home/fmoura/Documents/Applications/crunchyroll-downloader/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-organized-output-folder-metadata/07-CONTEXT.md
@.planning/phases/07-organized-output-folder-metadata/07-PATTERNS.md
@.planning/phases/07-organized-output-folder-metadata/07-03-CONTEXT.md
@.planning/research/PITFALLS.md
@internal/api/types.go
@internal/api/season.go
@internal/api/client.go
@internal/api/episode.go
@internal/media/segment.go
@internal/download/season.go
@internal/download/season_test.go
@internal/download/episode.go
@internal/download/episode_test.go
@internal/diag/diag.go
@internal/output/output.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build FetchArtwork helper with 404 sentinel + non-fatal status handling (D-12)</name>
  <files>internal/api/artwork.go, internal/api/artwork_test.go</files>
  <read_first>
    - .planning/phases/07-organized-output-folder-metadata/07-PATTERNS.md §`internal/api/artwork.go` (the BEST analog `media.DownloadSubs` at internal/media/segment.go:303-332 — authenticated GET → io.ReadAll → os.WriteFile; the D-12 404-non-fatal adaptation with status-check before draining body; the `httpDoer` interface at segment.go:27-29 — `*api.Client` satisfies it; the method-on-Client recommendation to inherit c.Do's 401-refresh retry)
    - .planning/research/PITFALLS.md Pitfall 6 (the row: "Fetching poster URL but never downloading it" + "A missing poster must not fail a successful 30-minute download" — artwork is best-effort, non-fatal)
    - .planning/phases/07-organized-output-folder-metadata/07-CONTEXT.md D-11 (artwork URLs from the existing CMS endpoint response — prefer the same object Plan 03 already fetches; D-07 series-level call is the fallback if episode-level has no artwork URLs), D-12 (404 + any failure non-fatal — `output.Global.Warn` + `diag.APILogger.Warn("artwork_fetch_failed", "series", seriesTitle, "err", err)`)
    - internal/media/segment.go (DownloadSubs lines 303-332 + the httpDoer interface lines 27-29; the Origin/Referer/User-Agent headers — NOTE: artwork served from the Crunchyroll CDN may need the same headers OR the bearer token; the helper must use the bearer-token path via c.newRequest + c.Do to inherit 401 retry per PATTERNS §Bearer-Token Authenticated GET)
    - internal/api/client.go (`newRequest` at lines 84-95 setting Authorization: Bearer + User-Agent; `Do` at lines 97-138 with the 401-refresh-and-retry loop — the artwork helper should use these so a stale token mid-artwork-fetch is transparently refreshed)
    - internal/api/types.go (the enriched EpisodeMetadata + the new SeriesInfo from Plan 03 — to identify which field carries the artwork URL; if discovery did NOT lock a field name, this task does a small follow-up: confirm from the Plan 03 captured response whether the artwork URL is a top-level string field on SeriesInfo (e.g. `images`/`poster_url`/`backdrop_url`) or a nested array, and use the available field)
  </read_first>
  <action>
Create internal/api/artwork.go with:

    1. A package-level sentinel `var ErrArtworkNotFound = errors.New("artwork not found (404)")` (add `"errors"` import).
    2. An exported helper, method form on `*Client` recommended: `func (c *Client) FetchArtwork(ctx context.Context, url, destPath string) error`:
       - URL-VALIDATION GATE (SSRF mitigation, T-07-08-T — REQUIRED, not optional): BEFORE building the request, parse `url` via `net/url`; require scheme `https` AND host matching one of the allowed Crunchyroll CDN families (`*.crunchyroll.com`, `*.vmdcdn.com`, `*.akamaized.net` — suffix-match on the host, exact subdomain allowed). If the scheme is not https OR the host is not an allowed-family suffix, return `ErrArtworkNotFound` WITHOUT making any HTTP call (treating an unknown host as a non-fatal "artwork not found" skip is consistent with D-12 — the caller warns and proceeds). Record the allowed-host list as a comment atop the helper so a future CDN host can be added by appending to the list.
       - build the request via `c.newRequest(ctx, http.MethodGet, url, nil)` — NOTE: do NOT run the URL through `c.url(...)`; artwork URLs from the CMS response are already absolute (the CDN hosts them). Pass the absolute URL straight to `newRequest`.
       - `resp, err := c.Do(req)` — inherits the 401-refresh retry transparently.
       - `defer resp.Body.Close()`.
       - STATUS-CHECK before draining (D-12): `if resp.StatusCode == http.StatusNotFound { return ErrArtworkNotFound }`; `if resp.StatusCode != http.StatusOK { return fmt.Errorf("artwork fetch: status %d", resp.StatusCode) }`.
       - `body, err := io.ReadAll(resp.Body)` then `if err != nil { return fmt.Errorf("artwork read: %w", err) }`.
       - `if err := os.WriteFile(destPath, body, 0644); err != nil { return fmt.Errorf("artwork write %s: %w", destPath, err) }`.
       - return nil on success.
     The helper does NOT call `output.Global` or `diag.APILogger` — it is a pure API-layer function that returns errors (incl. the sentinel). The CALLER (download/season.go) converts errors to non-fatal warns per D-12. This keeps the api package free of output/diag coupling (consistent with the existing api package — `GetEpisodeInfo` etc. return errors, do not log to output).
     Use the `c.Do` + `c.newRequest` form so 401 refresh is inherited; do NOT use a bare `http.Client{}` or the static-CDN-header form from `media.DownloadSubs` (that bypasses bearer auth, and artwork URLs may gate on the same auth as the CMS).
     If the URL-validation gate rejects a host observed in a real run (a new Crunchyroll CDN host not yet in the allowlist), expand the allowlist by appending the suffix in artwork.go and note it in the SUMMARY — do NOT relax the gate to a bare Warn (an un-warned SSRF surface is worse than a missing poster).

    TESTS (internal/api/artwork_test.go) — stdlib only (v1.0 D-03, no testify):
    - `TestFetchArtworkWritesFileOn200`: `httptest.NewServer` returning 200 + a few bytes of fake JPEG body — but note the gate requires https + a known host, and `httptest.NewServer` returns an `http://127.0.0.1:port` URL which FAILS the gate. To test the happy path THROUGH the gate, either (a) make the allowed-host list include `127.0.0.1`/`localhost` when a test-build flag is set (injectable allowlist — preferred: add an unexported `var allowedArtworkHostSuffixes = []string{".crunchyroll.com", ".vmdcdn.com", ".akamaized.net"}` so tests can append `127.0.0.1` then restore via `t.Cleanup`), OR (b) test FetchArtwork's post-gate logic by calling the gate separately. Pick (a): the injectable allowlist seam keeps the gate provable AND the 200/404/500 tests exercisable through a real httptest server on localhost. The test appends `127.0.0.1` to the allowlist slice, restores it in `t.Cleanup`, then calls `client.FetchArtwork(ctx, server.URL, destPath)` with `destPath` under `t.TempDir()`; assert no error and `os.ReadFile(destPath)` equals the server body. Assert the request carried `Authorization: Bearer initial-token` (mirrors season_test.go / episode_test.go Bearer assertion).
    - `TestFetchArtworkReturnsSentinelOn404`: server returns 404 with empty body (allowlist patched to include the host as above); assert `errors.Is(err, ErrArtworkNotFound)` is true and `destPath` does NOT exist (the helper must NOT create a partial file on 404 — the status-check precedes `os.WriteFile`).
    - `TestFetchArtworkReturnsErrorOn500`: server returns 500; assert the returned error is NOT `ErrArtworkNotFound` (it is the `artwork fetch: status 500` wrap) and that the caller can distinguish (the caller treats ANY non-sentinel error the same way for non-fatal warns, but the test asserts the sentinel is 404-specific).
    - `TestFetchArtworkRejectsUnknownHost` (SSRF negative test, T-07-08-T — REQUIRED for the disposition=mitigate to be provable): spin an `httptest.NewServer` whose `HandlerFunc` increments a request counter AND writes 200 (so the test can detect whether the HTTP call happened); call `client.FetchArtwork(ctx, server.URL, destPath)` WITHOUT patching the allowlist (so `127.0.0.1` is NOT allowed); assert (a) the returned error satisfies `errors.Is(err, ErrArtworkNotFound)` (unknown-host is treated as a non-fatal skip), (b) the request counter is ZERO (the helper returned from the gate WITHOUT making the HTTP call — the SSRF mitigation short-circuits), and (c) `destPath` does NOT exist. ALSO assert a non-http(s) scheme (e.g. `file:///etc/passwd`) returns `ErrArtworkNotFound` without any request. This proves the URL gate is the first-line mitigation (the SSRF defense that fires before any outbound HTTP) and that an attacker-controlled URL never reaches the network.
    Reuse the `newTestClient`/`api.NewTestClient` helper from internal/api/client_helper.go (or wherever the existing api tests resolve a test client with a bearer token), exactly as season_test.go does.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && go build ./internal/api/... && go vet ./internal/api/... && go test ./internal/api/... -count=1 -v -run 'TestFetchArtwork'</automated>
  </verify>
  <acceptance_criteria>
    - internal/api/artwork.go exists with: `grep -n 'func (c \*Client) FetchArtwork\|ErrArtworkNotFound' internal/api/artwork.go` matching both.
    - internal/api/artwork.go: URL-validation gate exists BEFORE the request build — `grep -n 'url.Parse\|allowedArtworkHostSuffixes\|https' internal/api/artwork.go` matches; an unexported `var allowedArtworkHostSuffixes = []string{...}` seam (the allowlist) is present so tests can inject `127.0.0.1`; a comment atop the helper documents the allowed-host list (T-07-08-T).
    - internal/api/artwork.go: status-check before draining — `grep -n 'StatusNotFound\|StatusOK' internal/api/artwork.go` matches; `grep -n 'os.WriteFile' internal/api/artwork.go` appears AFTER the status checks (the writes are gated on 200).
    - internal/api/artwork.go: uses `c.newRequest` + `c.Do` (bearer path, 401 retry inherited) — `grep -n 'c.newRequest\|c.Do' internal/api/artwork.go` matches; does NOT use a bare `http.Client{}`.
    - `go test ./internal/api/... -run 'TestFetchArtwork' -count=1 -v` prints `--- PASS` for all FOUR sub-tests (200, 404, 500, RejectsUnknownHost).
    - The SSRF negative test `TestFetchArtworkRejectsUnknownHost` exists and asserts BOTH an unknown host AND a non-https scheme return `ErrArtworkNotFound` WITHOUT making the HTTP call (request counter == 0): `grep -n 'TestFetchArtworkRejectsUnknownHost' internal/api/artwork_test.go` matches; the test references an allowed-suffix-injection (e.g. `allowedArtworkHostSuffixes` append + `t.Cleanup`) OR an equivalent gate-only stub proving the short-circuit.
    - The 404 test asserts `errors.Is(err, ErrArtworkNotFound)` and that `destPath` does not exist (no partial file).
    - internal/api/artwork.go does NOT import `internal/output` or `internal/diag` (pure API layer — `grep -n 'crunchyroll-downloader/internal/output\|crunchyroll-downloader/internal/diag' internal/api/artwork.go` returns nothing).
    - No testify: `grep -ni 'testify' internal/api/artwork_test.go` returns nothing.
    - `go test ./internal/api/... -count=1` exits 0 (no regression to GetEpisodeInfo/GetSeasons/GetSeriesInfo).
  </acceptance_criteria>
  <done>A bearer-authenticated artwork helper writes JPG bytes to a caller-named path on 200 (only after the URL scheme+host passes the SSRF allowlist gate), returns `ErrArtworkNotFound` on 404 before touching disk, returns a distinct error on other non-200 codes, rejects unknown hosts / non-https schemes BEFORE any network call (provable via TestFetchArtworkRejectsUnknownHost), and never logs from inside the api package.</done>
</task>

<task type="auto">
  <name>Task 2: Wire series-root poster.jpg + backdrop.jpg (season.go pre-loop, single-episode mirror, non-fatal, os.Stat-guarded) (D-10, D-11, D-12)</name>
  <files>internal/download/season.go, internal/download/season_test.go, internal/download/episode.go, internal/download/episode_test.go</files>
  <read_first>
    - .planning/phases/07-organized-output-folder-metadata/07-PATTERNS.md §`internal/download/season.go` (the pre-loop insertion at lines 47-52 alongside the tvshow.nfo write Plan 03 added in the SAME pre-loop block), Shared Patterns §Non-fatal Warn (dual-channel `output.Global.Warn` + nil-guarded `diag.APILogger.Warn`), §Resumability Skip (os.Stat guard like Plan 03's tvshow.nfo guard)
    - .planning/phases/07-organized-output-folder-metadata/07-CONTEXT.md D-10 (artwork at series root ONLY), D-11 (URLs from the Plan 03 CMS response — episode-level enrichment OR the GetSeriesInfo response; pick whichever carries usable poster/backdrop URLs; AVOID a dedicated images endpoint unless neither object response carries artwork), D-12 (404/non-fatal dual-channel warn), and the single-episode D-03 uniformity note (single-episode flow mirrors season — so episode.go must also write series-root artwork, OR delegate to the same helper)
    - internal/download/season.go (the Plan 03 pre-loop block where `seriesRoot` is computed and `tvshow.nfo` is written — EXTEND that same block to also fetch+write `poster.jpg` and `backdrop.jpg`; reuse the existing `seriesRoot` local, the existing series-metadata-writer seam var, and the existing os.Stat-guard pattern — do NOT recompute the series root)
    - internal/download/episode.go (the single-episode `tvshow.nfo` write block Plan 03 added inside `Episode` — EXTEND that same guarded block to also fetch+write `poster.jpg`/`backdrop.jpg` at `outputBase`; reuse the existing `outputBase`, the existing os.Stat guard pattern, and an `episodeFetchArtwork = api.FetchArtwork` seam mirroring `episodeMerge`/`episodeWriteNfo`)
    - internal/download/season_test.go (the Plan 03 season tests `TestRunSeasonWritesTvshowNfoOnceBeforeEpisodeLoop` / `TestRunSeasonTvshowNfoNonFatal` — extend the suite with artwork analogs asserting the 404 path warns but does not abort the episode loop)
    - internal/download/episode_test.go (`restoreEpisodeTestSeams` at lines ~366-422 — extend for an `episodeFetchArtwork` seam; the Plan 03 `TestEpisodeWritesTvshowNfoOnSingleEpisodeFlow` analog — extend to assert artwork also writes on single-episode)
    - internal/api/types.go (the enriched SeriesInfo / EpisodeMetadata from Plan 03 — confirm which field carries the poster/backdrop URL; if the field is an array of image objects, pick the URL whose type hint or dimensions match a poster aspect ratio, else pick the first available — DOCUMENT the chosen URL-extraction rule in the SUMMARY)
    - internal/api/artwork.go (the FetchArtwork helper + ErrArtworkNotFound from Task 1)
    - internal/diag/diag.go (APILogger — nil-guard pattern `if diag.APILogger != nil`)
  </read_first>
  <action>
    WIRE ARTWORK INTO SEASON PRE-LOOP (internal/download/season.go):
    Extend the Plan 03 pre-loop block (where `seriesRoot` and `tvshow.nfo` already live) to ALSO handle artwork. Reuse the existing `seriesRoot` local and the existing series-metadata-writer seam. Add, INSIDE the same `os.Stat(tvshow.nfo)` guard (or a sibling guard per-artwork so a prior partial run that wrote `poster.jpg` but not `backdrop.jpg` still completes the missing one):
    - For each of `poster.jpg`, `backdrop.jpg`:
      - `destPath := filepath.Join(seriesRoot, "poster.jpg")` / `"backdrop.jpg"`.
      - `if _, err := os.Stat(destPath); err == nil { continue }` (os.Stat resumability — skip if present).
      - Resolve the artwork URL from the CMS response Plan 03 already fetched (the enriched `SeriesInfo` if GetSeriesInfo was used, OR the enriched `EpisodeMetadata`/`SeasonEpisode` fields if the fallback was used). Apply the URL-extraction rule chosen in read_first (poster aspect vs first-available). If NO usable URL is present on the response, SKIP this artwork silently (no fetch attempt, no warn — there is simply nothing to fetch — but emit a one-time `diag.APILogger.Info("artwork_no_url", "series", seriesTitle)` for diagnostics).
      - `if err := fetchArtwork(ctx, client, url, destPath); err != nil {` then NON-FATAL per D-12: `output.Global.Warn("No artwork available for %s: %v", episodes[0].SeriesTitle, err)` AND `if diag.APILogger != nil { diag.APILogger.Warn("artwork_fetch_failed", "series", episodes[0].SeriesTitle, "file", baseName, "err", err) }`. Do NOT return the error; do NOT mark the season failed; proceed to the next artwork (or to the episode loop). The `ErrArtworkNotFound` sentinel is treated identically to any other fetch error for the warn purpose (404 is the expected non-fatal case).
    Add an `episodeFetchArtwork` / `seriesFetchArtwork` seam var (mirror Plan 03's seam form) so tests override the fetch without a real network. Default it to `api.FetchArtwork` (or `(*api.Client).FetchArtwork`).

    WIRE ARTWORK INTO SINGLE-EPISODE FLOW (internal/download/episode.go):
    Extend the Plan 03 single-episode `tvshow.nfo` block (guarded by `os.Stat(filepath.Join(outputBase, "tvshow.nfo"))`) inside `download.Episode` to ALSO fetch+write `poster.jpg`/`backdrop.jpg` at `outputBase` using the SAME non-fatal + os.Stat-guarded pattern, sourcing the URL from the enriched `info.EpisodeMetadata` (Plan 03 enrichment). Add the `episodeFetchArtwork = api.FetchArtwork` seam in the lines 27-47 seam block (sibling to `episodeWriteNfo`). This satisfies D-03 (single-episode mirrors season layout including artwork).

    URL EXTRACTION (D-11): determine the artwork URL field(s) from the Plan 03 captured CMS response. If the enriched `SeriesInfo`/`EpisodeMetadata` exposes a single poster URL string and a single backdrop URL string, use them directly. If it exposes an `images`/`episode_images` array, pick the entry whose dimensions/type match a poster aspect ratio (e.g. 2:3 vertical for poster, 16:9 for backdrop); if a type/role hint exists in the array entries, prefer it. Document the chosen extraction rule in the SUMMARY so a future change to the CMS shape is traceable. AVOID a dedicated images endpoint unless the object response carries NO usable artwork URLs — and if that fallback is needed, add it as a sibling method in internal/api/artwork.go and note in the SUMMARY.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && go build ./... && go vet ./... && go test ./internal/download/... ./internal/api/... -count=1 -v -run 'TestFetchArtwork|TestRunSeasonWritesArtwork|TestRunSeasonArtwork404|TestEpisodeWritesArtwork'</automated>
  </verify>
  <acceptance_criteria>
    - internal/download/season.go: `grep -n 'poster.jpg\|backdrop.jpg' internal/download/season.go` matches both; the paths are under `seriesRoot` (the SAME root Plan 03 computes — not recomputed): `grep -n 'filepath.Join(seriesRoot' internal/download/season.go` shows poster/backdrop joins.
    - internal/download/season.go: artwork fetch is os.Stat-guarded: `grep -B1 -A3 'os.Stat.*poster.jpg\|os.Stat.*backdrop.jpg' internal/download/season.go` matches.
    - internal/download/season.go: D-12 dual-channel warn on fetch failure: `grep -n 'No artwork available for\|artwork_fetch_failed' internal/download/season.go` matches BOTH strings.
    - internal/download/season.go: artwork error is NON-FATAL — `grep -A5 'fetchArtwork(ctx' internal/download/season.go` shows the warn block, no `return err`.
    - internal/download/season.go: artwork URL resolved from the enriched CMS response, NOT a new dedicated endpoint (unless the documented fallback): confirmed by SUMMARY + the URL-extraction-code reads a field off `SeriesInfo`/`EpisodeMetadata`.
    - internal/download/episode.go: single-episode artwork write present, os.Stat-guarded, non-fatal, at `outputBase`: `grep -n 'poster.jpg\|backdrop.jpg\|episodeFetchArtwork' internal/download/episode.go` matches; `episodeFetchArtwork` seam declared in the lines 27-47 block.
    - internal/download/season_test.go: `TestRunSeasonWritesArtworkNonFatalOn404` exists — `grep -n 'TestRunSeasonWritesArtworkNonFatalOn404' internal/download/season_test.go` matches; the test asserts (a) the fetchArtwork seam is invoked, (b) when it returns `api.ErrArtworkNotFound` the runSeason loop STILL completes ALL episodes (no abort), (c) `output.Global.Warn` received the "No artwork available" line.
    - internal/download/episode_test.go: `TestEpisodeWritesArtworkOnSingleEpisodeFlow` exists (or the Plan 03 single-episode test extended), asserting `download.Episode` invokes `episodeFetchArtwork` for both poster and backdrop at `outputBase` on the single-episode path; a 404 (seam returns `api.ErrArtworkNotFound`) does NOT fail the episode (`Episode` returns nil).
    - `go build ./...` and `go vet ./...` exit 0.
    - `go test ./internal/download/... ./internal/api/... -count=1` exits 0 (no regression to Plan 03 NFO tests or Plan 01/02 mux/layout tests).
    - No new testify import: `grep -ni 'testify' internal/download/season_test.go internal/download/episode_test.go internal/api/artwork_test.go` returns nothing.
    - No main.go modification: `git diff --name-only HEAD` shows main.go NOT in this plan's changed set.
  </acceptance_criteria>
  <done>Season pre-loop writes `poster.jpg`+`backdrop.jpg` at the series root from the CMS response; a 404 (or any fetch error) warns via both `output.Global.Warn` and `diag.APILogger.Warn` and the season's episode loop completes; the single-episode flow mirrors the same artwork write; artwork URLs come from the Plan 03 CMS response (no dedicated images endpoint unless documented fallback); the artefacts download path (MKV) is never blocked by a missing poster.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Crunchyroll CMS → poster/backdrop URL → HTTP GET (CDN) → bytes → `os.WriteFile` at series root | The artwork URL (string from authenticated CMS response) is fetched and its response bytes written to `poster.jpg`/`backdrop.jpg` under the sanitized series root. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-08-T | Tampering (SSRF / arbitrary URL fetch) | internal/api/artwork.go FetchArtwork URL | medium | mitigate | The artwork URL comes from the authenticated Crunchyroll CMS response (not user-supplied), reducing SSRF surface — but a compromised/malicious CMS-obtained string must not yield arbitrary outbound GETs. The helper follows redirects only via `c.Do` (Phase 6 hardened client); do NOT add a separate `http.Client{}` that bypasses the hardened transport. REQUIRED gate (not optional): before building the request, parse `url` via `net/url`; require scheme `https` AND host suffix-matching one of `*.crunchyroll.com`, `*.vmdcdn.com`, `*.akamaized.net` (the known Crunchyroll CDN families); reject others with `ErrArtworkNotFound` (treated as a non-fatal skip by the caller, consistent with D-12) WITHOUT making any HTTP call. Allowlist is an unexported `var allowedArtworkHostSuffixes` so tests inject `127.0.0.1`. Document the allowed-host list atop FetchArtwork in artwork.go; if a real run surfaces a new CDN host, append the suffix and note it in the SUMMARY. PROVABLE via `TestFetchArtworkRejectsUnknownHost` (asserts unknown host AND non-https scheme return `ErrArtworkNotFound` with request counter 0). |
| T-07-09-T | Tampering (path traversal via destPath) | internal/download/season.go + episode.go dest paths | low | mitigate | `poster.jpg`/`backdrop.jpg` are static filenames joined to `seriesRoot` (already sanitized via `cleanSeriesTitle` — see T-07-04-T in Plan 03). The artwork helper receives an absolute destPath; it uses `os.WriteFile` (exact path), no glob, no cleanup loop that could remove the written JPG. (Pitfalls Pitfall 6 cleanup-glob warning respected.) |
| T-07-10-D | Denial of Service (artwork failure blocks download) | download/season.go + download/episode.go non-fatal handling | high | mitigate | All artwork fetch failures (404 sentinel, 5xx, transport error, url-absent, unknown-host-reject) are NON-FATAL (D-12): caller emits `output.Global.Warn` + nil-guarded `diag.APILogger.Warn("artwork_fetch_failed", ...)` and proceeds. The `os.Stat` guard prevents re-fetching per season/episode in a multi-season run. A 30-minute download is never aborted by a missing poster (Pitfalls Pitfall 6). |
| T-07-11-I | Integrity (incorrect artwork bytes — fake/corrupt image) | FetchArtwork response body | low | accept | The helper writes whatever bytes the CDN returns; Jellyfin/Kodi tolerate a corrupt JPG by ignoring it. No decode/validation of image bytes is warranted (would add a dependency and a failure mode). The `Content-Type` is not trusted (servers mis-set it); the file extension `.jpg` is the only commitment. |

## Security Note — allowed-host validation (T-07-08-T)
The required host allowlist (`*.crunchyroll.com`, `*.vmdcdn.com`, `*.akamaized.net`) must be confirmed against the actual hosts the enriched CMS response returns for artwork URLs (read_first identifies these from the Plan 03 captured response). If the artwork URLs use unexpected hosts, expand the allowlist by appending the suffix in artwork.go and document in the SUMMARY. Because an unknown host returns `ErrArtworkNotFound` (a non-fatal skip), expanding the allowlist is the documented remedy when a real run surfaces a legitimate Crunchyroll CDN host not yet listed — NEVER relax the gate to a mere Warn (an un-warned SSRF surface is worse than a missing poster). Record the final allowlist in the SUMMARY.
</threat_model>

<verification>
- `go build ./...` and `go vet ./...` exit 0.
- `go test ./internal/api/... ./internal/download/... -count=1` exits 0 (the new artwork tests pass; Plan 01/02/03 tests remain green).
- 404 path provable in CI: `TestFetchArtworkReturnsSentinelOn404` + `TestRunSeasonWritesArtworkNonFatalOn404` + `TestEpisodeWritesArtwork...404` (single-episode) all pass.
- Series-root placement provable: the season_test + episode_test assert the dest paths are `filepath.Join(seriesRoot/outputBase, "poster.jpg")` and `"backdrop.jpg"` — NOT under `Season NN/`, NOT per-episode.
- No dedicated images endpoint added unless the documented fallback (verify via SUMMARY + the absence of a new `GetImages` method in `internal/api/artwork.go` unless fallback was chosen).
</verification>

<success_criteria>
- META-03 delivered: `poster.jpg`/`backdrop.jpg` written at the series root when the CMS response carries usable artwork URLs; a 404 (or any fetch failure) does NOT fail the download (dual-channel non-fatal warn per D-12).
- D-10 delivered: artwork lives at `Series Title/` root only — never per-season, never per-episode (provable by the dest-path assertions).
- D-11 delivered: artwork URLs sourced from the Plan 03 CMS response (enriched EpisodeMetadata or GetSeriesInfo); no dedicated images endpoint unless the documented fallback.
- D-12 delivered: every artwork error path emits `output.Global.Warn("No artwork available for %s: %v", seriesTitle, err)` AND `diag.APILogger.Warn("artwork_fetch_failed", "series", seriesTitle, "err", err)`; the `.mkv` + `.nfo` (from Plan 03) are the deliverable and are never blocked by artwork.
- D-03 uniformity preserved: single-episode flow writes the same series-root artwork as the season flow (mirror via `episodeFetchArtwork` seam).
</success_criteria>

<output>
Create `.planning/phases/07-organized-output-folder-metadata/07-04-SUMMARY.md` when done. SUMMARY MUST record:
- The artwork URL field/extraction rule chosen from the CMS response (D-11), and which object (`SeriesInfo` vs enriched `EpisodeMetadata`) carries it.
- The final FetchArtwork host allowlist (T-07-08-T) — the confirmed Crunchyroll CDN hosts the helper accepts.
- Whether a dedicated images endpoint was added (fallback) or NOT, with the reason.
</output>

## Artifacts this phase produces (Plan 04)

### New files
- `internal/api/artwork.go` — `FetchArtwork(ctx, url, destPath) error` method on `*api.Client`; `ErrArtworkNotFound` sentinel; (optional `GetImages` fallback method if D-11 fallback).
- `internal/api/artwork_test.go` — `TestFetchArtworkWritesFileOn200`, `TestFetchArtworkReturnsSentinelOn404`, `TestFetchArtworkReturnsErrorOn500`, `TestFetchArtworkRejectsUnknownHost` (the SSRF-negative test proving the gate short-circuits before any HTTP call).

### New / modified types
- None expected (artwork URL fields come from the Plan 03 enrichment). If discovery finds the artwork URL is a NEW field not captured in Plan 03, add it to `SeriesInfo` (internal/api/types.go) as part of this plan — document in SUMMARY.

### New seam vars
- `internal/download/season.go`: `seriesFetchArtwork = api.FetchArtwork` (or equivalent seam form mirroring the Plan 03 series-metadata-writer seam).
- `internal/download/episode.go`: `episodeFetchArtwork = api.FetchArtwork` (added to the lines 27-47 seam block, sibling to `episodeWriteNfo`).

### New tests
- `internal/download/season_test.go::TestRunSeasonWritesArtworkNonFatalOn404`.
- `internal/download/season_test.go::TestRunSeasonWritesArtworkAtSeriesRoot` (dest-path assertion).
- `internal/download/episode_test.go::TestEpisodeWritesArtworkOnSingleEpisodeFlow` (incl. 404-non-fatal case).

### Modified behavior (no new exported symbols beyond FetchArtwork/ErrArtworkNotFound)
- `download.runSeason` — pre-loop block (extend the Plan 03 tvshow.nfo block) now also fetches+writes `poster.jpg`/`backdrop.jpg` at series root, non-fatal, os.Stat-guarded.
- `download.Episode` — single-episode block (extend the Plan 03 tvshow.nfo block) now also fetches+writes `poster.jpg`/`backdrop.jpg` at `outputBase`, non-fatal, os.Stat-guarded.

### Files explicitly NOT modified by this plan
- `main.go` (no new flag, no dispatch change).
- `internal/nfo/` (Plan 03 owns; this plan composes with the pre-loop block Plan 03 added, not the nfo package).
- `internal/mux/mux.go` (Plan 01).
- `internal/api/season.go` (Plan 03 owns GetSeriesInfo; this plan reuses the response).
- `internal/config/config.go` (no `--no-artwork` flag — deferred idea, NOT requested).
</content>