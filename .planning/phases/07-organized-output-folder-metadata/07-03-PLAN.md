---
phase: 07-organized-output-folder-metadata
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
  - 07-02
files_modified:
  - internal/api/types.go
  - internal/api/season.go
  - internal/api/season_test.go
  - internal/api/testdata/api/episode-info-response.json
  - internal/nfo/nfo.go
  - internal/nfo/types.go
  - internal/nfo/nfo_test.go
  - internal/download/episode.go
  - internal/download/episode_test.go
  - internal/download/season.go
  - internal/download/season_test.go
autonomous: true
requirements:
  - META-01
  - META-02
must_haves:
  truths:
    - "A per-episode `.nfo` is written beside each `.mkv` at `Series Title/Season NN/Series Title S01E01 - Title.nfo` with `<uniqueid type=\"crunchyroll\">` whose chardata is the Crunchyroll content id (D-06, D-08, META-02)"
    - "On the DEFAULT production path (both single-episode and season flows) `client.GetSeriesInfo(ctx, seriesID, ...)` IS invoked — using the series-id decoded from the existing `EpisodeMetadata` (single-episode) / `SeasonEpisode` (season) responses — and the resulting `tvshow.nfo` at `Series Title/tvshow.nfo` carries `<tvshow>` root with `<title>`, `<plot>`, `<genre>`/`<studio>` (D-07 LOCKED: NEW series-level CMS call fires by default). A title-only `tvshow.nfo` is written ONLY as a true error-fallback when `GetSeriesInfo` fails (D-09 non-fatal warn). (D-07, D-08, META-01)"
    - "NFO XML is emitted via stdlib `encoding/xml` (`xml.MarshalIndent` + `xml.Header`), so `&`, `<`, `>`, `\"`, `'`, CJK, and emoji in titles/plots are entity-escaped automatically (Pitfalls Pitfall 6 — NEVER `fmt.Sprintf` for NFO body)"
    - "NFO write errors (and series-info fetch errors) are non-fatal: emit `output.Global.Warn` + `diag.MuxLogger.Warn` (or `diag.APILogger.Warn` for the series API call) and the `.mkv` download still succeeds (D-09)"
    - "A `tvshow.nfo` is written once per series root, guarded by an `os.Stat` existence check, so multi-season invocations (main.go season loop) do NOT re-fetch series info or re-write it (mirrors D-02 resumability)"
    - "The season number written into NFO (episode `<season>` / `tvshow` derived fields) is the corrected SeasonNumber (Plan 01 fix is in place), NOT EpisodeNumber"
  artifacts:
    - "internal/nfo/ package with `WriteEpisode(ctx, path, *api.EpisodeInfo) error` and `WriteTVShow(ctx, path, *api.SeriesInfo) error`"
    - "`SeriesInfo` and `SeriesInfoResponse` types in internal/api/types.go; enriched `EpisodeMetadata` fields (incl. a series-id field) in internal/api/types.go; NEW series-id field on `SeasonEpisode` in internal/api/types.go (decoded from the existing GetSeasonEpisodes response — Option A, no new network call for the id itself)"
    - "`GetSeriesInfo(ctx, seriesId, audioLocale, subLocale)` method on *api.Client in internal/api/season.go"
    - "internal/download/episode.go post-merge `episodeWriteNfo` seam var + call (non-fatal) + single-episode `client.GetSeriesInfo(info.EpisodeMetadata.SeriesID)` → `WriteTVShow` wiring (default path; title-only on GetSeriesInfo failure)"
    - "internal/download/season.go pre-loop series-metadata+tvshow.nfo write step: `client.GetSeriesInfo(episodes[0].SeriesID)` → `WriteTVShow` on the DEFAULT path (non-fatal, os.Stat-guarded; title-only error-fallback on GetSeriesInfo failure)"
  key_links:
    - "internal/nfo/ (emitter) ↔ internal/api/types.go (EpisodeMetadata + new SeriesInfo — the decoded CMS payloads that feed the NFO)"
    - "internal/download/episode.go (path to the deep .mkv) ↔ internal/nfo.WriteEpisode (writes .nfo beside it, path = strings.TrimSuffix(outputFile, \".mkv\") + \".nfo\")"
    - "internal/download/season.go (series root, computed as outputDir/cleanSeriesTitle parallel to episode.go) ↔ internal/nfo.WriteTVShow (writes tvshow.nfo there) and `api.GetSeriesInfo`"
    - "internal/download/season.go series-root computation ↔ internal/download/episode.go outputBase (SAME inline 3-line pattern: outputBase := cleanSeriesTitle; if outputDir != \"\" { outputBase = filepath.Join(outputDir, cleanSeriesTitle) } — must produce identical series roots so tvshow.nfo and the Season NN folder are siblings)"
---

<objective>
Emit Jellyfin/Kodi NFO metadata: a per-episode `.nfo` with `<uniqueid type="crunchyroll">` and a series-root `tvshow.nfo`, fed by an enriched existing episode CMS decode (D-06) and a NEW series-level CMS call (D-07) that fires on the DEFAULT production path. Per D-08/D-09, NFO writes are non-fatal and use the Crunchyroll content id as the stable uniqueid.

Purpose: Jellyfin/Kodi display correct title/plot without relying on online scraper matching (META-01), and stable re-scrapes survive title drift via `<uniqueid type="crunchyroll">` (META-02). Both must scan cleanly on a real Jellyfin AND a real Kodi (ROADMAP success criterion 4), which requires `encoding/xml` entity escaping (Pitfalls Pitfall 6 — never `fmt.Sprintf`) and the corrected `season_number` from Plan 01.

Wiring design (Option A — main.go stays UNMODIFIED): the series-id needed to call `GetSeriesInfo` is decoded from the ALREADY-FETCHED responses — `EpisodeMetadata.SeriesID` (single-episode path, from the existing `/content/v2/cms/objects/{id}` decode) and `SeasonEpisode.SeriesID` (season path, from the existing `GetSeasonEpisodes` decode). Both fields are added as struct-field enrichments; NO new network call is required just to obtain the series-id. `client.GetSeriesInfo(ctx, seriesID, ...)` then fires on the default path, producing a rich `tvshow.nfo` with `<plot>/<genre>/<studio>`. A title-only `tvshow.nfo` is written ONLY when `GetSeriesInfo` itself fails (D-09 non-fatal warn) — it is NOT the default.

Output: New `internal/nfo/` package (+ tests); enriched `EpisodeMetadata` (incl. `SeriesID`) + NEW series-id field on `SeasonEpisode` + new `SeriesInfo`/`SeriesInfoResponse` in api types; `GetSeriesInfo` method on `*api.Client` (+ test); post-merge per-episode NFO wiring in download/episode.go (+ single-episode tvshow.nfo via GetSeriesInfo, + seam + test); pre-loop series-metadata+tvshow.nfo write in download/season.go (GetSeriesInfo on default path, + seam + test). Depends on Plan 01 (correct season_number) and Plan 02 (nested `Series Title/Season NN/` layout the NFO sits inside).
</objective>

<execution_context>
@/home/fmoura/Documents/Applications/crunchyroll-downloader/.opencode/gsd-core/workflows/execute-plan.md
@/home/fmoura/Documents/Applications/crunchyroll-downloader/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-organized-output-folder-metadata/07-CONTEXT.md
@.planning/phases/07-organized-output-folder-metadata/07-PATTERNS.md
@.planning/phases/07-organized-output-folder-metadata/07-01-CONTEXT.md
@.planning/phases/07-organized-output-folder-metadata/07-02-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/research/STACK.md
@internal/api/types.go
@internal/api/season.go
@internal/api/season_test.go
@internal/api/episode.go
@internal/api/testdata/api/episode-info-response.json
@internal/download/episode.go
@internal/download/episode_test.go
@internal/download/season.go
@internal/download/season_test.go
@internal/diag/diag.go
@internal/output/output.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Discover CMS response shapes, enrich EpisodeMetadata + SeasonEpisode with a series-id (Option A), add SeriesInfo type + GetSeriesInfo (D-06, D-07)</name>
  <files>internal/api/types.go, internal/api/season.go, internal/api/season_test.go, internal/api/testdata/api/episode-info-response.json</files>
  <read_first>
    - .planning/phases/07-organized-output-folder-metadata/07-CONTEXT.md D-06 (enrich from EXISTING `/content/v2/cms/objects/{id}` decode — NO new network call for episode NFO), D-07 (NEW series-level CMS call — LOCKED, must fire on the default production path)
    - .planning/phases/07-organized-output-folder-metadata/07-PATTERNS.md §`internal/api/types.go` (EpisodeMetadata lines 24-31; `SeasonEpisode` lines 42-51 — the struct whose existing GetSeasonEpisodes decode is the source of the Season-path series-id; the Season/Seasons sibling pair lines 53-60 as template for SeriesInfo/SeriesInfoResponse), §`internal/api/season.go` (GetSeasonEpisodes lines 11-43 — the CALLER whose decoded `SeasonEpisode` slice is enriched with a series-id; GetSeasons lines 45-77 — the skeleton to clone for GetSeriesInfo; note the `/content/v2/cms/seasons/%s/episodes` path at line 20 vs the `/content/v2/cms/series/%s/seasons` path at line 54), §`internal/api/episode.go` (the GetEpisodeInfo caller at lines 47-75 — the EXISTING decode that auto-populates new EpisodeMetadata fields without code change)
    - internal/api/types.go (EpisodeMetadata struct lines 24-31; `SeasonEpisode` struct lines 42-51 — the NEW series-id field attaches HERE for the season path; Season/Seasons at 53-60; EpisodeInfo at 19-22)
    - internal/api/season.go (GetSeasonEpisodes lines 11-43 — note the decoded `var episodes SeasonEpisodes` into `SeasonEpisode` struct; this is the struct the season-path series-id enriches; GetSeasons lines 45-77 — the exact skeleton to copy for GetSeriesInfo)
    - internal/api/episode.go (GetEpisodeInfo lines ~47-75 — the `c.url(fmt.Sprintf("/content/v2/cms/objects/%s?ratings=true&preferred_audio_language=ja-JP&locale=en-US", id))` path; note the `?ratings=true` flag already present, supporting ratings enrichment; the decoded `EpisodeMetadata` is the single-episode-path series-id source)
    - internal/api/testdata/api/episode-info-response.json (the CURRENT fixture — minimal; only title+episode_metadata with audio_locale/episode_number/season_number/series_title/availability_starts/versions. This fixture is INCOMPLETE relative to the real Crunchyroll response: the live response contains additional fields the episode NFO needs, INCLUDING a series-id field usable to call GetSeriesInfo. The executor MUST capture a real `/content/v2/cms/objects/{id}` response (and a real `/content/v2/cms/seasons/{id}/episodes` response) before locking the enriched field tags — DO NOT fabricate exact field names.)
    - internal/api/testdata/api/season-list-response.json (the GetSeasons test fixture — its `data[].id` is a SEASON id (e.g. "season-1-id"), NOT a series id. This file is for reference only; the series-id enrichment comes from the GetSeasonEpisodes response, NOT this file. If a season-list-response-equivalent for GetSeasonEpisodes does not exist as a testdata file, the executor captures the live response shape.)
    - internal/api/season_test.go (the httptest + `newTestClient`/`api.NewTestClient` pattern at lines 11-34 — copy verbatim, swap the path assertion to the series endpoint; ALSO add a GetSeasonEpisodes-decodes-series-id assertion if no such test exists — inline JSON in the test is acceptable, no new testdata file required)
    - internal/api/season_test.go for the imported `newTestClient` helper: also internal/api/client_helper.go if referenced
  </read_first>
  <action>
    This task does THREE discovery-driven groundings plus implementation. The series-id source (Option A, per D-07 LOCKED wiring) is decoded from the EXISTING responses — NO new network call is added just to obtain the id; the NEW network call is `GetSeriesInfo` itself.

    (A) DISCOVERY — Episode-level enrichment fields + single-episode-path series-id (D-06). The real Crunchyroll `/content/v2/cms/objects/{id}` response carries fields beyond what the current minimal testdata shows: e.g. a description/synopsis field, a duration field, ratings, a slug/identifier usable as `<uniqueid type="crunchyroll">`, AND a series-id field usable to call GetSeriesInfo on the single-episode path. Capture or inspect a real response (reuse the project's authenticated client if a helper exists, or reference the known Crunchyroll CMS v2 schema), then UPDATE the testdata fixture at internal/api/testdata/api/episode-info-response.json to include a representative subset of the newly-decoded fields (incl. the series-id field) so the enrichment decode is testable. Record the confirmed field names as a comment in internal/api/types.go above the new fields (e.g. `// CMS /content/v2/cms/objects/{id} fields: <list>`). Do NOT fabricate exact field names from memory — if a field cannot be confirmed, OMIT it and note the omission in the SUMMARY.

    (B) DISCOVERY — Season-path series-id on the EXISTING GetSeasonEpisodes response (Option A). The `/content/v2/cms/seasons/{id}/episodes` response (already called by `GetSeasonEpisodes` at `internal/api/season.go:11-43`) carries a series-id field on each episode object that the current flat `SeasonEpisode` struct (types.go lines 42-51) does NOT yet decode. Inspect a real GetSeasonEpisodes response (or the season-list hierarchy) to identify WHICH field carries the series-id — candidate names include a top-level `series_id` on each episode item, or a nested `episode_metadata.series_id`; the exact field path is confirmed by inspecting the live response. The executor MUST confirm the field exists before adding it; if the GetSeasonEpisodes response genuinely carries NO series-id field, the executor MUST escalate (D-07 is LOCKED and Option A is not viable without it) — in that case STOP and record the absence in the SUMMARY; do NOT silently fall back to a title-only tvshow.nfo (that would regress D-07). The expectation per Option A is that the field IS present (Crunkyroll CMS episode objects carry series linkage); the discovery confirms the exact name.

    (C) DISCOVERY — Series-level endpoint (D-07). Confirm the series-level CMS endpoint path by inspecting the real API. Candidate paths to try (in order, stop at first that returns a series-level object with title/plot/genre/studio): `/content/v2/cms/objects/{seriesId}` (mirrors the episode-info path), `/content/v2/cms/series/{id}` (mirrors the GetSeasons path prefix). Confirm the decoded JSON shape (`data` array with id/title/plot-or-description/genre[]/studio/images). Record the confirmed endpoint path + the confirmed response shape as a comment atop the new `GetSeriesInfo` method. D-07 is LOCKED — the series-level call is REQUIRED on the default path; if NEITHER candidate endpoint resolves, the executor MUST escalate (do NOT silently fall back to a title-only tvshow.nfo — that contradicts D-07).

    IMPLEMENTATION — enrich `EpisodeMetadata` (internal/api/types.go lines 24-31): add the confirmed enrichment fields (from discovery A) with lowercase-snake json tags matching the real response — including a `SeriesID`-equivalent field (the series-id usable to call GetSeriesInfo on the single-episode path), description/synopsis, duration, ratings, slug/the Crunchyroll content id. Keep existing field tags and order; append new fields. `json.Unmarshal` auto-populates new tags from the already-fetched body — NO change to internal/api/episode.go is needed (the existing decode covers it).

    IMPLEMENTATION — enrich `SeasonEpisode` (internal/api/types.go lines 42-51): add the confirmed series-id field (from discovery B) with the json tag matching the real GetSeasonEpisodes response. Keep existing field tags and order; append the new field. `json.Unmarshal` auto-populates it from the already-fetched GetSeasonEpisodes body — NO change to internal/api/season.go's `GetSeasonEpisodes` is needed. This is the field Task 3's `runSeason` reads as `episodes[0].<newField>` to call `client.GetSeriesInfo`.

    IMPLEMENTATION — add the new type pair modeled on `Seasons`/`Season` (lines 53-60):
      `type SeriesInfoResponse struct { Data []SeriesInfo \`json:"data"\` }` and
      `type SeriesInfo struct { ID string \`json:"id"\`; Title string \`json:"title"\`; ... confirmed enrichment fields (plot, genre []string, studio, ratings, images) ... }`.

    IMPLEMENTATION — add `GetSeriesInfo` to internal/api/season.go, cloning the `GetSeasons` skeleton (lines 45-77) exactly: same `audioLocale`/`subLocale` empty-string defaults, same `c.newRequest` + `c.Do` + `defer resp.Body.Close` + `io.ReadAll` + `json.Unmarshal` into the NEW `SeriesInfoResponse`; only the URL path and decoded type change. Use the confirmed path from discovery (C). Preserve the 401-refresh retry inherited from `c.Do` (client.go). Add a test `TestGetSeriesInfo` to internal/api/season_test.go cloned from the existing GetSeasons test (lines 11-34): httptest server asserts the request path equals the confirmed series endpoint, asserts `Authorization: Bearer initial-token`, returns a `{"data":[{"id":"...","title":"...",...enrichment fields...}]}` body matching the new SeriesInfo shape, and asserts the decoded field values equal the expected constants. Also add (if no such test exists) `TestGetSeasonEpisodesDecodesSeriesID` asserting the enriched `SeasonEpisode` series-id field populates from the inline JSON body — this protects the Option-A wiring the season path depends on.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && go build ./internal/api/... && go vet ./internal/api/... && go test ./internal/api/... -count=1 -v -run 'TestGetSeriesInfo|TestGetSeasonEpisodes|TestGetEpisodeInfo|TestGetSeasons'</automated>
  </verify>
  <acceptance_criteria>
    - internal/api/types.go: `grep -n 'type SeriesInfo struct\|type SeriesInfoResponse struct' internal/api/types.go` matches both new types.
    - internal/api/types.go: `EpisodeMetadata` has at least one NEW appended field beyond the pre-Phase-7 set (audio_locale, episode_number, season_number, series_title, availability_starts, versions) — INCLUDING a series-id field — verified by `grep -A 12 'type EpisodeMetadata struct' internal/api/types.go` showing the new tags. Each new field has a json tag matching the confirmed real response field name (NOT a fabricated guess); a comment block listing the confirmed CMS field names sits above the new fields.
    - internal/api/types.go: `SeasonEpisode` has a NEW series-id field beyond the pre-Phase-7 set (id, versions, season_number, episode_number, series_title, audio_locale, title, availability_starts) — verified by `grep -A 12 'type SeasonEpisode struct' internal/api/types.go` showing the new tag. This field is the Option-A source for the season-path GetSeriesInfo call (Task 3).
    - internal/api/testdata/api/episode-info-response.json: the fixture now includes a representative subset of the newly-decoded fields INCLUDING the series-id field (the json object under `data[0]` / `episode_metadata` / sibling keys now has more keys than the pre-Phase-7 6 keys) — verified by inspecting the file.
    - internal/api/season.go: `grep -n 'func (c \*Client) GetSeriesInfo' internal/api/season.go` matches the new method; it uses `c.newRequest` + `c.Do` + `io.ReadAll` + `json.Unmarshal` into `SeriesInfoResponse` (same skeleton fields as GetSeasons).
    - internal/api/season.go: the GetSeriesInfo URL path matches the confirmed discovery finding (assert via grep for the confirmed path literal).
    - `go test ./internal/api/... -run TestGetSeriesInfo -count=1 -v` prints `--- PASS` and the test asserts both the request path AND the decoded enrichment field values.
    - `go test ./internal/api/... -run TestGetSeasonEpisodes -count=1 -v` prints `--- PASS` and (if the new assertion was added) asserts the SeasonEpisode series-id field decodes from the response.
    - `go test ./internal/api/... -count=1` exits 0 (no regression to GetEpisodeInfo/GetSeasons/GetSeasonEpisodes).
    - D-07 LOCKED honored: `GetSeriesInfo` is NOT omitted — the series-level CMS call exists and is testable. There is NO title-only-aggregation fallback that bypasses the network call (the only title-only path is Task 3's GetSeriesInfo-ERROR fallback, which still requires GetSeriesInfo to have been attempted).
  </acceptance_criteria>
  <done>`EpisodeMetadata` enriched (incl. series-id) from the real CMS response, `SeasonEpisode` enriched with a series-id field decoded from the existing GetSeasonEpisodes response (Option A — no new network call for the id itself), `SeriesInfo`/`SeriesInfoResponse` types and `GetSeriesInfo` method added and tested, `GetEpisodeInfo`'s existing decode auto-populates the new EpisodeMetadata fields with no episode.go path change needed.</done>
</task>

<task type="auto">
  <name>Task 2: Build internal/nfo/ emitter (tvshow + episodedetails) with encoding/xml — Pitfall 6 escape tests (D-08, D-09)</name>
  <files>internal/nfo/types.go, internal/nfo/nfo.go, internal/nfo/nfo_test.go</files>
  <read_first>
    - .planning/research/PITFALLS.md Pitfall 6 (NFO XML with unescaped `&`/`<` causes Jellyfin/Kodi to silently reject the NFO and fall back to filename heuristics — `encoding/xml` mandatory, `fmt.Sprintf` explicitly REJECTED), and the integration-gotcha row on folder-metadata placement and cleanup
    - .planning/research/STACK.md lines 29, 122, 137 (encoding/xml is the right tool; ~40-line struct set; `<uniqueid type="crunchyroll">` via `xml:"type,attr"` + `xml:",chardata"`; no NFO library beats raw encoding/xml)
    - .planning/phases/07-organized-output-folder-metadata/07-PATTERNS.md §`internal/nfo/` NEW package (the package-shape analog to internal/diag/diag.go, the marshal struct tag analog to internal/api/types.go json tags swapped to xml, the sentinel-error + 404 pattern, and the FULL mandatory test case list: `&`, `<`, `>`, `"`, `'`, CJK, emoji)
    - .planning/phases/07-organized-output-folder-metadata/07-CONTEXT.md D-08 (`<uniqueid type="crunchyroll">` with Crunchyroll content id as chardata), D-09 (non-fatal — NFO write errors warn, never fail the download)
    - internal/api/types.go (the enriched EpisodeMetadata and new SeriesInfo from Task 1 — these are the inputs to the emitter)
    - internal/diag/diag.go (the package singleton pattern to mirror for any package-level seam var, if added)
  </read_first>
  <action>
    Create a new `internal/nfo/` package with three files: types.go, nfo.go, nfo_test.go.

    types.go — define the XML struct set modeled on the Jellyfin/Kodi NFO schema, swapping `json:` tags for `xml:` tags:
      - `type tvshow struct { XMLName xml.Name \`xml:"tvshow"\`; Title string \`xml:"title"\`; Plot string \`xml:"plot"\`; Genre []string \`xml:"genre"\`; Studio string \`xml:"studio"\`; UniqueID uniqueID \`xml:"uniqueid"\`; ...ratings element if the enriched SeriesInfo carries ratings... }`
      - `type episodedetails struct { XMLName xml.Name \`xml:"episodedetails"\`; Title string \`xml:"title"\`; ShowTitle string \`xml:"showtitle"\`; Season int \`xml:"season"\`; Episode int \`xml:"episode"\`; Plot string \`xml:"plot"\`; UniqueID uniqueID \`xml:"uniqueid"\`; ...aired, runtime, ratings as available from the enriched EpisodeMetadata... }`
      - `type uniqueID struct { Type string \`xml:"type,attr"\`; Default bool \`xml:"default,attr,omitempty"\`; Value string \`xml:",chardata"\` }` — the `type,attr` + `,chardata` pair produces `<uniqueid type="crunchyroll">CONTENT_ID</uniqueid>`.

    nfo.go — implement two exported entry functions:
      - `func WriteEpisode(ctx context.Context, path string, info *api.EpisodeInfo, contentID string) error`: build an `episodedetails` from `info.EpisodeMetadata` + `info.Title`, using `info.EpisodeMetadata.SeasonNumber` (NOT EpisodeNumber) for the `<season>` element, the enriched plot/description for `<plot>` (skip empty `<plot>` if the field is absent/empty — use `omitempty` on the field tag), and `contentID` for the uniqueid chardata. Marshal with `xml.MarshalIndent(doc, "", "  ")`, prepend `[]byte(xml.Header)` (so the `<?xml version="1.0" encoding="utf-8" ?>` declaration is present — Jellyfin/Kodi both require it), write via `os.WriteFile(path, out, 0644)`. Wrap any marshal/write error with `fmt.Errorf("nfo episode %s: %w", path, err)` and RETURN it (the caller in download/episode.go converts the returned error to a non-fatal warn per D-09 — do NOT warn from inside nfo.go itself; nfo.go is a pure emitter).
      - `func WriteTVShow(ctx context.Context, path string, info *api.SeriesInfo) error`: build a `tvshow` from `info` (Title, Plot, Genre, Studio, ratings), uniqueid chardata = `info.ID` (the Crunchyroll series content id) with `type="crunchyroll"`. Same marshal+header+WriteFile pattern; same error-return convention.
    Add a package-level seam var ONLY if Task 3 needs injection indirection (it will): `var WriteEpisodeFn = WriteEpisode` and `var WriteTVShowFn = WriteTVShow` (or equivalent) — choose the seam form that mirrors internal/download/episode.go:27-47 (`var episodeMerge = mux.MergeEverything`). Default the seam var to the real function so production callers get real behavior; tests override via `t.Cleanup`.

    nfo_test.go — table-driven stdlib tests (v1.0 D-03: NO testify). Cases MUST cover (per Pitfalls Pitfall 6):
      - title/plot containing `&`, `<`, `>`, `"`, `'` → marshaled output contains entity-escaped forms (`&amp;`, `&lt;`, `&gt;`) AND does NOT contain a literal unescaped `&` or `<` in the element text.
      - CJK title (`Hidamari Sketch × Honeycomb`, the `×` U+00D7 multiplication sign) → output contains the UTF-8 `×` byte sequence intact, not corrupted.
      - emoji title (e.g. a 🎌 in plot) → UTF-8 preserved.
      - `<uniqueid type="crunchyroll">CONTENT_ID</uniqueid>` is present in both tvshow and episodedetails output; `type,attr` produces `type="crunchyroll"` and chardata equals the passed content id.
      - empty optional fields (empty plot) are omitted via `omitempty` — output has no empty `<plot></plot>`.
      - the `xml.Header` (`<?xml ...?>`) prepends the body.
    Assert via string assertions on the marshaled `[]byte` (use `strings.Contains` / `bytes.Contains`); assert well-formedness by re-parsing with `xml.Unmarshal` into an empty struct and confirming no error. Use `t.Run` sub-tests for each escape case. Keep fixtures minimal (no temp-file IO required where `WriteEpisode`/`WriteTVShow` can be tested via marshaling inside the function — split so the marshal is testable without disk: a private `marshalEpisode(*api.EpisodeInfo, contentID string) ([]byte, error)` and `WriteEpisode` = `marshalEpisode` + `os.WriteFile`; test `marshalEpisode` directly for the escape cases, test `WriteEpisode` once against a `t.TempDir()` path).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && go build ./internal/nfo/... && go vet ./internal/nfo/... && go test ./internal/nfo/... -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - internal/nfo/types.go: `grep -n 'type tvshow struct\|type episodedetails struct\|type uniqueID struct' internal/nfo/types.go` matches all three.
    - internal/nfo/nfo.go: `grep -n 'xml.MarshalIndent\|xml.Header' internal/nfo/nfo.go` matches BOTH (marshal + header prepend).
    - internal/nfo/nfo.go: `grep -n 'fmt.Sprintf' internal/nfo/nfo.go` returns NO matches inside the marshaling functions (Pitfalls Pitfall 6 — `fmt.Sprintf` is forbidden for NFO body construction). If `fmt.Sprintf` is used only for the returned error wrap (`fmt.Errorf`), that is acceptable; the marshaling path must not use it.
    - internal/nfo/nfo.go: exported `WriteEpisode` and `WriteTVShow` functions exist: `grep -n 'func WriteEpisode\|func WriteTVShow' internal/nfo/nfo.go` matches both; `<season>` element uses `info.EpisodeMetadata.SeasonNumber`.
    - `<uniqueid type="crunchyroll">` assertion: `internal/nfo/nfo_test.go` contains a test case asserting `strings.Contains(output, \`<uniqueid type="crunchyroll">\`)` (or equivalent) for both tvshow and episodedetails.
    - Escape cases: `grep -n 'amp;\|&lt;\|&gt;' internal/nfo/nfo_test.go` shows assertions for entity-escaped forms; `grep -n 'Hidamari\|×' internal/nfo/nfo_test.go` shows the CJK case; `grep -n '🎌\|emoji' internal/nfo/nfo_test.go` shows the emoji case.
    - `go test ./internal/nfo/... -count=1 -v` exits 0 with a `--- PASS` per escape sub-test.
    - No testify import: `grep -ni 'testify' internal/nfo/` returns no matches.
  </acceptance_criteria>
  <done>`internal/nfo/` package emits entity-escaped, Jellyfin/Kodi-scannable `tvshow.nfo` and per-episode `.nfo` with `<uniqueid type="crunchyroll">`; all Pitfall 6 escape cases pass; the package is a pure emitter (returns errors, never logs/warns); seam var exists for Task 3 injection.</done>
</task>

<task type="auto">
  <name>Task 3: Wire per-episode NFO (download/episode.go post-merge) + once-per-series tvshow.nfo via GetSeriesInfo-default-path (download/season.go pre-loop) with non-fatal warns (D-07, D-09, D-02 tvshow guard)</name>
  <files>internal/download/episode.go, internal/download/episode_test.go, internal/download/season.go, internal/download/season_test.go</files>
  <read_first>
    - .planning/phases/07-organized-output-folder-metadata/07-PATTERNS.md §`internal/download/episode.go` (the per-episode NFO insertion point at line ~340-343 after a successful merge; the seam-var block at lines 27-47 — where `episodeMerge = mux.MergeEverything` lives and where `episodeWriteNfo` + `episodeGetSeriesInfo` seams append; the `strings.TrimSuffix(outputFile, ".mkv") + ".nfo"` path derivation), §`internal/download/season.go` (the pre-loop insertion at lines 47-52 before the `for _, ep := range episodes` loop; the os.Stat-guarded once-per-series pattern mirroring D-02; the series-root computation parallel to episode.go outputBase), Shared Patterns §Non-fatal Warn (dual-channel `output.Global.Warn` + `diag.<Subsystem>Logger.Warn` with nil-guard), §Resumability Skip, §Bearer-Token Authenticated GET
    - .planning/phases/07-organized-output-folder-metadata/07-CONTEXT.md D-02 (keys off sanitized Series Title — apply the same os.Stat skip to tvshow.nfo so multi-season invocations don't re-fetch), D-03 (single-episode flow produces the SAME nested tree + NFO + tvshow.nfo), D-07 LOCKED (the new series-level CMS call MUST fire on the default production path — Option A sources seriesID from the enriched structs, NO main.go change), D-09 (non-fatal warn convention)
    - internal/download/episode.go (the Episode signature at line 62: `Episode(ctx, client *api.Client, baseContentID string, info *api.EpisodeInfo, ..., outputDir string, totalEpisodes int)` — `client` IS available so `client.GetSeriesInfo` is callable; `baseContentID` is the EPISODE id on the single-episode path, NOT the series id — use `info.EpisodeMetadata.SeriesID` (Task 1 enrichment A) for GetSeriesInfo; the seam-var block lines 27-47 to append `episodeWriteNfo` + `episodeGetSeriesInfo`; the post-merge insertion point at line ~340-343 — `completed = true` after `episodeMerge` succeeds; the deep outputFile and outputBase/seasonPath locals from Plan 02; the import block at lines 3-23 to add `crunchyroll-downloader/internal/nfo` and `strings`-if-not-present)
    - internal/download/season.go (the runSeason loop at lines 47-76; the pre-loop insertion after `output.Global.Info("Downloading season %d...", ...)` at line 52; the series-root computation pattern episode.go uses: `outputBase := cleanSeriesTitle; if outputDir != "" { outputBase = filepath.Join(outputDir, cleanSeriesTitle) }` — replicate HERE inline; the episodeDownloader seam type at line 13 as the template for any new seam var. runSeason receives `episodes []api.SeasonEpisode` — `episodes[0].SeriesID` (Task 1 enrichment B) IS the series-id for GetSeriesInfo. NO seriesID param needs threading through main.go — Option A keeps main.go unmodified.)
    - internal/download/season_test.go (TestRunSeasonContinuesAfterEpisodeFailure at lines 50-108 — the seam-injection template; the inline `func` override pattern)
    - internal/download/episode_test.go (the `restoreEpisodeTestSeams` helper at lines ~366-422 — extend for `episodeWriteNfo` + `episodeGetSeriesInfo`)
    - internal/nfo/nfo.go (the WriteEpisode/WriteTVShow signatures + seam from Task 2)
    - internal/api/types.go (the enriched EpisodeMetadata + SeriesInfo from Task 1; the GetSeriesInfo signature `(*Client).GetSeriesInfo(ctx, seriesId, audioLocale, subLocale) (*SeriesInfo, error)` from Task 1; the NEW `SeasonEpisode.SeriesID` field from Task 1 enrichment B)
    - internal/diag/diag.go (MuxLogger, APILogger, DownloadLogger — nil-guard pattern `if diag.MuxLogger != nil` at episode.go:126-127 already established)
  </read_first>
  <action>
    The wiring is FULLY PRE-DECIDED (Option A). There are no conditional "if discovery chose X" branches below — the series-id source is fixed (`EpisodeMetadata.SeriesID` on the single-episode path, `SeasonEpisode.SeriesID` on the season path), and `client.GetSeriesInfo` fires UNCONDITIONALLY on the default production path (D-07 LOCKED). The title-only `tvshow.nfo` is written ONLY when the `GetSeriesInfo` call itself errors (D-09 non-fatal fallback).

    WIRE PER-EPISODE NFO + SINGLE-EPISODE tvshow.nfo (internal/download/episode.go):
    1. Add `"crunchyroll-downloader/internal/nfo"` to the import block (lines 3-23). Add `"strings"` if not already imported (check current imports).
    2. In the seam-var block (lines 27-47), append TWO seams mirroring `episodeMerge = mux.MergeEverything`:
       - `episodeWriteNfo = nfo.WriteEpisode` (per-episode NFO emitter; tests override to a fake).
       - `episodeGetSeriesInfo = func(ctx context.Context, client *api.Client, seriesID, audioLocale, subLocale string) (*api.SeriesInfo, error) { return client.GetSeriesInfo(ctx, seriesID, audioLocale, subLocale) }` — indirection so single-episode tvshow.nfo tests can stub the series call without a live HTTP server; matches the `episodeGetEpisode`/`episodeFetchManifest` indirection shape. Align with the seam form Task 2 exposed (if Task 2 used `nfo.WriteEpisodeFn`, use the package-call form here instead).
    3. After the `if err := episodeMerge(...); err != nil { return ... }` block (line ~340-342) and AFTER `completed = true` (line ~343) — so the NFO only writes on a successful merge — compute `nfoPath := strings.TrimSuffix(outputFile, ".mkv") + ".nfo"` and invoke `if err := episodeWriteNfo(ctx, nfoPath, info, baseContentID); err != nil {` then NON-FATAL: `output.Global.Warn("Failed to write NFO for %s: %v", info.Title, err)` and `if diag.MuxLogger != nil { diag.MuxLogger.Warn("nfo_write_failed", "episode", info.EpisodeMetadata.EpisodeNumber, "path", nfoPath, "err", err) }` (do NOT return the error; the `.mkv` succeeded). Use `baseContentID` (the param already in the `Episode` signature) as the uniqueid chardata — it IS the Crunchyroll episode content id for `/watch/<id>`.
    4. SINGLE-EPISODE tvshow.nfo (D-03 + D-07 on the default path): immediately after the per-episode NFO block, guard a single-episode tvshow.nfo write with `tvshowNfoPath := filepath.Join(outputBase, "tvshow.nfo"); if _, err := os.Stat(tvshowNfoPath); err != nil {` (D-02 resumability guard — a re-run of the same episode skips the re-fetch). INSIDE the guard, on the DEFAULT path: call `seriesInfo, serr := episodeGetSeriesInfo(ctx, client, info.EpisodeMetadata.SeriesID, "", "")` (using the enriched `EpisodeMetadata.SeriesID` from Task 1; pass empty audioLocale/subLocale so GetSeriesInfo applies its ja-JP/en-US defaults). If `serr == nil` AND `seriesInfo != nil`: write the RICH tvshow.nfo via `episodeWriteTvshowNfo(ctx, tvshowNfoPath, seriesInfo)` (add `episodeWriteTvshowNfo = nfo.WriteTVShow` to the seam block too). If `serr != nil`: NON-FATAL warn per D-09 — `output.Global.Warn("Failed to fetch series metadata for %s: %v", info.EpisodeMetadata.SeriesTitle, serr)` and `if diag.APILogger != nil { diag.APILogger.Warn("series_info_fetch_failed", "series", info.EpisodeMetadata.SeriesTitle, "err", serr) }` — THEN fall back to a title-only tvshow.nfo using `fallbackInfo := &api.SeriesInfo{ID: info.EpisodeMetadata.SeriesID, Title: info.EpisodeMetadata.SeriesTitle}` and `episodeWriteTvshowNfo(ctx, tvshowNfoPath, fallbackInfo)` (still wrapped non-fatal: a write error here also warns, never returns). The episode download NEVER aborts on a series-info or tvshow-nfo failure.
    5. Do NOT modify the post-NFO success log block (lines ~345-364). NFO write failure does not flip `completed` back to false.

    WIRE ONCE-PER-SERIES tvshow.nfo (internal/download/season.go):
    1. Compute the series root inside runSeason using the SAME inline 3-line pattern as episode.go (parallel computation, no cross-file helper import — keeps the seam honest and the two files converging on ONE root): `cleanSeriesTitle := sanitizeFilename(episodes[0].SeriesTitle)` (`sanitizeFilename` is in the same `download` package, directly callable from season.go), then `seriesRoot := cleanSeriesTitle; if outputDir != "" { seriesRoot = filepath.Join(outputDir, cleanSeriesTitle) }` then `if err := os.MkdirAll(seriesRoot, 0777); err != nil { return fmt.Errorf("creating series directory: %w", err) }`. Add `"path/filepath"` to the season.go imports if not present (season.go currently imports context/fmt/strings + internal/api, internal/diag, internal/output; filepath is NOT yet imported).
    2. Insert a NEW once-per-series step AFTER the `output.Global.Info("Downloading season %d...", ...)` line (~52) and BEFORE the `for _, ep := range episodes` loop (~55): `tvshowNfoPath := filepath.Join(seriesRoot, "tvshow.nfo"); if _, err := os.Stat(tvshowNfoPath); err != nil {` (D-02 resumability guard — multi-season invocations skip re-fetch/re-write). INSIDE the guard, on the DEFAULT path: `seriesID := episodes[0].SeriesID` (the enriched `SeasonEpisode.SeriesID` field from Task 1 enrichment B — Option A), then `seriesInfo, serr := seriesGetSeriesInfo(ctx, client, seriesID, "", "")`. Add the `seriesGetSeriesInfo` + `seriesWriteTvshowNfo` seam vars mirroring the episodeDownloader seam form (section 4 below). If `serr == nil` AND `seriesInfo != nil`: write RICH tvshow.nfo via `seriesWriteTvshowNfo(ctx, tvshowNfoPath, seriesInfo)`. If `serr != nil`: NON-FATAL warn — `output.Global.Warn("Failed to fetch series metadata for %s: %v", episodes[0].SeriesTitle, serr)` and `if diag.APILogger != nil { diag.APILogger.Warn("series_info_fetch_failed", "series", episodes[0].SeriesTitle, "err", serr) }` — THEN fall back to a title-only tvshow.nfo using `fallbackInfo := &api.SeriesInfo{ID: seriesID, Title: episodes[0].SeriesTitle}` and `seriesWriteTvshowNfo(ctx, tvshowNfoPath, fallbackInfo)` (a write error here also non-fatal-warns, never returns). The season episode loop runs REGARDLESS of the series-metadata outcome (D-09).
    3. Add the path/filepath + os imports to season.go (check current imports — `os` likely not imported yet; `path/filepath` not yet imported).

    SEAM VARS (mirroring the existing `episodeDownloader` function-type seam at season.go:13 and the `episodeMerge` var at episode.go:46):
    - internal/download/episode.go seam block (lines 27-47) — add THREE: `episodeWriteNfo = nfo.WriteEpisode`, `episodeWriteTvshowNfo = nfo.WriteTVShow`, `episodeGetSeriesInfo = func(ctx, client, seriesID, audioLocale, subLocale) (*api.SeriesInfo, error) { return client.GetSeriesInfo(...) }` (the indirection wraps the method receiver so tests stub without touching `*api.Client`).
    - internal/download/season.go — add a package-level seam: `var seriesGetSeriesInfo = func(ctx context.Context, client *api.Client, seriesID, audioLocale, subLocale string) (*api.SeriesInfo, error) { return client.GetSeriesInfo(ctx, seriesID, audioLocale, subLocale) }` AND `var seriesWriteTvshowNfo = nfo.WriteTVShow`. Default both to the real functions so production gets real behavior; tests override via assignment + `t.Cleanup` restore (mirroring `restoreEpisodeTestSeams`).
    - This keeps main.go UNMODIFIED — no seriesID threading through `download.Season`'s signature, no call-site edit. The series-id is sourced in-package from the already-fetched `episodes[0].SeriesID`.

    TESTS:
    - internal/download/episode_test.go: extend `restoreEpisodeTestSeams` (lines ~366-422) to save/restore the new `episodeWriteNfo`, `episodeWriteTvshowNfo`, `episodeGetSeriesInfo` seams. Add `TestEpisodeWritesPerEpisodeNfoNonFatal` asserting (a) after a successful merge, `episodeWriteNfo` IS invoked with the path beside the `.mkv` and `baseContentID` as uniqueid; (b) when `episodeWriteNfo` returns an error, `Episode` still returns nil and `output.Global.Warn` received the NFO warn line. Add `TestEpisodeWritesTvshowNfoOnSingleEpisodeFlow` asserting (a) on the DEFAULT path (seam returns a rich `*api.SeriesInfo` with Plot/Genre/Studio), `episodeGetSeriesInfo` IS invoked with `info.EpisodeMetadata.SeriesID` and `episodeWriteTvshowNfo` is invoked with that rich SeriesInfo (NOT a title-only one); (b) on the ERROR path (seam returns an error), `Episode` still returns nil, both warn channels fire (`output.Global.Warn` + `diag.APILogger.Warn("series_info_fetch_failed", ...)`), and a title-only tvshow.nfo IS still written (the fallback SeriesInfo has only ID+Title).
    - internal/download/season_test.go: add `TestRunSeasonWritesTvshowNfoOnceBeforeEpisodeLoop` asserting (a) on the DEFAULT path (seam returns rich SeriesInfo), `seriesGetSeriesInfo` IS invoked with `episodes[0].SeriesID` and `seriesWriteTvshowNfo` receives the rich SeriesInfo (NOT title-only); (b) when a `tvshow.nfo` already exists at `seriesRoot`, the os.Stat guard SKIPS both the fetch and the write (use a pre-created `tvshow.nfo` + assert the seam was NOT invoked). Add `TestRunSeasonTvshowNfoNonFatal` asserting (a) when `seriesGetSeriesInfo` returns an error, the episode loop STILL completes ALL episodes (no early return) and `output.Global.Warn` received the "Failed to fetch series metadata" line; (b) a title-only tvshow.nfo IS written as the error-fallback. Mirror the existing `runSeason` inline-func seam-injection pattern at season_test.go:74-87.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && go build ./... && go vet ./... && go test ./internal/download/... ./internal/nfo/... ./internal/api/... -count=1 -v -run 'TestEpisodeWritesPerEpisodeNfo|TestEpisodeWritesTvshowNfo|TestRunSeasonWritesTvshowNfo|TestRunSeasonTvshowNfoNonFatal|TestGetSeriesInfo'</automated>
  </verify>
  <acceptance_criteria>
    - internal/download/episode.go: `grep -n 'episodeWriteNfo\|episodeWriteTvshowNfo\|episodeGetSeriesInfo' internal/download/episode.go` matches the THREE seam-var decls in the lines 27-47 block AND the post-merge/per-tvshow call sites.
    - internal/download/episode.go: the post-merge per-episode NFO call is followed by `output.Global.Warn` + a nil-guarded `diag.MuxLogger.Warn`, NOT a `return err`: `grep -A 4 'episodeWriteNfo(ctx' internal/download/episode.go` shows the warn path, no hard return.
    - internal/download/episode.go: `grep -n 'strings.TrimSuffix(outputFile' internal/download/episode.go` matches the `.nfo` path derivation.
    - internal/download/episode.go: a single-episode tvshow.nfo write exists guarded by `os.Stat(filepath.Join(outputBase, "tvshow.nfo"))` AND invokes `episodeGetSeriesInfo(ctx, client, info.EpisodeMetadata.SeriesID, ...)` on the DEFAULT path (NOT a title-only fallback before attempting the call): `grep -n 'episodeGetSeriesInfo' internal/download/episode.go` matches the call; `grep -n 'tvshow.nfo' internal/download/episode.go` matches the guard.
    - internal/download/episode.go: D-07 default path provable — the rich `*api.SeriesInfo` (with Plot/Genre/Studio) is passed to `episodeWriteTvshowNfo` on the success branch; the title-only `api.SeriesInfo{ID: ..., Title: ...}` appears ONLY inside the `serr != nil` error-fallback branch. `grep -n 'SeriesInfo{' internal/download/episode.go` matches exactly within the error-fallback scope.
    - internal/download/season.go: `grep -n 'tvshow.nfo\|seriesRoot\|seriesGetSeriesInfo\|seriesWriteTvshowNfo' internal/download/season.go` matches the pre-loop step; the series-root computation uses the same `cleanSeriesTitle`+`outputDir` projection as episode.go (verify by `grep -n 'filepath.Join(outputDir, cleanSeriesTitle)' internal/download/season.go` matching).
    - internal/download/season.go: D-07 default path provable — `seriesGetSeriesInfo(ctx, client, episodes[0].SeriesID, ...)` is invoked on the DEFAULT path inside the os.Stat guard (NOT before any GetSeriesInfo attempt); the title-only `api.SeriesInfo{ID: seriesID, Title: episodes[0].SeriesTitle}` appears ONLY inside the `serr != nil` fallback. `grep -n 'episodes\[0\].SeriesID' internal/download/season.go` matches.
    - internal/download/season.go: the tvshow.nfo write is `os.Stat`-guarded (so multi-season invocation skips): `grep -B2 -A2 'os.Stat.*tvshow.nfo' internal/download/season.go` matches.
    - internal/download/season.go: NFO/series-info errors are non-fatal — `grep -n 'series_info_fetch_failed\|tvshow_nfo_write_failed' internal/download/season.go` matches; `runSeason` does NOT return these errors (the `for _, ep` loop runs regardless).
    - `go test ./internal/download/... -run 'TestEpisodeWritesPerEpisodeNfo|TestEpisodeWritesTvshowNfo|TestRunSeasonWritesTvshowNfo|TestRunSeasonTvshowNfoNonFatal' -count=1 -v` prints `--- PASS` for each, AND the default-path assertions verify the RICH SeriesInfo (not title-only) is passed to the writer seam.
    - `go build ./...` and `go vet ./...` exit 0 (whole repo builds — main.go still compiles even though not modified; the new imports resolve).
    - No new testify import in either test file: `grep -ni 'testify' internal/download/episode_test.go internal/download/season_test.go` returns nothing new.
    - No main.go modification: `git diff --name-only HEAD` shows main.go NOT in the changed set for this plan.
  </acceptance_criteria>
  <done>Per-episode `.nfo` writes beside every `.mkv` with `<uniqueid type="crunchyroll">`; `tvshow.nfo` writes once at the series root (single-episode AND season flows) via `client.GetSeriesInfo` on the DEFAULT production path (D-07 LOCKED satisfied, Option A source from enriched structs, NO main.go change); a title-only `tvshow.nfo` is written ONLY when GetSeriesInfo errors (D-09 non-fatal warn); `os.Stat` resumability guards multi-season runs; full repo builds and the download+nfo+api test suites are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Crunchyroll CMS JSON → Go struct → XML NFO file | Authenticated CMS response (title/plot/genre contain arbitrary Unicode incl. `&`, `<`, emoji, CJK) is decoded then re-emitted as XML written to disk under the sanitized series-root folder. |
| Series root folder path construction | `cleanSeriesTitle` → `os.MkdirAll` + `os.WriteFile` for `tvshow.nfo`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03-I | Information Disclosure / Integrity | internal/nfo/ XML emission | high | mitigate | Use stdlib `encoding/xml` (`xml.MarshalIndent` + `xml.Header`) which entity-escapes `&`→`&amp;`, `<`→`&lt;`, `>`→`&gt;`, `"`→`&quot;` by default. Forbid `fmt.Sprintf` for the NFO body (Pitfalls Pitfall 6). Test cases: `&`, `<`, `>`, `"`, `'`, CJK (`×`), emoji — assert escaped/preserved output. (nfo_test.go escape cases are the mitigation proof.) |
| T-07-04-T | Tampering (path traversal) | internal/download/season.go + episode.go series-root path | medium | mitigate | Reuse `sanitizeFilename` (episode.go:49) on the series title before `os.MkdirAll`/`os.WriteFile`; it strips `/`, `\`, `:`, `*`, `?`, `"`, `<`, `>`, `|` and collapses underscores, so `..` cannot survive through the title. `tvshow.nfo`, `Season %02d` (int-derived), and the episode filename are all generated from trusted (int/sanitized) inputs. No glob/wildcard cleanup introduced (Pitfall 6 cleanup-glob warning respected — NFO writes are exact-path `os.WriteFile` to known paths only). |
| T-07-05-D | Denial of Service (download blocked by metadata failure) | download/season.go series-info pre-loop + episode.go post-merge NFO | medium | mitigate | All NFO writes and the series-info fetch are NON-FATAL (D-09/D-12): error → `output.Global.Warn` + `diag.*Logger.Warn` (nil-guarded), download proceeds. No metadata failure aborts the `.mkv` (matches Phase 6 D-09 warn-not-fatal convention). The `os.Stat` guard on `tvshow.nfo` prevents re-fetching series info every season/episode, bounding metadata network calls to once-per-series-root. |
| T-07-06-E | Elevation (unscoped file write) | tvshow.nfo / per-episode .nfo write targets | low | mitigate | NFO write paths are constructed from the SAME `outputBase`/`seasonPath` the `.mkv` uses (no separate root resolution), so an NFO can never write outside the series folder. The `.nfo` path is `strings.TrimSuffix(outputFile, ".mkv") + ".nfo"` — co-located with the `.mkv`. `outputDir` is operator-validated (Phase 1/3 `validateOutputDir`). |

## Security Note — Real Jellyfin + Kodi scan
ROADMAP success criterion 4 requires "a real Jellyfin and a real Kodi scan". The mitigation for T-07-03-I (entity escaping via `encoding/xml`) is the technical precondition for that scan to succeed (an unescaped `&` causes Jellyfin/Kodi to silently reject the NFO — Pitfalls Pitfall 6). A live scan against running Jellyfin/Kodi instances is a manual end-of-phase UAT step (not automatable in unit tests); the unit-test escape cases prove the XML is well-formed and entity-escaped, which is the closest automated proxy. The end-of-phase UAT (per /gsd-verify-work) covers the live-scan confirmation.
</threat_model>

<verification>
- `go build ./...` and `go vet ./...` exit 0 (whole repo compiles with the new `internal/nfo/` package and the enriched types).
- `go test ./internal/nfo/... ./internal/api/... ./internal/download/... -count=1` exits 0 (all three suites green; no regression to mux tests either — `go test ./internal/mux/... -count=1` already covered by Plan 01).
- Spot-check on a real run (manual/UAT): a season download produces `Series Title/tvshow.nfo` + `Series Title/Season 01/Series Title S01E01 - Title.nfo` (and `.mkv`) with `<uniqueid type="crunchyroll">` present and entity-escaped title/plot. This is the end-of-phase UAT step, NOT a CI gate.
- NFO entity-escaping is provable in CI: the nfo_test.go escape cases assert `&amp;`/`&lt;`/`&gt;` are present for escaping inputs.
- Season number in NFO: `<season>` element (episodedetails) uses `info.EpisodeMetadata.SeasonNumber` — Plan 01's mux fix is mirrored into the NFO (OUT-03 GEN the file-metadata bug does not resurface in NFO).
</verification>

<success_criteria>
- META-01 delivered: `tvshow.nfo` written at the series root for both single-episode and season flows, Jellyfin/Kodi-scannable (well-formed XML with header + entity escaping).
- META-02 delivered: per-episode `.nfo` written beside each `.mkv` with `<uniqueid type="crunchyroll">` using the Crunchyroll content id (stable across title drift).
- D-06 delivered (approach): `EpisodeMetadata` enriched from the EXISTING `/content/v2/cms/objects/{id}` decode — no new network call for episode NFO. Exact enrichment fields (incl. a series-id field) confirmed from the real response (recorded in testdata + a comment in types.go), not fabricated.
- D-07 delivered (LOCKED, Option A): `client.GetSeriesInfo(ctx, seriesID, ...)` fires on the DEFAULT production path for BOTH flows — `seriesID` sourced from the enriched `EpisodeMetadata.SeriesID` (single-episode) / `SeasonEpisode.SeriesID` (season) decoded from the already-fetched CMS responses (no new network call just to obtain the id). The rich `tvshow.nfo` carries `<plot>/<genre>/<studio>` from the series call. A title-only `tvshow.nfo` is written ONLY as the GetSeriesInfo-error fallback (D-09 non-fatal warn). main.go is UNMODIFIED. The confirmed series endpoint path is recorded in the SUMMARY.
- D-08 delivered: `<uniqueid type="crunchyroll">` present in both NFO types with the content id as chardata.
- D-09 delivered: all NFO writes + the series-info fetch are non-fatal warns (dual-channel `output.Global.Warn` + `diag.*Logger.Warn` nil-guarded); the `.mkv` download never aborts on a metadata failure.
- Pitfalls Pitfall 6 honored: NFO emitted via `encoding/xml`, never `fmt.Sprintf`; escape-case tests pass for `&`, `<`, `>`, `"`, `'`, CJK, emoji.
- Cleans up after Plan 01 + Plan 02: NFO inherits the corrected `season_number` (Plan 01) and sits inside the nested `Series Title/Season NN/` layout (Plan 02).
</success_criteria>

<output>
Create `.planning/phases/07-organized-output-folder-metadata/07-03-SUMMARY.md` when done. SUMMARY MUST record:
- The confirmed Crunchyroll `/content/v2/cms/objects/{id}` enrichment field names (D-06) actually decoded — INCLUDING the `EpisodeMetadata.SeriesID` field name.
- The confirmed series-level endpoint path (D-07) — GetSeriesInfo is NOT omitted (D-07 LOCKED).
- The confirmed GetSeasonEpisodes series-id field name decoded onto `SeasonEpisode.SeriesID` (Option A source for the season-path GetSeriesInfo call). If discovery found the season-episodes response carries NO series-id, escalate per the Task 1 escalation clause (do NOT silently regress to title-only).
- That main.go was NOT modified (Option A wiring succeeded in-package).
</output>

## Artifacts this phase produces (Plan 03)

### New package
- `internal/nfo/` (package path) — NEW.

### New files
- `internal/nfo/types.go` — XML struct set: `tvshow`, `episodedetails`, `uniqueID` (unexported).
- `internal/nfo/nfo.go` — exported `WriteEpisode(ctx, path, *api.EpisodeInfo, contentID)` and `WriteTVShow(ctx, path, *api.SeriesInfo)`; private marshalers; package-level seam vars (`WriteEpisodeFn`/`WriteTVShowFn` OR equivalent).
- `internal/nfo/nfo_test.go` — escape-case tests (`&`, `<`, `>`, `"`, `'`, CJK, emoji) + uniqueid + omitempty + header assertions.
- `internal/api/artwork` is NOT touched by this plan (Plan 04).

### New / modified types (internal/api/types.go)
- `EpisodeMetadata` struct — NEW enriched fields appended (series-id field, slug/identifier, plot/description, duration, ratings) with confirmed CMS json tags + a comment block listing the confirmed field names. The series-id field is the single-episode-path source for `client.GetSeriesInfo` (Option A).
- `SeasonEpisode` struct — NEW series-id field appended (decoded from the EXISTING GetSeasonEpisodes response — no new network call for the id). This is the season-path source for `client.GetSeriesInfo` (Option A).
- `SeriesInfo` struct — NEW.
- `SeriesInfoResponse` struct — NEW.

### New method (internal/api/season.go)
- `(*Client).GetSeriesInfo(ctx, seriesId, audioLocale, subLocale) (*SeriesInfo, error)` — NOT omitted (D-07 LOCKED; the only omission case is the Task 1 escalation clause where the endpoint genuinely did not resolve, which requires escalating rather than regressing to title-only).

### New tests
- `internal/api/season_test.go::TestGetSeriesInfo` — NOT omitted (D-07 LOCKED).
- `internal/api/season_test.go::TestGetSeasonEpisodesDecodesSeriesID` (or analog) — asserts the enriched `SeasonEpisode` series-id field decodes (protects the Option-A wiring).
- `internal/download/episode_test.go::TestEpisodeWritesPerEpisodeNfoNonFatal`.
- `internal/download/episode_test.go::TestEpisodeWritesTvshowNfoOnSingleEpisodeFlow` — incl. the DEFAULT-path (rich SeriesInfo) and ERROR-path (title-only fallback) assertions.
- `internal/download/season_test.go::TestRunSeasonWritesTvshowNfoOnceBeforeEpisodeLoop` — incl. the DEFAULT-path (rich SeriesInfo via `episodes[0].SeriesID`) and os.Stat-skip assertions.
- `internal/download/season_test.go::TestRunSeasonTvshowNfoNonFatal` — incl. the title-only error-fallback assertion.

### Modified behavior (no new exported symbols)
- `download.Episode` — writes per-episode `.nfo` post-merge + `tvshow.nfo` at series root (single-episode path, GetSeriesInfo default-path via `info.EpisodeMetadata.SeriesID`, title-only error-fallback), all non-fatal. Signature unchanged (no new param; `baseContentID` already present).
- `download.runSeason` — pre-loop `tvshow.nfo` write (once-per-series, os.Stat-guarded) before the episode loop; GetSeriesInfo default-path via `episodes[0].SeriesID`; title-only error-fallback; non-fatal.
- `internal/api/testdata/api/episode-info-response.json` — fixture enriched with representative new fields incl. the series-id field.

### New seam vars
- `internal/download/episode.go` (lines 27-47 block): `episodeWriteNfo = nfo.WriteEpisode`, `episodeWriteTvshowNfo = nfo.WriteTVShow`, `episodeGetSeriesInfo` (indirection wrapping `client.GetSeriesInfo` for test stubbing).
- `internal/download/season.go`: `seriesGetSeriesInfo` (indirection wrapping `client.GetSeriesInfo`) + `seriesWriteTvshowNfo = nfo.WriteTVShow` (mirrors the `episodeDownloader` seam form).

### Files explicitly NOT modified by this plan
- `main.go` (no new flag; no dispatch change; NO seriesID threading — Option A sources the series-id from the enriched in-package structs `EpisodeMetadata.SeriesID` / `SeasonEpisode.SeriesID`; `download.Season`'s signature is unchanged).
- `internal/mux/mux.go` (Plan 01 owns the season_number fix; Plan 03 only consumes the corrected value).
- `internal/api/artwork.go` (Plan 04).
- `internal/config/config.go` (no `--no-artwork` flag per D discretion deferred idea).
</content>