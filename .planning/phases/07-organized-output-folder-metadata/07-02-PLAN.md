---
phase: 07-organized-output-folder-metadata
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/download/episode.go
  - internal/download/episode_test.go
autonomous: true
requirements:
  - OUT-01
  - OUT-02
must_haves:
  truths:
    - "Every download (single-episode OR season) lands under `Series Title/Season NN/Series Title S01E01 - Title.mkv` (per D-01: NO parenthetical year in the series root; per D-04: NO `[{quality}]` bracket in the filename)"
    - "The episode skip check (os.Stat the final outputFile) still works on the deeper Season NN path, preserving resumability keyed off the sanitized Series Title root directory (D-02)"
    - "`--output-dir` semantics preserved: the series root nests under outputDir when set (outputBase = filepath.Join(outputDir, cleanSeriesTitle))"
    - "Existing episode_test.go path assertions are updated for the nested layout and pass"
  artifacts:
    - "internal/download/episode.go output-path block (lines 62-86) produces a Season NN subfolder; os.MkdirAll on the season path; os.Stat skip on the deep outputFile"
    - "internal/download/episode_test.go contains a nested-layout assertion (Series Title/Season 01/Series Title S01E01 - Title.mkv)"
  key_links:
    - "episode.go (path-build) ↔ episode.go (os.Stat skip, D-02) use the SAME outputFile variable, so resumability keys off the deep path"
    - "episode.go outputBase computation ↔ --output-dir flag in main.go:32 (projects as prefix when set)"
---

<objective>
Reorganize downloaded output into the Jellyfin/Kodi-friendly nested folder layout per D-01, D-02, D-03, D-04 (OUT-01 + OUT-02).

Purpose: Today downloads land flat at `Series Title/Series Title S01E01 - Title [quality].mkv`. Jellyfin/Kodi expect `Series Title/Season 01/Series Title S01E01 - Title.mkv`. This plan produces that nested layout for BOTH single-episode and season flows (uniformity per D-03), removes the `[{quality}]` bracket from the filename (D-04), omits a parenthetical year from the series root (D-01 override of OUT-01's literal string), and preserves the v1.0 resumability skip (D-02) on the now-deeper path.

Output: Modified output-path construction in internal/download/episode.go (lines 62-86) plus updated/new path tests in internal/download/episode_test.go.
</objective>

<execution_context>
@/home/fmoura/Documents/Applications/crunchyroll-downloader/.opencode/gsd-core/workflows/execute-plan.md
@/home/fmoura/Documents/Applications/crunchyroll-downloader/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-organized-output-folder-metadata/07-CONTEXT.md
@.planning/phases/07-organized-output-folder-metadata/07-PATTERNS.md
@internal/download/episode.go
@internal/download/episode_test.go
@internal/api/types.go
@main.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the nested `Series Title/Season NN/` output path and drop the `[{quality}]` filename bracket (D-01, D-03, D-04)</name>
  <files>internal/download/episode.go</files>
  <read_first>
    - .planning/phases/07-organized-output-folder-metadata/07-PATTERNS.md §`internal/download/episode.go` (current path-build lines 62-86 and the D-03 nested-layout target shape)
    - .planning/phases/07-organized-output-folder-metadata/07-CONTEXT.md D-01 (NO parenthetical year in series root), D-03 (single-episode mirrors season layout), D-04 (drop `[{quality}]` bracket)
    - internal/download/episode.go (the full file; specifically lines 49-86 sanitizeFilename + path-build + skip-check; line 46 the `episodeMerge` seam var; line 340 the post-merge point — NOT modified by this plan)
    - internal/api/types.go (EpisodeMetadata.SeasonNumber field line 27, used in the new `Season %02d` folder name)
    - main.go line 32 (`--output-dir` flag) and lines 62-86 (outputDir flows into Episode unchanged)
  </read_first>
  <action>
    Per D-01/D-03/D-04, modify only the output-path construction block in the `Episode` function at internal/download/episode.go lines 62-86. Keep the existing two locals `cleanSeriesTitle` and `cleanEpisodeTitle` (lines 63-64) and the `outputBase` computation (lines 66-69, which already projects under outputDir when set) UNCHANGED. Then:

    1. After the existing `os.MkdirAll(outputBase, 0777)` success (line 71-73 — keep the error wrap `creating output directory: %w` verbatim), create the season subfolder: introduce `seasonDir := fmt.Sprintf("Season %02d", info.EpisodeMetadata.SeasonNumber)` then `seasonPath := filepath.Join(outputBase, seasonDir)` and `if err := os.MkdirAll(seasonPath, 0777); err != nil { return fmt.Errorf("creating season directory: %w", err) }`. The season folder name uses zero-padded two-digit form (`Season 01`, `Season 12`) matching Jellyfin/Kodi scan convention.

    2. Replace the `outputFile` construction at lines 75-81 so it (a) joins under `seasonPath` and (b) drops the `[%s]` quality bracket and the `*videoQuality` arg: `outputFile := filepath.Join(seasonPath, fmt.Sprintf("%s S%02dE%02d - %s.mkv", cleanSeriesTitle, info.EpisodeMetadata.SeasonNumber, info.EpisodeMetadata.EpisodeNumber, cleanEpisodeTitle))`.

    3. Leave the `os.Stat(outputFile)` skip check at lines 83-86 UNCHANGED — it now keys off the deeper path, which is exactly the D-02 resumability behavior (skip if the deep `.mkv` already exists). Do not change the `output.Global.Info(...skipped...)` message text.

    Do NOT remove the `*videoQuality` parameter from the `Episode` function signature (it still drives media selection upstream via `media.GetVideoBaseUrl` and is logged in `diag.DownloadLogger.Info("episode_start", ...)`) — it simply no longer appears in the on-disk filename. Do NOT modify the `episodeMerge` seam var, the post-merge block at line 343, `sanitizeFilename`, or anything below line 86 in this plan (Plan 03 adds per-episode NFO post-merge). Do NOT add a year anywhere (D-01 explicitly overrides OUT-01's literal `Series Title (Year)/`).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && go build ./internal/download/... && go vet ./internal/download/... && go test ./internal/download/... -run 'TestOutputDir|TestSanitize|TestEpisode' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '\[%s\]\|.mkv"' internal/download/episode.go` shows the filename format string is `"%s S%02dE%02d - %s.mkv"` with NO `[%s]` bracket and NO `*videoQuality` arg on that line.
    - `grep -n 'Season %02d' internal/download/episode.go` matches the new `seasonDir` literal.
    - `grep -n 'filepath.Join(seasonPath' internal/download/episode.go` matches the new outputFile construction (joins under the Season subfolder, not outputBase directly).
    - `grep -n 'os.MkdirAll(seasonPath' internal/download/episode.go` matches the season-path mkdir with the `creating season directory: %w` error wrap.
    - The `os.Stat(outputFile)` skip block (lines ~83-86 area) is unchanged in text; `grep -n 'skipped (already downloaded)' internal/download/episode.go` still matches.
    - No occurrence of `(Year)` or a year literal is introduced into the path construction (`grep -n 'Year' internal/download/episode.go` returns nothing in the modified block).
    - The `Episode` function signature is unchanged (`grep -n 'func Episode(' internal/download/episode.go` shows the same params, including `videoQuality *string`).
    - `go build ./internal/download/...` and `go vet ./internal/download/...` exit 0.
  </acceptance_criteria>
  <done>`go build` passes, the on-disk output path nests under `Series Title/Season NN/Series Title S01E01 - Title.mkv` with no year and no quality bracket, and the skip check keys off the deep path.</done>
</task>

<task type="auto">
  <name>Task 2: Update path tests for the nested layout + resumability on the deep path (D-02)</name>
  <files>internal/download/episode_test.go</files>
  <read_first>
    - .planning/phases/07-organized-output-folder-metadata/07-PATTERNS.md §`internal/download/episode.go` Test analog (the `TestOutputDirCreatesSeriesSubfolderInOutputDir` structural template at episode_test.go lines 262-294; `restoreEpisodeTestSeams` at lines 366-422; `TestSanitizeFilenameCollapsesMultiUnderscore` at lines 238-260)
    - .planning/phases/07-organized-output-folder-metadata/07-CONTEXT.md D-02 (resumability keys off sanitized Series Title root directory) and D-03 (single-episode mirrors season layout)
    - internal/download/episode_test.go (the existing path-output test at lines ~262-294 and the seam-restore helper; the stdlib table-driven pattern)
    - internal/download/episode.go (the modified path block from Task 1, so the test expectations match the new `Season %02d` folder and dropped quality bracket)
  </read_first>
  <action>
    In internal/download/episode_test.go, update the existing output-path test (the `TestOutputDirCreatesSeriesSubfolderInOutputDir` analog at lines ~262-294, or whichever existing test asserts the on-disk `.mkv` path) to reflect the nested layout:

    1. The expected directory becomes `filepath.Join(outputDir, sanitizeFilename("Test Series"), "Season 01")` (two-level nesting). The expected filename becomes `sanitizeFilename("Test Series") + " S01E01 - " + sanitizeFilename("Test Episode") + ".mkv"` with NO `[{quality}]` bracket. Update the `expectedDir` / `expectedFile` (or equivalent expected-path) assertions accordingly.

    2. Add a new test `TestEpisodeOutputSkipsAlreadyDownloadedInNestedLayout` that proves D-02 resumability still works on the deeper path: create the series-root + `Season 01` folders + the deep `.mkv` file at the exact path the new path-build produces, then invoke `Episode(...)` with the test seam-vars overridden so the actual download/mux is skipped (reuse the `restoreEpisodeTestSeams` pattern that stubs `episodeGetEpisode`, `episodeMerge`, etc.), and assert (a) the function returns nil with NO download/mux seam invoked, and (b) `output.Global` received a `...skipped (already downloaded)` info line. Use a stubbed Outputter captured to a buffer if the existing suite has one (mirror the established capture pattern in episode_test.go); if no capture seam exists, assert via side effect that `episodeMerge` was NOT called (the seam-call counter stays zero). The test must use an EpisodeInfo where SeasonNumber and EpisodeNumber produce `Season 01` and `S01E01`.

    3. Add a new test `TestEpisodeSingleEpisodeMirrorsSeasonLayout` (D-03) that invokes `Episode(...)` with `totalEpisodes=1` (the single-episode flow) and asserts the output path has the SAME `Series Title/Season 01/` nesting as a season episode would — i.e. the nesting is uniform regardless of `totalEpisodes`. Reuse the same seam-stub pattern so no real download runs; assert the produced outputFile path contains the `Season 01` segment.

    Do NOT introduce testify (v1.0 D-03: stdlib table-driven tests only). Reuse whatever `restoreEpisodeTestSeams` / inline `func` stub pattern the file already uses for `episodeMerge`, `episodeGetEpisode`, etc. If a new seam stub is needed (e.g. to count `episodeMerge` invocations), add a package-level counter guarded by `t.Cleanup` reset — mirror the existing seam-injection idioms in the file.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && go test ./internal/download/... -run 'TestOutputDir|TestEpisodeOutputSkips|TestEpisodeSingleEpisode|TestSanitize' -count=1 -v && go test ./internal/download/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/download/... -run 'TestOutputDirCreatesSeriesSubfolderInOutputDir|TestEpisodeOutputSkipsAlreadyDownloadedInNestedLayout|TestEpisodeSingleEpisodeMirrorsSeasonLayout' -count=1 -v` prints `--- PASS` for each (or, if the renamed existing test kept its name, the same family of path tests passes).
    - The modified existing-path test's expected directory literal contains `"Season 01"` (or via `fmt.Sprintf("Season %02d", ...)`) — verifiable by `grep -n 'Season 01\|Season %02d' internal/download/episode_test.go`.
    - The modified existing-path test's expected filename literal contains NO `[{quality}]` / `[1080p]` style bracket — `grep -n '\[1080p\]\|\[' internal/download/episode_test.go` shows no quality bracket in the expected-filename assertion (assertion may use other brackets only if unrelated to quality).
    - `TestEpisodeOutputSkipsAlreadyDownloadedInNestedLayout` creates the deep `.mkv` at `Series Title/Season 01/...` and asserts the merge seam is NOT invoked — the test body references `"Season 01"` and an `episodeMerge` stub counter (or equivalent skipped assertion).
    - `go test ./internal/download/... -count=1` exits 0 (full package green, including any existing segment/season tests that touch episode seams).
    - No new testify import: `grep -ni 'testify' internal/download/episode_test.go` returns no matches.
  </acceptance_criteria>
  <done>Nested-layout path tests pass, a targeted resumability-on-deep-path test proves the skip fires on `Series Title/Season NN/`, and a single-episode-mirrors-season test proves uniform nesting — full download package suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Crunchyroll series title → filesystem path | The series title (authenticated CMS data, but user-controllable in practice via crafted Crunchyroll catalog entries) flows into `sanitizeFilename` then an `os.MkdirAll` path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-02-T | Tampering | internal/download/episode.go path construction | medium | mitigate | Reuse `sanitizeFilename` (episode.go:49) for the series root and episode filename. It strips `\ / : * ? " < > | ' ’ \` “ ”`, collapses double underscores, trims trailing ` .` → no path separator or `..` survives through a sanitized title. The `Season NN` segment is generated from an `int` via `%02d`, so it cannot carry user input. `outputDir` itself is operator-supplied via `--output-dir` (already validated in main.go `validateOutputDir` Phase 1/3); not in this plan's scope. No new glob/wildcard cleanup introduced (Pitfalls Pitfall 6 cleanup warning respected). |

## Security Note
This plan does NOT add NFO or artwork. It only restructures the existing `.mkv` path. The `sanitizeFilename` reuse is the entire mitigation; no new code path accepts untrusted strings. Path traversal via crafted series title is mitigated by the existing strip-list covering `..`-relevant separators (`/`, `\`).
</threat_model>

<verification>
- `go build ./internal/download/...` and `go vet ./internal/download/...` exit 0.
- Full package suite: `go test ./internal/download/... -count=1` exits 0.
- On-disk layout provable by the new/updated tests: `Series Title/Season NN/Series Title S01E01 - Title.mkv`, no year, no quality bracket.
- Resumability: a pre-existing deep `.mkv` causes the skip path to fire (proven by `TestEpisodeOutputSkipsAlreadyDownloadedInNestedLayout`).
- `--output-dir` semantics: when `outputDir != ""` the series root nests under it (the `outputBase = filepath.Join(outputDir, cleanSeriesTitle)` line is unchanged; verified by the updated `TestOutputDirCreatesSeriesSubfolderInOutputDir` which asserts the `outputDir/` prefix on the deep path).
</verification>

<success_criteria>
- OUT-01 delivered: downloads land in `Series Title/Season 01/Series Title S01E01 - Title.mkv` (no `(Year)` per D-01; no `[{quality}]` per D-04).
- OUT-02 delivered: re-running an episode download skips because the deep `.mkv` already exists (resumability keys off the sanitized series-title root + deep path per D-02).
- D-03 delivered: single-episode downloads produce the SAME nested tree as season downloads (proven by `TestEpisodeSingleEpisodeMirrorsSeasonLayout`).
- Unblocks Plan 03 (NFO placement at series root + per-episode NFO beside the deep `.mkv`).
</success_criteria>

<output>
Create `.planning/phases/07-organized-output-folder-metadata/07-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (Plan 02)

- **Modified behavior:** `download.Episode` — output path now nests under `Series Title/Season NN/` and the `.mkv` filename drops the `[{quality}]` bracket. Public signature unchanged.
- **New tests (added to existing file):**
  - `TestEpisodeOutputSkipsAlreadyDownloadedInNestedLayout` (internal/download/episode_test.go)
  - `TestEpisodeSingleEpisodeMirrorsSeasonLayout` (internal/download/episode_test.go)
- **Modified test:** the existing `TestOutputDirCreatesSeriesSubfolderInOutputDir` analog — expected path/directory/filename literals updated for the nested layout.
- **No new exported functions, types, struct fields, or package paths.** (A `seasonDir`/`seasonPath` local pair is introduced inside `Episode` — not exported, not a new package symbol.)
</content>