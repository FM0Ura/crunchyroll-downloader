---
phase: 07-organized-output-folder-metadata
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/mux/mux.go
  - internal/mux/mux_test.go
autonomous: true
requirements:
  - OUT-03
must_haves:
  truths:
    - "FFmpeg `-metadata:g season_number=` arg uses api.EpisodeMetadata.SeasonNumber (git blame: Phase 7 D-05), not EpisodeNumber"
    - "A regression test asserts the season_number metadata arg equals the info.SeasonNumber value, distinct from EpisodeNumber"
    - "Existing mux_test.go tests pass unchanged"
  artifacts:
    - "internal/mux/mux.go line ~108 uses SeasonNumber for the season_number metadata arg"
    - "internal/mux/mux_test.go contains TestMergeEverythingSetsCorrectSeasonNumber"
  key_links:
    - "internal/mux/mux.go (metadata args) → internal/api/types.go EpisodeMetadata.SeasonNumber (already populated in both flows per internal/download/season.go:59 and internal/download/episode.go:77)"
---

<objective>
Fix the latent v1.0 `season_number=EpisodeNumber` bug at internal/mux/mux.go:108 (per D-05, OUT-03).

Purpose: The MKV container metadata currently stamps the *episode* number as the season number, producing visible season mismatches in Jellyfin/Kodi once Phase 7 emits NFO mirroring the file metadata. This must land before any NFO emission (Plan 03) so the NFO inherits the correct season number.

Output: A one-line correctness fix at mux.go:108 plus a focused regression test proving the season_number arg equals SeasonNumber (distinct from EpisodeNumber).
</objective>

<execution_context>
@/home/fmoura/Documents/Applications/crunchyroll-downloader/.opencode/gsd-core/workflows/execute-plan.md
@/home/fmoura/Documents/Applications/crunchyroll-downloader/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-organized-output-folder-metadata/07-CONTEXT.md
@.planning/phases/07-organized-output-folder-metadata/07-PATTERNS.md
@.planning/research/PITFALLS.md
@internal/mux/mux.go
@internal/mux/mux_test.go
@internal/api/types.go
@internal/download/episode.go
@internal/download/season.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix season_number bug at internal/mux/mux.go:108 and add regression test</name>
  <files>internal/mux/mux.go, internal/mux/mux_test.go</files>
  <read_first>
    - .planning/phases/07-organized-output-folder-metadata/07-PATTERNS.md §`internal/mux/mux.go` (the bug line + neighbors, D-05 fix, test analog `testEpisodeInfo`)
    - .planning/phases/07-organized-output-folder-metadata/07-CONTEXT.md (D-05: one-line fix at mux.go:108; SeasonNumber exists at internal/api/types.go:27 and is populated in both flows)
    - .planning/research/PITFALLS.md Pitfall 6 (the latent season_number=EpisodeNumber bug is called out explicitly; fix FILE-metadata correctness BEFORE folder metadata)
    - internal/mux/mux.go (current full file; the target arg at line ~108 and surrounding metadata block lines 104-110, stderr/cleanup lines 115-121)
    - internal/mux/mux_test.go (the `ffmpegCommand` seam override + `restoreFFmpegCommand` helper at lines 140-157; `testEpisodeInfo` fixture at lines 238-247; existing TestHelperProcess arg-capture pattern)
    - internal/api/types.go (EpisodeMetadata struct lines 24-31 — SeasonNumber field at line 27)
  </read_first>
  <action>
    Per D-05: in internal/mux/mux.go, change the `season_number=` metadata arg at line ~108 from `fmt.Sprintf("%v", info.EpisodeMetadata.EpisodeNumber)` to `fmt.Sprintf("%v", info.EpisodeMetadata.SeasonNumber)`. Keep the `fmt.Sprintf("%v", ...)` formatting wrapper unchanged; only the field reference changes. Do NOT touch the sibling args on lines ~105-107 (title/show/track) — `track` legitimately uses EpisodeNumber; `title` already uses SeasonNumber correctly. Do NOT touch the stderr/cleanup block at lines 115-121 or the `ffmpegCommand` seam var at line 21.

    Then add a regression test `TestMergeEverythingSetsCorrectSeasonNumber` to internal/mux/mux_test.go that: (1) builds an `*api.EpisodeInfo` via the existing `testEpisodeInfo()` helper, overridden so SeasonNumber and EpisodeNumber are DISTINCT (e.g. SeasonNumber=2, EpisodeNumber=7) — this separation is what makes the bug detectable; (2) uses the `restoreFFmpegCommand(t, "0", "")` seam so cmd.Run succeeds without invoking real ffmpeg; (3) invokes `MergeEverything(...)` with one minimal video MediaTrack pointing at a temp file (create via os.CreateTemp, path captured); (4) asserts no error returned; (5) re-invokes through the `GO_HELPER_*` arg-capture pattern already established by TestHelperProcess OR, if the existing tests use a different capture mechanism, follow whichever pattern `mux_test.go` already uses to inspect the assembled args — the assertion is that a captured `-metadata:g season_number=7` (old buggy value) is ABSENT and `-metadata:g season_number=2` (SeasonNumber-derived value) IS PRESENT. Do not duplicate positional-map logic from the existing tests; reuse any existing args-capture helper. Keep the test stdlib-only (no testify) per v1.0 D-03. Parallel tests (`t.Parallel()`) are encouraged only where the existing suite already opts in.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && go test ./internal/mux/... -run TestMergeEverythingSetsCorrectSeasonNumber -count=1 -v && go test ./internal/mux/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - internal/mux/mux.go the arg appended after `"track="+...` reads `"season_number="+fmt.Sprintf("%v", info.EpisodeMetadata.SeasonNumber)` (SeasonNumber field, not EpisodeNumber) — verified by `grep -n "season_number=" internal/mux/mux.go` showing SeasonNumber on that line.
    - `go vet ./internal/mux/...` exits 0.
    - `go test ./internal/mux/... -run TestMergeEverythingSetsCorrectSeasonNumber -count=1 -v` prints `--- PASS` for the new test.
    - `go test ./internal/mux/... -count=1` exits 0 (no regression to existing suite; the cleanup/stderr/memory tests remain green).
    - The new test uses SeasonNumber and EpisodeNumber values that differ (e.g. 2 and 7), so the assertion is meaningful — a grep of the new test body shows the literal `SeasonNumber:` and `EpisodeNumber:` set to distinct integers.
    - No change to the `track=` arg (still EpisodeNumber) and no change to the stderr/cleanup block (lines ~115-121 still remove partial output on FFmpeg failure).
  </acceptance_criteria>
  <done>internal/mux/mux.go stamps SeasonNumber into the season_number metadata arg, proven by a passing regression test using distinct Season/Episode numbers, with all pre-existing mux tests still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | No new trust boundary in this one-line fix; arg flows from already-populated api.EpisodeMetadata (Phase 6 hardening covers the api response pipeline) into an FFmpeg `-metadata:g` string. No new input parsing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | internal/mux/mux.go season_number arg | low | accept | Fix INCREASES metadata integrity (was wrong value); no new input. SeasonNumber populated from authenticated CMS response (Phase 6 hardened). |

## Security Note — Path/XML (carried to later plans)
This plan adds no path or XML construction. Path-traversal (crafted series title → folder) and NFO XML injection (`&`/`<` in titles) are addressed in Plans 02 and 03 respectively; `sanitizeFilename` (episode.go:49) mitigates folder creation, and `encoding/xml` (Plan 03) mitigates XML escaping per Pitfalls Pitfall 6.
</threat_model>

<verification>
- `go test ./internal/mux/... -count=1` exits 0 (whole mux package green).
- `grep -n "season_number=" internal/mux/mux.go` shows `info.EpisodeMetadata.SeasonNumber` on that line.
- No regressions to existing mux_test.go: run `go test ./internal/mux/... -run 'TestMergeEverything|TestCleanup|TestTrack' -count=1` if those names exist; the full-package run above covers it regardless.
</verification>

<success_criteria>
- OUT-03 delivered: MKV container metadata season_number equals SeasonNumber, not EpisodeNumber.
- Regression test `TestMergeEverythingSetsCorrectSeasonNumber` passes and would FAIL on the pre-fix line (proof by distinct Season/Episode values).
- Plumbing unblocked: Plan 03 can mirror this correct season_number into NFO without re-introducing the v1.0 bug (Pitfalls Pitfall 6).
</success_criteria>

<output>
Create `.planning/phases/07-organized-output-folder-metadata/07-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (Plan 01)

- **Modified symbol:** `mux.MergeEverything` — internal behavior change only (the `season_number=` arg value source flips from `EpisodeNumber` to `SeasonNumber`). Signature unchanged.
- **New tests:** `TestMergeEverythingSetsCorrectSeasonNumber` in internal/mux/mux_test.go.
- **No new functions, types, fields, or package paths.**
</content>