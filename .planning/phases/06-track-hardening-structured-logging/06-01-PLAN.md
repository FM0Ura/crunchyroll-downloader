---
phase: 06-track-hardening-structured-logging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/config/config.go
  - internal/config/config_test.go
autonomous: true
requirements:
  - LOG-01
  - ERR-01
  - ERR-02
  - ERR-03
must_haves:
  truths:
    - "config.json containing \"audio_lang\": \"ja-JP\" (single string) loads without error and yields Config.AudioLang == []string{\"ja-JP\"} (D-02 backward compat)"
    - "config.json containing \"audio_lang\": [\"ja-JP\",\"en-US\"] (array) loads and yields Config.AudioLang == []string{\"ja-JP\",\"en-US\"} (D-02 new schema)"
    - "config.json containing \"subs_lang\": \"en-US\" or [\"en-US\"] both load to the corresponding slice (D-02 symmetric)"
    - "Config struct has LogLevel *string `json:\"log_level,omitempty\"` and LogFile *string `json:\"log_file,omitempty\"` (D-19)"
    - "Merge overlays LogLevel and LogFile from a non-nil overlay; nil overlay fields preserve base (D-19 + Merge pattern)"
    - "WriteSkeleton writes \"log_level\": \"info\" and writes audio_lang/subs_lang as JSON arrays (D-02 + D-19 default)"
    - "isAllNilConfig semantics preserved: a fresh/empty config.json still triggers WriteSkeleton (AudioLang/SubsLang nil-slice test, new LogLevel/LogFile nil test added in 06-03)"
  artifacts:
    - "internal/config/config.go (modified: AudioLang/SubsLang become []string; LogLevel/LogFile *string added; Merge branches added; WriteSkeleton updated)"
    - "internal/config/config_test.go (extended: array-load cases, single-string-backward-compat cases, Merge overlay for LogLevel/LogFile, WriteSkeleton assertions)"
  key_links:
    - "Config.AudioLang / SubsLang []string feed primary-element-first semantics consumed by main.go parseLangs and download.Episode audioLangs[0] (D-01) in plan 06-04"
    - "Config.LogLevel / LogFile *string read by main.go resolveString precedence in plan 06-03 (D-19)"
---

<objective>
Migrate the config schema so language lists are arrays (D-02) and add the diagnostic-log pointer fields (D-19). This plan is the config-layer foundation that the new diagnostic logging (06-02/06-03) and the primary/secondary track logic (06-04) rely on.

Purpose: The D-01 "primary = first element" rule is only unambiguous if audio_lang/subs_lang are arrays. D-19 needs pointer fields so --log-level/--log-file resolve via the existing flag>env>config>default precedence.

Output: Modified internal/config/config.go and extended internal/config/config_test.go. No new files.
</objective>

<execution_context>
@/home/fmoura/Documents/Applications/crunchyroll-downloader/.opencode/gsd-core/workflows/execute-plan.md
@/home/fmoura/Documents/Applications/crunchyroll-downloader/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-track-hardening-structured-logging/06-CONTEXT.md
@.planning/phases/06-track-hardening-structured-logging/06-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Config struct (D-02 arrays + D-19 LogLevel/LogFile) and Merge/WriteSkeleton</name>
  <files>internal/config/config.go</files>
  <read_first>
    - internal/config/config.go (current Config struct lines 10-21, Load lines 43-57, WriteSkeleton lines 59-81, Merge lines 83-123 — the patterns being extended)
    - .planning/phases/06-track-hardening-structured-logging/06-PATTERNS.md §"`internal/config/config.go`" (lines 137-178) for exact target field shapes and Merge branch pattern
    - main.go lines 212-221 (isAllNilConfig) — confirm nil-slice equality semantics so no main.go edit is required by this plan
  </read_first>
  <action>
    Change the Config struct field `AudioLang` from `*string` to `[]string` keeping its `json:"audio_lang,omitempty"` tag; comment must explain "nil = absent from file; empty slice = explicit empty (D-02 array schema — first element is the primary track per D-01)". Apply the same array treatment to `SubsLang` (`[]string`, `json:"subs_lang,omitempty"`). Add two new pointer fields per D-19 exactly: `LogLevel *string \`json:"log_level,omitempty"\`` and `LogFile *string \`json:"log_file,omitempty"\``. Group comment must note these are diagnostic-log explicit-override fields consumed via resolveString in main.go.

    Add a custom unmarshal helper so the audio/subs fields accept BOTH legacy string form and the new array form (D-02 "tolerated or migrated with a warning"). Implement this with a small `unmarshalLangs(data []byte) ([]string, error)` helper or by giving the slice fields a named type with a `UnmarshalJSON` method; whichever is used, when a bare JSON string is encountered it MUST produce a single-element slice (e.g. "ja-JP" → ["ja-JP"]) AND emit a warning via `fmt.Fprintf(os.Stderr, "Warning: config audio_lang is a single string; migrating to array form. Update config.json to use an array.\n")`. Use `fmt.Fprintf(os.Stderr, ...)` for the warning (the diagnostic log does not exist yet at config-load time, and `fmt` and `os` are already imported — do NOT import `log/slog` or `internal/output` into this package to avoid import-cycle risk).

    Add two Merge branches in `Merge` mirroring the existing pointer-field pattern: `if overlay.LogLevel != nil { result.LogLevel = overlay.LogLevel }` and the symmetric `LogFile` branch. For the array fields, the existing slice-nil test `overlay.AudioLang != nil` (nil vs non-nil slice) already distinguishes absent from explicit-empty, so reuse it verbatim: `if overlay.AudioLang != nil { result.AudioLang = overlay.AudioLang }`.

    Update `WriteSkeleton`'s skeleton map so `audio_lang` and `subs_lang` are written as JSON arrays: `"audio_lang": []string{"ja-JP"}` and `"subs_lang": []string{"en-US"}`. Add `"log_level": "info"` to the skeleton map (D-19 default — do NOT add log_file here; the default path ./logs/animeheaven.log is a main.go-level default, not a config-file default).

    Do NOT modify main.go in this plan — isAllNilConfig's `cfg.AudioLang == nil` check still works because a nil []string compares equal to nil. Do NOT remove the pointer-style comment on the struct (update it to describe nil-slice semantics).
  </action>
  <verify>
    <automated>go test ./internal/config/... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - go build ./... exits 0 with the modified config.go
    - go test ./internal/config/... exits 0 (Task 2 tests added in same plan must pass)
    - `rg -c 'AudioLang\s+\[\]string' internal/config/config.go` is 1
    - `rg -c 'SubsLang\s+\[\]string' internal/config/config.go` is 1
    - `rg -c 'LogLevel\s+\*string' internal/config/config.go` is 1
    - `rg -c 'LogFile\s+\*string' internal/config/config.go` is 1
    - `rg -n 'overlay.LogLevel != nil' internal/config/config.go` shows a Merge branch adding LogLevel (D-19)
    - `rg -n 'overlay.AudioLang != nil' internal/config/config.go` still shows the AudioLang Merge branch (slice-nil overlay test preserved)
    - `rg -c '"log_level": "info"' internal/config/config.go` is 1 (WriteSkeleton default)
    - json.Unmarshal of `{"audio_lang":"ja-JP"}` produces AudioLang == []string{"ja-JP"} (asserted in Task 2 test)
    - json.Unmarshal of `{"audio_lang":["ja-JP","en-US"]}` produces AudioLang == []string{"ja-JP","en-US"} (asserted in Task 2 test)
    - The file does NOT import `log/slog` or `crunchyroll-downloader/internal/output`
  </acceptance_criteria>
  <done>Config struct migrated to array-typed audio/subs lang fields with backward-compatible single-string acceptance, LogLevel/LogFile pointer fields added, Merge and WriteSkeleton updated, package still compiles and existing config tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend config_test.go with array + LogLevel/LogFile table-driven cases</name>
  <files>internal/config/config_test.go</files>
  <read_first>
    - internal/config/config_test.go (whole file: existing TestLoadValidFile line 245, TestWriteSkeleton line 295, TestMergeOverlayFields line 373, TestMergeNilFieldsFallThrough line 422, TestMergeBothNil line 448 — extend/parallel these)
    - internal/config/config.go (Task-1 modified struct + Merge + WriteSkeleton)
  </read_first>
  <behavior>
    - Test 1 (D-02 new schema): Load of a config JSON with `"audio_lang":["ja-JP","en-US"]` and `"subs_lang":["en-US"]` yields Config.AudioLang == []string{"ja-JP","en-US"} and Config.SubsLang == []string{"en-US"}.
    - Test 2 (D-02 backward compat): Load of a config JSON with `"audio_lang":"ja-JP"` (bare string) yields Config.AudioLang == []string{"ja-JP"} and no error. Same for `"subs_lang":"en-US"`.
    - Test 3 (D-19 Merge overlay): Merge(base, overlay) where overlay.LogLevel points to "debug" and overlay.LogFile points to "/tmp/x.log" yields result.LogLevel == "debug" and result.LogFile == "/tmp/x.log"; nil LogLevel/LogFile overlay fields preserve the base values.
    - Test 4 (D-19 + D-02 WriteSkeleton): WriteSkeleton to a temp path writes a file whose bytes contain `"log_level": "info"`, `"audio_lang": [` and `"subs_lang": [` (array form markers).
    - Test 5 (D-02 explicit empty): A config JSON with `"audio_lang":[]` yields Config.AudioLang == []string{} (non-nil, length 0) — explicit-empty preserved through Merge.
  </behavior>
  <action>
    Add the four behaviors above as table-driven stdlib tests following the existing `TestLoadValidFile`/`TestMergeOverlayFields`/`TestWriteSkeleton` structures already in the file. For the backward-compat string-load case, write the config bytes to a t.TempDir file and call Load (NOT direct json.Unmarshal on the struct — exercise the actual custom-unmarshal path). Use the env-var isolation pattern from config_test.go lines 41-57 if any test temp-writes a config file in a shared path. Use `t.TempDir()` for WriteSkeleton output. Assert on slice equality with `reflect.DeepEqual` or `slices.Equal` (stdlib). For the bare-string-accepts warning, capture os.Stderr if needed or assert silently on the migrated value (the warning is best-effort, the migration is the contract).
  </action>
  <verify>
    <automated>go test ./internal/config/... -run 'TestLoad|TestMerge|TestWriteSkeleton' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - go test ./internal/config/... exits 0
    - `rg -c 'audio_lang.*ja-JP.*en-US' internal/config/config_test.go` ≥ 1 (array-load case present)
    - `rg -c 'log_level' internal/config/config_test.go` ≥ 1 (D-19 covered)
    - `rg -c 'slices.Equal|reflect.DeepEqual' internal/config/config_test.go` ≥ 1 (slice assertions, not pointer-deref)
    - No new test imports `log/slog` or `internal/output`
  </acceptance_criteria>
  <done>All five behaviors pass as table-driven stdlib tests; the package test suite is green; the D-02 schema migration and D-19 pointer fields are covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.json (disk) → Config struct | untrusted JSON crosses here; legacy single-string form must not panic unmarshal |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01-01 | Tampering | Config unmarshal of legacy single-string audio_lang | medium | mitigate | custom unmarshal accepts string OR array; test covers both forms |
| T-06-01-02 | Denial of Service | malformed audio_lang (e.g. number) passed to unmarshal | low | accept | json.Unmarshal already returns wrapped error via existing Load path; not a new vector |
| T-06-01-SC | Tampering | go.mod unchanged in this plan (no new dep) | low | accept | no package install in this plan |
</threat_model>

<verification>
- go build ./... exits 0
- go test ./internal/config/... exits 0
- go vet ./internal/config/... exits 0
- Config struct field shapes match D-02 (arrays) and D-19 (pointer fields)
</verification>

<success_criteria>
- Config.AudioLang and SubsLang are []string accepting both legacy string and new array JSON forms (D-02)
- LogLevel and LogFile are *string pointer fields with proper Merge overlay behavior (D-19)
- WriteSkeleton emits array form + log_level default
- All existing config tests stay green; new array/pointer tests added and green
</success_criteria>

<output>
Create `.planning/phases/06-track-hardening-structured-logging/06-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

This plan contributes:
- `internal/config/config.go` — modified: `AudioLang`/`SubsLang` become `[]string` (D-02); `LogLevel`/`LogFile` `*string` fields added (D-19); custom string→array unmarshal helper; Merge branches for LogLevel/LogFile; WriteSkeleton array form + log_level default.

Phase-wide new artifacts (created across plans 06-01..06-06):
- `internal/diag` package — `diag.go` (Global, Init, parseLevel, subsystem `*slog.Logger` vars) [06-02]
- `internal/diag/redact.go` — `redactingHandler` type, `redactKeys` map, redact-suppression (D-20/D-21/D-22) [06-02]
- `internal/diag/diag_test.go` — table-driven parseLevel + redaction tests [06-02]
- `gopkg.in/natefinch/lumberjack.v2` dependency in go.mod/go.sum [06-02]
- CLI flags `--log-level` / `--log-file` in main.go + `parseLogLevel` helper [06-03]
- `main_test.go` — TestResolveString precedence tests [06-03]
- Config struct fields: `LogLevel`/`LogFile` `*string`, array-typed `AudioLang`/`SubsLang` [this plan, 06-01]
- Hardened track handling in `internal/download/episode.go` (primary/secondary + partial marker) [06-04]
- `os.Stat` size-only validation in `internal/mux/mux.go` `MergeEverything` [06-05]
- Strategic slog instrumentation across download/drm/media/mux/api subsystems [06-06]
</content>