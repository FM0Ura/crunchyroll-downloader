---
phase: 06-track-hardening-structured-logging
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/diag/diag.go
  - internal/diag/redact.go
  - internal/diag/diag_test.go
  - go.mod
  - go.sum
autonomous: true
requirements:
  - LOG-01
  - LOG-02
  - LOG-03
  - LOG-04
  - LOG-05
must_haves:
  truths:
    - "diag.Global is *slog.Logger initialized to a no-op default before Init runs (mirrors output.Global default humanOutput{})"
    - "diag.Init(level slog.Level, path string) sets Global to a slog.Logger backed by slog.TextHandler writing through lumberjack rotation (D-13, D-14, D-16, D-23)"
    - "lumberjack.Writer uses Filename=path, MaxSize=10 (MB), MaxBackups=5, MaxAge=0, Compress=false (D-15)"
    - "diag.Init creates five subsystem child loggers via Global.WithGroup(\"download\")/\"drm\"/\"media\"/\"mux\"/\"api\" stored in exported package vars DownloadLogger, DrmLogger, MediaLogger, MuxLogger, ApiLogger (D-17, LOG-02)"
    - "The slog handler is a handlerWrapper wrapping slog.TextHandler; attrs with keys token, cookie, etp_rt, client_id, private_key, authorization have their values replaced with [REDACTED] (D-20, D-21, LOG-04)"
    - "device_id and URLs/content-ids are NOT redacted (D-21 explicit scope)"
    - "ParseLevel(\"debug\")→slog.LevelDebug, \"info\"→slog.LevelInfo, \"warn\"→slog.LevelWarn, \"error\"→slog.LevelError, unknown→slog.LevelInfo (enables the strategic-events-only default per D-18/LOG-05)"
    - "When path == \"\" the Init uses the default ./logs/animeheaven.log (D-14)"
    - "Redaction lives in the handler layer only — producers never redact, the handlerWrapper intercepts every record before write (D-22)"
  artifacts:
    - "internal/diag/diag.go (NEW: Global, Init, ParseLevel, subsystem *slog.Logger vars)"
    - "internal/diag/redact.go (NEW: handlerWrapper struct, redactKeys map, redactAttr ReplaceAttr callback)"
    - "internal/diag/diag_test.go (NEW: table-driven ParseLevel + redact membership + Init-creates-subsystem-logger tests)"
    - "go.mod / go.sum (modified: gopkg.in/natefinch/lumberjack.v2 added to require block)"
  key_links:
    - "diag.Global + subsystem *slog.Logger vars are consumed by main.go diag.Init call (06-03) and by strategic slog instrumentation (06-06)"
    - "The actual PII redaction seam is the redactAttr ReplaceAttr callback set on the inner TextHandler; handlerWrapper.Handle is a structural pass-through delegate to h.inner.Handle (it does not redact itself). Every subsystem child logger inherits redaction because all are children of Global whose inner handler carries the redactAttr callback (D-22)"
---

<objective>
Create the new `internal/diag` package: the structured diagnostic logging singleton, per-subsystem grouped loggers, lumberjack rotation, and the PII-redacting slog.Handler wrapper. This is the heart of Phase 6's logging deliverable (LOG-01..LOG-05).

Purpose: The diagnostic plane (D-13) is a SEPARATE plane from output.Global. Delivering this package in Wave 1 unblocks main.go wiring (06-03, Wave 2) and strategic-event instrumentation (06-06, Wave 3).

Output: New package files internal/diag/diag.go, internal/diag/redact.go, internal/diag/diag_test.go, and updated go.mod/go.sum with the lumberjack.v2 dependency.
</objective>

<execution_context>
@/home/fmoura/Documents/Applications/crunchyroll-downloader/.opencode/gsd-core/workflows/execute-plan.md
@/home/fmoura/Documents/Applications/crunchyroll-downloader/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-track-hardening-structured-logging/06-CONTEXT.md
@.planning/phases/06-track-hardening-structured-logging/06-PATTERNS.md
@internal/output/output.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add lumberjack.v2 dep + create diag.go (Global/Init/ParseLevel/subsystem loggers)</name>
  <files>go.mod, go.sum, internal/diag/diag.go</files>
  <read_first>
    - go.mod (current require blocks — place lumberjack.v2 alongside golang.org/x/term in the direct require block, NOT indirect)
    - internal/output/output.go lines 32-43 (Global + Outputter interface — the singleton+Init pattern to mirror) and lines 81-104 (Init-by-mode pattern)
    - .planning/phases/06-track-hardening-structured-logging/06-PATTERNS.md §"`internal/diag/diag.go`" (lines 28-90) for the exact Global/Init/subsystem-var pattern and the no-op default
    - .planning/phases/06-track-hardening-structured-logging/06-CONTEXT.md D-13..D-19 and D-23 (the logging-plane decisions)
  </read_first>
  <action>
    Add the dependency: run `go get gopkg.in/natefinch/lumberjack.v2@latest` so go.mod/go.sum are updated by the toolchain. Confirm the new entry lands in the direct `require` block (not `// indirect`).

    Create internal/diag/diag.go with package `diag`. Imports in project order (stdlib first, internal next if any, external last): `log/slog`, `os`, `path/filepath`, `fmt`, `gopkg.in/natefinch/lumberjack.v2`. Do NOT import `internal/output` (separate plane — D-13).

    Declare `var Global *slog.Logger` with a doc comment replicating the output.Global doc-comment style: "Global is the application-wide diagnostic logger. Set once in main() after flag parsing + config load — same pattern as internal/output. nil until Init is called; callers must nil-check before first use." Also declare the five subsystem package-level vars as a single var block: `DownloadLogger`, `DrmLogger`, `MediaLogger`, `MuxLogger`, `ApiLogger`, each `*slog.Logger`, with the group name recorded in the doc comment for each (download/drm/media/mux/api). All start nil until Init populates them.

    Implement `func Init(level slog.Level, path string)`. If path == "" set path to "./logs/animeheaven.log" (D-14). Build the writer: `w := &lumberjack.Logger{Filename: path, MaxSize: 10, MaxBackups: 5, MaxAge: 0, Compress: false}` (D-15 constants). Ensure the parent dir exists via `os.MkdirAll(filepath.Dir(path), 0755)` — ignore the error if the dir already exists (logging should still start); if MkdirAll fails, fall back to a no-op handler (do NOT return an error from Init — the diagnostic plane must never abort the app; emit an os.Stderr fmt.Fprintf warning and set Global to slog.New(slog.NewTextHandler(io.Discard, nil))). Build the base handler as `slog.NewTextHandler(w, &slog.HandlerOptions{Level: level, ReplaceAttr: redactAttr})` (D-23 TextHandler default; the ReplaceAttr hook performs the redaction seam). Wrap it: `h := handlerWrapper{inner: base}` (definition lives in redact.go — Task 2). Assign `Global = slog.New(h)` then assign the five subsystem vars: `DownloadLogger = Global.WithGroup("download")`, `DrmLogger = Global.WithGroup("drm")`, `MediaLogger = Global.WithGroup("media")`, `MuxLogger = Global.WithGroup("mux")`, `ApiLogger = Global.WithGroup("api")` (D-17, LOG-02). Ordering: Global MUST be non-nil before any WithGroup call.

    Implement `func ParseLevel(s string) slog.Level` lowercase-tolerant (strings.ToLower first — add `strings` import). Map: "debug"→slog.LevelDebug, "info"→slog.LevelInfo, "warn"→slog.LevelWarn, "error"→slog.LevelError, anything else (incl. "")→slog.LevelInfo (default strategic-events level per D-18/LOG-05). This is the helper main.go (06-03) will call (exported capital-P so callers in package main can reach it).

    Do NOT inline the handlerWrapper type here — it belongs in redact.go (Task 2). Leave a TODO-free forward reference: this file imports nothing from redact.go because both files share `package diag`.
  </action>
  <verify>
    <automated>test -f internal/diag/diag.go && rg -n 'func ParseLevel|slog.NewTextHandler|lumberjack' internal/diag/diag.go && rg -n 'gopkg.in/natefinch/lumberjack.v2' go.mod</automated>
  </verify>
  <acceptance_criteria>
    - `test -f internal/diag/diag.go` exits 0
    - `rg -c 'gopkg.in/natefinch/lumberjack.v2' go.mod` is 1
    - `rg -n 'var Global \*slog.Logger' internal/diag/diag.go` returns one match
    - `rg -n 'DownloadLogger\|DrmLogger\|MediaLogger\|MuxLogger\|ApiLogger' internal/diag/diag.go` returns five var lines
    - `rg -c 'MaxSize:\s*10' internal/diag/diag.go` is 1 (D-15)
    - `rg -c 'MaxBackups:\s*5' internal/diag/diag.go` is 1 (D-15)
    - `rg -c 'MaxAge:\s*0' internal/diag/diag.go` is 1 (D-15)
    - `rg -c 'Compress:\s*false' internal/diag/diag.go` is 1 (D-15)
    - `rg -n 'WithGroup\("download"\)\|WithGroup\("drm"\)\|WithGroup\("media"\)\|WithGroup\("mux"\)\|WithGroup\("api"\)' internal/diag/diag.go` returns five matches (D-17)
    - `rg -n 'func ParseLevel' internal/diag/diag.go` returns one match
    - `rg -c 'slog.NewTextHandler' internal/diag/diag.go` is 1 (D-23 TextHandler default)
    - The file does NOT import `crunchyroll-downloader/internal/output`
    - The file does NOT contain a hard-coded "[REDACTED]" literal (lives in redact.go)
  </acceptance_criteria>
  <done>Global + five subsystem loggers + ParseLevel are declared in diag.go, lumberjack.v2 is a direct require in go.mod, and Init wires Global through lumberjack + handlerWrapper + subsystem WithGroup vars; Task 2 performs the full package build after handlerWrapper exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create redact.go (handlerWrapper + redactKeys + redactAttr) and diag_test.go</name>
  <files>internal/diag/redact.go, internal/diag/diag_test.go</files>
  <read_first>
    - internal/output/output.go lines 106-180 (the humanOutput/jsonOutput/quietOutput concrete-struct-wrapping-sink pattern that the handlerWrapper struct-wrapping-style mirrors)
    - .planning/phases/06-track-hardening-structured-logging/06-PATTERNS.md §"`internal/diag/redact.go`" (lines 94-133) for the exact redactKeys set and the handler-wrapping pattern
    - .planning/phases/06-track-hardening-structured-logging/06-PATTERNS.md §"`internal/diag/diag_test.go`" (lines 465-519) for the t.Cleanup restore-seam + env-var isolation + t.TempDir log-file-read-back patterns
    - internal/mux/mux_test.go lines 78-95 (restoreFFmpegCommand t.Cleanup seam) and internal/config/config_test.go lines 41-57 (env-var isolation)
    - internal/diag/diag.go (Task 1 output — Init uses ReplaceAttr: redactAttr)
  </read_first>
  <behavior>
    - Test ParseLevel: table-driven — {"debug"→LevelDebug, "info"→LevelInfo, "warn"→LevelWarn, "error"→LevelError, "DEBUG"→LevelDebug (case-insensitive), "xyz"→LevelInfo, ""→LevelInfo}.
    - Test redactKeys membership: key "token" IS in redactKeys; "device_id" is NOT; "authorization" IS; "url" is NOT; "etp_rt" IS; "client_id" IS; "private_key" IS; "cookie" IS.
    - Test Init wiring: after a t.Cleanup-restored call to diag.Init(slog.LevelDebug, <t.TempDir>/test.log), Global is non-nil and all five subsystem loggers (DownloadLogger/DrmLogger/MediaLogger/MuxLogger/ApiLogger) are non-nil.
    - Test redaction end-to-end (LOG-04): a record emitted as `Global.With("token", "supersecret").Info("x")` and as `Global.With("device_id", "uuid-123").Info("y")` is written to the t.TempDir log file; the file content contains the literal [REDACTED] and the substring "device_id=uuid-123" but does NOT contain the substring "supersecret".
  </behavior>
  <action>
    Create internal/diag/redact.go with `package diag`. Imports: `context`, `log/slog` only.

    Declare the redaction key set as a package-level var: `var redactKeys = map[string]struct{}{"token": {}, "cookie": {}, "etp_rt": {}, "client_id": {}, "private_key": {}, "authorization": {}}` (D-21 exact six keys). Add a doc comment: "Redacts the six D-21 PII fields only. device_id and URLs/content-ids are NOT redacted."

    Implement `func redactAttr(groups []string, a slog.Attr) slog.Attr` to be passed as `slog.HandlerOptions.ReplaceAttr`: if `_, ok := redactKeys[a.Key]; ok` return `slog.String(a.Key, "[REDACTED]")`, else return `a` unchanged. Keep this function unexported (the redaction seam is internal — D-22).

    Declare `type handlerWrapper struct{ inner slog.Handler }` — a concrete struct wrapping a base handler (mirror the humanOutput concrete-struct style from output.go). Implement the four slog.Handler methods so this is itself an slog.Handler: `Handle(ctx context.Context, r slog.Record) error` walks r.Attrs through redact rules (use r.Attrs callback which provides each attr; clone the record via `r.Clone()` and rebuild attr list, OR rely on the base handler's already-applied ReplaceAttr — since we ALSO pass `redactAttr` as ReplaceAttr to the base TextHandler, the redaction is applied at the base layer; the handlerWrapper is a structural seam for FUTURE per-handler upgrades, but in the v1.1 wiring redaction happens via the ReplaceAttr callback of the inner TextHandler). Concretely: have `Handle` delegate to `h.inner.Handle(ctx, r)` directly (the ReplaceAttr callback on the inner TextHandler already redacts). Implement `WithAttrs(attrs []slog.Attr) slog.Handler` returning `handlerWrapper{h.inner.WithAttrs(attrs)}`; `WithGroup(name string) slog.Handler` returning `handlerWrapper{h.inner.WithGroup(name)}`; `Enabled(ctx, l slog.Level) bool` delegating to `h.inner.Enabled(ctx, l)`. Document in the redact.go header comment that the actual redaction in v1.1 is performed by the `redactAttr` callback set as `slog.HandlerOptions.ReplaceAttr` on the base TextHandler (D-22 — single seam), and the `handlerWrapper` struct preserves the wrapping seam for future format-change (e.g. JSON) without losing redaction.

    Create internal/diag/diag_test.go with `package diag` and stdlib-only `testing`/`os`/`strings`/`log/slog`/`path/filepath`/`io` imports as needed. Use the env-var isolation pattern (config_test.go:41-57) to unset `CRUNCHYROLL_LOG_LEVEL` and `CRUNCHYROLL_LOG_FILE` before Init tests. Use a `t.Cleanup` restore-seam that saves the original `Global` and five subsystem `*slog.Logger` vars and restores them after each test (mirrors restoreFFmpegCommand in mux_test.go:78-95), because `diag.Init` mutates package globals. Use `t.TempDir()` for the log-file path and read the file back with `os.ReadFile` to assert redaction substrings. Cover the four behaviors above with table-driven stdlib tests.

    NOTE: do NOT name a test `TestHelperProcess` (that name is reserved by mux_test.go's helper-process seam convention; collision-free naming required). Use names like `TestParseLevel`, `TestRedactKeys`, `TestInitWiresSubsystemLoggers`, `TestRedactionEndToEnd`.
  </action>
  <verify>
    <automated>go test ./internal/diag/... -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./internal/diag/...` exits 0
    - `go test ./internal/diag/...` exits 0
    - `rg -c 'redactKeys' internal/diag/redact.go` is 1 (the map var)
    - `rg -c '"token".*"cookie".*"etp_rt".*"client_id".*"private_key".*"authorization"' internal/diag/redact.go` ≥ 1 (all six D-21 keys present)
    - `rg -n 'device_id' internal/diag/redact.go` returns NO match inside the redactKeys map literal (D-21 explicit exclusion — present only in a doc comment is acceptable, present as a map key is forbidden)
    - `rg -n 'func redactAttr' internal/diag/redact.go` returns one match
    - `rg -n 'type handlerWrapper struct' internal/diag/redact.go` returns one match
    - `rg -c 'WithGroup\|WithAttrs\|Enabled\|Handle' internal/diag/redact.go` ≥ 4 (slog.Handler tetrad implemented)
    - `rg -c 'TestParseLevel\|TestRedactKeys\|TestInitWiresSubsystemLoggers\|TestRedactionEndToEnd' internal/diag/diag_test.go` ≥ 4
    - `rg -c 't\.Cleanup' internal/diag/diag_test.go` ≥ 1 (restore-seam pattern)
    - The diag_test.go redaction end-to-end test asserts the log file content contains the [REDACTED] literal and does NOT contain the secret value "supersecret" (assert via `strings.Contains` + negated `!strings.Contains`)
  </acceptance_criteria>
  <done>redact.go provides the single redaction seam (redactAttr via ReplaceAttr on the base TextHandler) with the six D-21 keys; handlerWrapper implements the slog.Handler tetrad as a structural wrapping seam; diag_test.go proves ParseLevel, redactKeys membership, Init wiring of five subsystem loggers, and end-to-end redaction of bearer-style secret values.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| slog producer → log file on disk | any PII attr (token/cookie/etp_rt/client_id/private_key/authorization) crosses this boundary and MUST be redacted before write |
| go get lumberjack.v2 → go.mod | supply-chain insertion of a new third-party dependency |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02-01 | Information Disclosure | slog producers emitting token/cookie/etp_rt attrs | critical | mitigate | redactAttr ReplaceAttr callback on the base TextHandler replaces the six D-21 keys' values with [REDACTED]; end-to-end test asserts secret absent from log file |
| T-06-02-02 | Information Disclosure | device_id, URLs, content-ids logged unredacted | low | accept | D-21 explicit exclusion — UUID generated by tool, not user PII; accept per CONTEXT.md |
| T-06-02-03 | Denial of Service | log dir not writable; Init panic | medium | mitigate | Init ignores MkdirAll error, falls back to io.Discard handler + os.Stderr warning; diagnostic plane never aborts the app |
| T-06-02-04 | Tampering | lumberjack.v2 supply-chain risk | high | mitigate | go get pins a version; the package is the de facto Go log-rotation std (per D-16 adopted decision); review the resolved version on pkg.go.dev before commit |
| T-06-02-SC | Tampering | npm/pip/cargo install bombing | low | accept | go modules are not in the npm/pip/cargo slopcheck gate; gopkg.in/natefinch/lumberjack.v2 is the explicit D-16 user decision with a known maintainer |
</threat_model>

<verification>
- go build ./... exits 0
- go test ./internal/diag/... exits 0
- go vet ./internal/diag/... exits 0
- go.mod directly requires gopkg.in/natefinch/lumberjack.v2
- End-to-end redaction test writes a synthetic secret token attr and asserts [REDACTED] appears in the log file content while the secret value does not
</verification>

<success_criteria>
- internal/diag package compiles and is green-tested
- diag.Global + five subsystem *slog.Logger vars are exported and nil-checked-default-safe
- diag.Init wires TextHandler → lumberjack rotation at D-15 constants; default path ./logs/animeheaven.log (D-14)
- redaction seam redacts exactly the six D-21 keys via the handler layer (D-22), never the producer layer
- ParseLevel maps the four named levels + unknown→Info (LOG-05 strategic default)
</success_criteria>

<output>
Create `.planning/phases/06-track-hardening-structured-logging/06-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

This plan contributes (the novel Phase-6 logging core):
- `internal/diag/diag.go` — `Global *slog.Logger`, `Init(level slog.Level, path string)`, exported `ParseLevel(s string) slog.Level`, subsystem `*slog.Logger` vars `DownloadLogger`/`DrmLogger`/`MediaLogger`/`MuxLogger`/`ApiLogger` (D-17), lumberjack rotation seams (D-15), default path ./logs/animeheaven.log (D-14)
- `internal/diag/redact.go` — `handlerWrapper` struct implementing slog.Handler tetrad, `redactKeys` map (six D-21 keys), `redactAttr` ReplaceAttr callback
- `internal/diag/diag_test.go` — table-driven `TestParseLevel`, `TestRedactKeys`, `TestInitWiresSubsystemLoggers`, `TestRedactionEndToEnd`
- `go.mod`/`go.sum` — `gopkg.in/natefinch/lumberjack.v2` added to direct require block

Phase-wide new artifacts (across plans 06-01..06-06) listed in 06-01-PLAN.md §"Artifacts this phase produces".
</content>
