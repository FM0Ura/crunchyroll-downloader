---
phase: 06-track-hardening-structured-logging
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/download/episode.go
  - internal/download/episode_test.go
autonomous: true
requirements:
  - ERR-01
  - ERR-02
  - ERR-03
must_haves:
  truths:
    - "When audioLangs[0] (i==0, the primary) is not in guidByLocale, Episode returns a fmt.Errorf naming the locale as primary (D-03 hard error)"
    - "When a non-primary audioLangs[i>0] is not in guidByLocale, Episode emits output.Global.Warn and continues; the locale is appended to skippedTracks (D-04 + D-08 NDJSON reuse via existing warn event)"
    - "When len(versions)==0 after the audio loop (e.g. ALL primaries missing), Episode returns a fmt.Errorf hard error so no empty MKV is produced (ERR-03)"
    - "When subsLangs[0] (the primary subtitle) is nil in firstEpisode.Subtitles, Episode returns a fmt.Errorf naming the subtitle locale as primary (D-05 hard error)"
    - "When a non-primary subsLangs[i>0] is nil in firstEpisode.Subtitles, Episode emits output.Global.Warn and continues (D-05 + D-08)"
    - "When media.DownloadSubs fails for any requested subtitle (present but infra-failed), Episode returns a fmt.Errorf wrapping the download err with %w (D-06 hard error — kept)"
    - "A skippedTracks []string accumulator collects every skipped audio/subtitle locale string during the episode; after the success line, if len(skippedTracks)>0, Episode emits a separate conditional warn line of the form '⚠ Episode %d downloaded partially: %d track(s) skipped (%s)' (D-07)"
    - "Skip emission uses output.Global.Warn only — no new NDJSON event type, no new event field (D-08 forbids contract change; the existing warn event at internal/output/ndjson.go is reused unchanged)"
    - "Human-mode skip rendering reuses the existing output.Global.Warn yellow ⚠ format with no new visual style (D-09)"
  artifacts:
    - "internal/download/episode.go (modified: audio locale loop primary/secondary split, subtitle presence/download split, skippedTracks accumulator + partial-warn line)"
    - "internal/download/episode_test.go (extended: existing TestEpisodeReturnsErrorForUnavailableAudioLocale updated to the new primary-error message; new cases for secondary-skip, all-audio-missing hard error, partial-episode mark)"
  key_links:
    - "audioLangs[0]/subsLangs[0] primary semantics come from D-01 (first element of the language list — config array form is provided by 06-01; CLI []string form already produced by main.go parseLangs)"
    - "Skip events flow through output.Global.Warn which already emits the existing Type:\"warn\" NDJSON event in --json mode (D-08 — internal/output/ndjson.go is reused, NOT modified)"
---

<objective>
Replace the all-or-nothing missing-track error handling in download.Episode with the D-01..D-09 primary/secondary rules: missing primary = hard error, missing secondary = warn+skip, present-but-download-failed = hard error, and partial episodes are marked with a separate warn line.

Purpose: Deliverables A of Phase 6 — graceful missing-track handling so partial audio/subtitle dubs no longer abort episodes (ERR-01, ERR-02, ERR-03).

Output: Modified internal/download/episode.go and extended internal/download/episode_test.go.
</objective>

<execution_context>
@/home/fmoura/Documents/Applications/crunchyroll-downloader/.opencode/gsd-core/workflows/execute-plan.md
@/home/fmoura/Documents/Applications/crunchyroll-downloader/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-track-hardening-structured-logging/06-CONTEXT.md
@.planning/phases/06-track-hardening-structured-logging/06-PATTERNS.md
@internal/output/ndjson.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audio locale loop — primary hard error / secondary warn+skip (D-01, D-03, D-04, D-08, ERR-03)</name>
  <files>internal/download/episode.go</files>
  <read_first>
    - internal/download/episode.go lines 64-100 (guidByLocale map build, "all" expansion, and the audio locale loop at 94-100 — the exact lines to rewrite)
    - .planning/phases/06-track-hardening-structured-logging/06-PATTERNS.md §"`internal/download/episode.go`" (lines 245-321) for the D-03/D-04 audio-loop rewrite and the error-formatting convention
    - .planning/phases/06-track-hardening-structured-logging/06-CONTEXT.md D-01..D-09 (the missing-track failure rules being implemented)
  </read_first>
  <action>
    Rename the `versions` loop index to `(i, locale)` so the primary element (i==0) is detectable. Inside the loop, when `!ok` (locale missing from guidByLocale):
      - If `i == 0`: return `fmt.Errorf("primary audio locale %s not available for episode %d", locale, info.EpisodeMetadata.EpisodeNumber)` (D-03 hard error; note "primary" in the message and %d integer formatting consistent with the file).
      - Else (i>0): call `output.Global.Warn("Skipping %s audio: not available for episode %d", locale, info.EpisodeMetadata.EpisodeNumber)` (D-04 soft skip + D-09 human warn format) and `continue` WITHOUT appending to versions. Append the locale label (e.g. `locale + " dub"`) to a `skippedTracks []string` accumulator declared in scope just before the loop (this accumulator is consumed by Task 3's D-07 partial-warn line).
    After the loop, add a guard: `if len(versions) == 0 { return fmt.Errorf("no audio tracks available for episode %d", info.EpisodeMetadata.EpisodeNumber) }` (ERR-03 — no silent empty MKV; fires when the primary was the only requested locale and it was missing, or every requested locale was a secondary that got skipped).

    Preserve the existing `audioVersion` struct type and the `versions = append(versions, audioVersion{locale: locale, contentId: guid})` happy-path append. Preserve the `firstEpisode, err := client.GetEpisode(ctx, versions[0].contentId)` at line 123 — it still relies on versions[0] being valid (now guaranteed either by the happy path or by the early hard-error return).

    Do NOT use slog / diag.*Logger calls in this task — strategic slog instrumentation belongs to plan 06-06 (Wave 3). Do NOT change the error-wrapping style: keep `fmt.Errorf("PLAIN CONTEXT %s", value)` for new hard errors (no %w because there is no wrapped source err for the absence case).
  </action>
  <verify>
    <automated>go build ./internal/download/... && go vet ./internal/download/...</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./internal/download/...` exits 0
    - `rg -n 'i == 0' internal/download/episode.go` returns a match inside the audio locale loop (primary hard-error branch)
    - `rg -n 'primary audio locale' internal/download/episode.go` returns one match (D-03 message)
    - `rg -n 'Skipping %s audio' internal/download/episode.go` returns one match (D-04 warn message)
    - `rg -n 'skippedTracks = append' internal/download/episode.go` returns at least one match (accumulator set up)
    - `rg -n 'no audio tracks available' internal/download/episode.go` returns one match (ERR-03 hard-error guard)
    - The old single hard-error line `return fmt.Errorf("audio locale %s is not available for episode %v", locale, info.EpisodeMetadata.EpisodeNumber)` is NO LONGER present (run `rg -c 'audio locale %s is not available' internal/download/episode.go` → expect 0)
  </acceptance_criteria>
  <done>Audio locale loop implements primary-hard-error / secondary-warn-skip; the skippedTracks accumulator is populated for secondary skips; an empty-versions post-loop guard returns the ERR-03 hard error; the module builds.</done>
</task>

<task type="auto">
  <name>Task 2: Subtitle loop — presence vs download split + partial-episode warn (D-05, D-06, D-07, D-08)</name>
  <files>internal/download/episode.go</files>
  <read_first>
    - internal/download/episode.go lines 141-160 (subtitle presence-check loop and subtitle download loop — the exact lines to rewrite) and lines 296-313 (mux call + success line at 307 + the completed=true flag at 299)
    - internal/output/ndjson.go (the existing `warn` event shape — DO NOT modify; output.Global.Warn already emits it)
    - .planning/phases/06-track-hardening-structured-logging/06-CONTEXT.md D-05/D-06/D-07 (symmetric subtitle rules + partial marker format)
    - .planning/phases/06-track-hardening-structured-logging/06-PATTERNS.md §"`internal/download/episode.go`" subtitle section (lines 293-321)
  </read_first>
  <action>
    Rewrite the subtitle presence-check loop at lines 141-145 into an index-aware form: iterate `(j, locale)` over subsLangs; when `firstEpisode.Subtitles[locale] == nil`: if `j == 0` return `fmt.Errorf("primary subtitle locale %s not available for episode %d", locale, info.EpisodeMetadata.EpisodeNumber)` (D-05 hard error for primary); else `output.Global.Warn("Skipping %s subtitles: not available for episode %d", locale, info.EpisodeMetadata.EpisodeNumber)` (D-05 + D-09 secondary skip) and append `locale + " sub"` to `skippedTracks`. Then `continue` is not possible because the presence loop and download loop are currently separate — consolidate them into ONE loop iterating subsLangs where each iteration does presence-check THEN (if present) downloads. Skip the download step for the secondary-missing locales by using a `continue` after the warn+append. Keep the download failure path returning `fmt.Errorf("downloading subtitles for %s: %w", locale, err)` unchanged (D-06 — present-but-failed stays a hard error).

    After the existing success line at line 307 (the `output.Global.Info("%s[Episode %d/%d] %s ... %s%s %s%s %s", ...)` with the green ✓ marker), insert a conditional partial warn: `if len(skippedTracks) > 0 { output.Global.Warn("⚠ Episode %d downloaded partially: %d track(s) skipped (%s)", info.EpisodeMetadata.EpisodeNumber, len(skippedTracks), strings.Join(skippedTracks, ", ")) }` (D-07 — note "partially" in the message; the ⚠ is rendered by the output.Global.Warn human impl, do NOT duplicate the ⚠ glyph in the format string because output.Global.Warn already prepends it — pass just "Episode %d downloaded partially: %d track(s) skipped (%s)"). The success line at 307 is unchanged (D-07 keeps the green ✓ line, the partial warn is a SEPARATE line after it).

    The `completed = true` at line 299 stays — a partial episode (with skipped secondaries) is still a completed success; only a hard-error return leaves completed=false and triggers the deferred cleanup.

    Do NOT add slog / diag.*Logger calls. Do NOT modify internal/output/ndjson.go (D-08 forbids contract change).
  </action>
  <verify>
    <automated>go build ./internal/download/... && go vet ./internal/download/...</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./internal/download/...` exits 0
    - `rg -n 'primary subtitle locale' internal/download/episode.go` returns one match (D-05)
    - `rg -n 'Skipping %s subtitles' internal/download/episode.go` returns one match (D-05 secondary skip)
    - `rg -n 'downloading subtitles for %s: %w' internal/download/episode.go` returns one match (D-06 download-failure hard error preserved)
    - `rg -n 'downloaded partially' internal/download/episode.go` returns one match (D-07 partial marker)
    - `rg -n 'strings.Join(skippedTracks' internal/download/episode.go` returns one match (D-07 format join)
    - `internal/output/ndjson.go` is UNCHANGED (verify via `git diff --name-only` listing internal/output/ndjson.go absent)
    - The subtitle presence-check and download are consolidated into a single loop (presence + download in the same iteration, not two separate loops)
  </acceptance_criteria>
  <done>Subtitle loop splits primary hard-error from secondary soft-skip, preserves the D-06 download-failure hard error, populates skippedTracks for both audio and subtitle secondary skips, and emits the D-07 partial-episode warn as a separate line after the success line; the NDJSON contract is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: episode_test.go — update + add primary/secondary/all-missing/partial table-driven cases</name>
  <files>internal/download/episode_test.go</files>
  <read_first>
    - internal/download/episode_test.go (whole file — TestEpisodeReturnsErrorForUnavailableAudioLocale lines 14-36 currently asserts substring "audio locale en-US is not available" which will FAIL after Task 1's message change to "primary audio locale"; must update)
    - internal/download/episode_test.go lines 53-115 (TestEpisodeSingleVersion, TestEpisodeParallelAudio — the api.Client seam + EpisodeInfo construction patterns to mirror)
    - internal/mux/mux_test.go lines 78-95 (t.Cleanup restore seam and writeTempFile)
  </read_first>
  <behavior>
    - Test 1 (updated TestEpisodeReturnsErrorForUnavailableAudioLocale — D-03 primary hard error): Episode called with audioLangs=["en-US"] (single = primary) on an EpisodeInfo whose AudioLocale is "ja-JP" and no Versions → returns non-nil error containing substring "primary audio locale en-US not available" (the OLD assertion substring "audio locale en-US is not available" is removed/replaced).
    - Test 2 (D-04 secondary warn+skip): Episode with audioLangs=["ja-JP","en-US"] where only "ja-JP" is in guidByLocale → no error, episode proceeds (use a fake api.Client that supplies valid manifest/segments for ja-JP); assert a warn line containing "Skipping en-US audio" appeared (capture via a test stub of output.Global OR via the existing pattern in episode_test.go — if no seam exists, assert via the success-complete outcome: episode did not error AND output fd captured contains "Skipping en-US").
    - Test 3 (ERR-03 all audio missing hard error): Episode with audioLangs=["en-US","es-419"] where neither is in guidByLocale → returns non-nil error containing "primary audio locale en-US not available" (the i==0 guard fires first; OR if both are secondaries of a non-existent primary, the post-loop "no audio tracks available" guard fires — pick the scenario that exercises the post-loop guard: audioLangs=["ja-JP","en-US"] with guidByLocale empty → i==0 fires first; to exercise the post-loop guard use audioLangs where the primary is present but is then removed by the loop — simplest is the i==0 case).
    - Test 4 (D-07 partial marker): Episode with audioLangs=["ja-JP","en-US"] and subsLangs=["en-US","es-419"] where en-US sub exists but es-419 sub does not → episode completes; assert a warn line containing "downloaded partially" and "es-419 sub" appeared alongside the success ✓ line.
  </behavior>
  <action>
    Update the existing `TestEpisodeReturnsErrorForUnavailableAudioLocale` (lines 14-36) assertion substring from "audio locale en-US is not available" to "primary audio locale en-US not available" (Task 1 changes this message). Add the three new test cases above as additional table-driven funcs following the existing seam style in the file (EpisodeInfo literal + the existing api.Client fake); if a test needs to capture output.Global.Warn output, either build a minimal fake `output.Outputter` and swap `output.Global` via a `t.Cleanup` restore-seam (mirroring restoreFFmpegCommand in mux_test.go:78-95), OR use the same approach the existing file uses to observe Warn side-effects. Use `t.TempDir()` for output paths. Keep stdlib-only `testing`/`strings`/`os`/`context` imports — no new external deps.

    If the existing episode_test.go fake api.Client does not expose a "Subtitles map keyed by locale" knob, extend the existing test seam minimally (do NOT add a new fake type — extend the existing one) to control which subtitle locales are present.
  </action>
  <verify>
    <automated>go test ./internal/download/... -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/download/...` exits 0
    - `rg -c 'primary audio locale en-US not available' internal/download/episode_test.go` is 1 (updated assertion)
    - `rg -c 'downloaded partially' internal/download/episode_test.go` is 1 (D-07 test present)
    - `rg -c 'Skipping en-US audio' internal/download/episode_test.go` ≥ 1 (D-04 secondary-skip test)
    - The OLD assertion substring "audio locale en-US is not available" is NO LONGER present — `rg -c 'audio locale en-US is not available' internal/download/episode_test.go` → 0
    - All tests stay green; no test imports `log/slog` or `internal/diag` (slog is Wave 3's 06-06 concern)
  </acceptance_criteria>
  <done>Existing test updated to the new primary-error message; secondary-skip, all-missing hard-error, and partial-episode cases added and green; NDJSON contract left untouched; no slog instrumentation tested here (06-06 owns that).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Crunchyroll API → guidByLocale/Subtitles map | remote data (which locales exist) drives the primary/secondary decision; a missing primary must hard-error not silently degrade |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04-01 | Spoofing / Integrity | silent wrong-language MKV delivered when primary track absent | high | mitigate | D-03 primary hard-error path returns a clear fmt.Errorf naming "primary audio locale"; test asserts the message |
| T-06-04-02 | Information Disclosure | skipped locale leaked to NDJSON consumers via a new field | medium | mitigate | D-08 reuses the existing Type:warn event only — no new field; acceptance criteria assert ndjson.go unchanged |
| T-06-04-03 | Denial of Service | empty MKV produced when all tracks missing | high | mitigate | ERR-03 post-loop len(versions)==0 guard returns hard error before mux call |
| T-06-04-SC | Tampering | no package install in this plan | low | accept | no go.mod change |
</threat_model>

<verification>
- go build ./internal/download/... exits 0
- go test ./internal/download/... exits 0
- go vet ./internal/download/... exits 0
- internal/output/ndjson.go is NOT in this plan's git diff (D-08 contract preserved)
</verification>

<success_criteria>
- Missing primary audio = hard error (D-03); missing secondary = warn + skip (D-04); ALL missing = hard error (ERR-03)
- Subtitle rules symmetric (D-05); download failure stays hard error (D-06)
- Partial-episode warns via a separate line after the ✓ success line (D-07)
- NDJSON warn event reused unchanged (D-08); human warn reuses the existing ⚠ format (D-09)
- All four behaviors covered by stdlib-only table-driven tests
</success_criteria>

<output>
Create `.planning/phases/06-track-hardening-structured-logging/06-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces

This plan contributes:
- `internal/download/episode.go` — hardened audio locale loop (primary/secondary split + ERR-03 empty-versions guard), consolidated subtitle presence/download loop (D-05/D-06), `skippedTracks []string` accumulator + D-07 partial-episode warn line after the success line
- `internal/download/episode_test.go` — updated primary-error assertion + new secondary-skip, all-missing, and partial-episode table-driven cases

Phase-wide new artifacts (across plans 06-01..06-06) listed in 06-01-PLAN.md §"Artifacts this phase produces".
</content>