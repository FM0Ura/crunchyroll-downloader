---
phase: 06-track-hardening-structured-logging
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - main.go
  - main_test.go
autonomous: true
requirements:
  - LOG-01
must_haves:
  truths:
    - "main.go declares log-level and log-file CLI flags (D-19, LOG-01)"
    - "main.go resolves log level/file via resolveString precedence: explicit flag > CRUNCHYROLL_LOG_LEVEL/CRUNCHYROLL_LOG_FILE env > cfg.LogLevel/cfg.LogFile > default (info / ./logs/animeheaven.log)"
    - "diag.Init(diag.ParseLevel(resolvedLogLevel), resolvedLogFile) is called AFTER config load (line ~309) and AFTER the resolveString calls, and BEFORE checkFFmpeg() (D-18 init-ordering rule — diagnostic logger ready for FFmpeg preflight)"
    - "isAllNilConfig is extended to also check cfg.LogLevel == nil && cfg.LogFile == nil (so a config.json containing only log fields is not treated as fresh)"
    - "The level string is resolved via diag.ParseLevel (exported by 06-02 Task 1, maps the four named levels + unknown→LevelInfo); main.go has NO local parseLogLevel wrapper — single canonical path"
    - "TestResolveString in main_test.go proves precedence: explicit flag wins, then env var, then config, then default for both log-level and log-file"
  artifacts:
    - "main.go (modified: two new flag declarations, two resolveString calls, one diag.Init(diag.ParseLevel(...)) call, isAllNilConfig extension — no local parseLogLevel wrapper)"
    - "main_test.go (NEW: TestResolveString for log-level/log-file precedence with env-var isolation + flag.Visit seam)"
  key_links:
    - "diag.Init call consumes the internal/diag package (06-02) and the Config.LogLevel/LogFile fields (06-01) — the two Wave-1 plans this plan depends on"
    - "diag.Global and subsys loggers become available to downstream strategic instrumentation in plan 06-06 (Wave 3)"
---

<objective>
Wire the new diagnostic logging plane into main.go: declare the CLI flags, resolve them through the existing flag>env>config>default precedence (D-19), and call diag.Init at the correct position in the init sequence (after config load, before checkFFmpeg per D-18 ordering rule). Add main_test.go coverage for the new resolveString call sites.

Purpose: This is the integration point between the Config fields (06-01) and the diag package (06-02). It MUST run in Wave 2, after both Wave-1 plans land.

Output: Modified main.go and new main_test.go.
</objective>

<execution_context>
@/home/fmoura/Documents/Applications/crunchyroll-downloader/.opencode/gsd-core/workflows/execute-plan.md
@/home/fmoura/Documents/Applications/crunchyroll-downloader/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-track-hardening-structured-logging/06-CONTEXT.md
@.planning/phases/06-track-hardening-structured-logging/06-PATTERNS.md
@.planning/phases/06-track-hardening-structured-logging/06-01-SUMMARY.md
@.planning/phases/06-track-hardening-structured-logging/06-02-SUMMARY.md
@internal/config/config.go
@internal/diag/diag.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare --log-level/--log-file flags + resolveString calls + diag.Init(diag.ParseLevel) wiring</name>
  <files>main.go</files>
  <read_first>
    - main.go (whole file — flag block lines 22-35, resolveString lines 223-237, resolveEtpRt lines 239-252, main() lines 254-379 with the config-load block lines 293-309, the resolveString call sites at 312-313, the checkFFmpeg call at 316, parseLangs at 322-323)
    - internal/config/config.go (modified by 06-01 — LogLevel/LogFile *string fields exist, AudioLang/SubsLang are []string)
    - internal/diag/diag.go (created by 06-02 — diag.Init(level slog.Level, path string) and exported parseLevel(s string) slog.Level signatures)
    - .planning/phases/06-track-hardening-structured-logging/06-PATTERNS.md §"`main.go`" (lines 181-242) for exact flag-declaration style, resolveString call-site pattern, and the CRITICAL init-ordering note (D-18)
  </read_first>
  <action>
    Add two flag declarations to the existing `var (...)` block at main.go lines 22-35, keeping the established `flag.String("name", default, "desc")` convention: `logLevel = flag.String("log-level", "info", "Diagnostic log level (debug|info|warn|error)")` and `logFile = flag.String("log-file", "", "Diagnostic log file path (default ./logs/animeheaven.log)")` (D-19). Add an import for `crunchyroll-downloader/internal/diag` alongside the existing internal imports at lines 15-19. There is exactly ONE canonical path to resolve the level string: call the helper exported by 06-02 Task 1 — `diag.ParseLevel(s string) slog.Level` (capital P, exported per Go rules) — directly from main.go. Do NOT add a local `parseLogLevel` wrapper, do NOT add a conditional branch / fallback; the single call site is `diag.Init(diag.ParseLevel(resolvedLogLevel), resolvedLogFile)`.

    In `isAllNilConfig` (lines 212-221), append two more nil checks to the AND chain so a config.json containing only log fields is not treated as fresh: `&& cfg.LogLevel == nil && cfg.LogFile == nil`. Do NOT change the existing AudioLang/SubsLang checks — a nil []string still compares equal to nil (06-01 preserved this).

    In main(), immediately after the existing resolvedEtpRt/resolvedOutputDir resolveString calls at lines 312-313 (and BEFORE the `checkFFmpeg()` call at line 316), add two resolveString calls following the exact pattern of line 313: `resolvedLogLevel := resolveString(explicitFlags, "log-level", *logLevel, "CRUNCHYROLL_LOG_LEVEL", cfg.LogLevel, "info")` and `resolvedLogFile := resolveString(explicitFlags, "log-file", *logFile, "CRUNCHYROLL_LOG_FILE", cfg.LogFile, "./logs/animeheaven.log")` (D-19 — env var names are CRUNCHYROLL_LOG_LEVEL / CRUNCHYROLL_LOG_FILE; defaults are "info" and "./logs/animeheaven.log").

    Immediately after those two line resolutions (still before `checkFFmpeg()`), add the Init call: `diag.Init(diag.ParseLevel(resolvedLogLevel), resolvedLogFile)` (D-18 ordering — logger ready for FFmpeg preflight; the diag package's own Init handles the default-path fallback if resolvedLogFile is somehow empty).

    Do NOT change the existing `output.Init` block at lines 277-285 — output.Global is a SEPARATE plane (D-13). Do NOT touch the parseLangs call sites at 322-323. Do NOT add a JSON-format flag (LOG-06 deferred per REQUIREMENTS.md).
  </action>
  <verify>
    <automated>go build ./... && go vet ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` exits 0
    - `rg -n 'flag.String\("log-level"' main.go` returns one match
    - `rg -n 'flag.String\("log-file"' main.go` returns one match
    - `rg -n 'CRUNCHYROLL_LOG_LEVEL' main.go` returns one match (env-var name in resolveString call)
    - `rg -n 'CRUNCHYROLL_LOG_FILE' main.go` returns one match
    - `rg -n 'diag.Init\(' main.go` returns one match
    - `rg -n 'diag.ParseLevel(' main.go` returns one match (single canonical level-resolution path — no local wrapper)
    - `rg -c 'parseLogLevel' main.go` is 0 (no local parseLogLevel wrapper survives in main.go)
    - `rg -n 'cfg.LogLevel == nil && cfg.LogFile == nil' main.go` returns one match (isAllNilConfig extension)
    - The diag.Init call appears AFTER `cfg, err = config.Load(cfgPath)` (the config-load block) and BEFORE `checkFFmpeg()` (the FFmpeg preflight) — verify by reading the source order
    - main.go imports `crunchyroll-downloader/internal/diag`
    - main.go does NOT import a JSON-handler or add any `--log-format` flag (LOG-06 deferred)
  </acceptance_criteria>
  <done>main.go declares the two diagnostic-log flags, resolves them via resolveString precedence, calls diag.Init in the correct init-order position (after config, before checkFFmpeg), extends isAllNilConfig, and the whole module still builds + vets cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create main_test.go with TestResolveString precedence coverage for log-level/log-file</name>
  <files>main_test.go</files>
  <read_first>
    - main.go (the resolveString function at lines 223-237 — the function under test; the new flag vars logLevel/logFile; the diag.ParseLevel call site added by Task 1)
    - internal/config/config_test.go lines 41-57 (env-var isolation pattern to mirror: save+cleanup unset env, set test value)
    - .planning/phases/06-track-hardening-structured-logging/06-PATTERNS.md §"`main_test.go`" (lines 523, 533) — TestResolveString cases: flag wins, env over config, config over default, default last
  </read_first>
  <behavior>
    - Test 1 (flag wins): with explicitFlags["log-level"]=true and flagVal="warn", resolveString returns "warn" regardless of env/config/default.
    - Test 2 (env over config): explicitFlags["log-level"]=false, CRUNCHYROLL_LOG_LEVEL="debug", cfg.LogLevel points to "info" → returns "debug".
    - Test 3 (config over default): explicitFlags["log-level"]=false, CRUNCHYROLL_LOG_LEVEL unset, cfg.LogLevel points to "error" → returns "error".
    - Test 4 (default last): explicitFlags=false, env unset, cfg.LogLevel nil → returns "info".
    - Test 5 (log-file default): same four-tier precedence for log-file with default "./logs/animeheaven.log".
  </behavior>
  <action>
    Create main_test.go with `package main` and stdlib-only imports (`os`, `testing`, plus the existing `crunchyroll-downloader/internal/config` only if needed to construct a *config.Config for the `*string` field). Use the env-var isolation pattern from config_test.go:41-57 to save + cleanup-restore CRUNCHYROLL_LOG_LEVEL and CRUNCHYROLL_LOG_FILE around each env-reliant subtest. Build `explicitFlags := map[string]bool{"log-level": true}` etc. per case. Assert on the returned string equality with `t.Fatalf`. Cover the five behaviors above as table-driven subtests (one `func TestResolveString(t *testing.T)` with a `tests := []struct{...}` loop) — both log-level and log-file rows share the table since resolveString is generic.

    IMPORTANT: do NOT call flag.Parse() in tests (it conflicts with the test binary's flag handling). Test resolveString as a pure function with a hand-built explicitFlags map. Do NOT invoke main() or processURL() in this task — they require live API/FFmpeg.
  </action>
  <verify>
    <automated>go test ./ -count=1 -run TestResolveString -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./ -run TestResolveString` exits 0
    - `rg -c 'TestResolveString' main_test.go` is 1
    - `rg -c 'CRUNCHYROLL_LOG_LEVEL\|CRUNCHYROLL_LOG_FILE' main_test.go` ≥ 1 (env-var isolation covered)
    - `rg -c 't\.Cleanup' main_test.go` ≥ 1 (env-var restore seam)
    - main_test.go is in `package main` (not `package main_test`)
    - No test calls flag.Parse() or main()
  </acceptance_criteria>
  <done>TestResolveString proves the four-tier precedence for both log-level and log-file; env vars are save-and-restored via t.Cleanup; the test binary is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user CLI → flag vars → diag.Init | untrusted --log-file path crosses here; arbitrary path write to disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03-01 | Tampering | user-supplied --log-file path traversal | low | accept | same trust level as the pre-existing --output-dir CLI flag; lumberjack.Writer normalizes the path; user controlled by design |
| T-06-03-02 | Denial of Service | diag.Init ordering wrong (runs before config load) | medium | mitigate | plan enforces "after config load / before checkFFmpeg" via acceptance criteria reading source order; test would catch a regression if added (defer) |
| T-06-03-03 | Information Disclosure | diag.Init called late enough that early startup errors are not logged | low | accept | output.Global still prints to stdout/stderr for user-facing errors; the diagnostic plane only covers post-init events by design |
| T-06-03-SC | Tampering | no new package install in this plan | low | accept | no go get / go.sum change in this plan |
</threat_model>

<verification>
- go build ./... exits 0
- go vet ./... exits 0
- go test ./ -run TestResolveString exits 0
- The diag.Init call is positioned after config.Load and before checkFFmpeg() in the source
</verification>

<success_criteria>
- --log-level and --log-file flags declared and consumed via resolveString precedence (D-19)
- diag.Init called at the correct init-order position (D-18)
- isAllNilConfig extended so a config.json with only log fields is not treated as fresh
- main_test.go proves the four-tier precedence for both flags
</success_criteria>

<output>
Create `.planning/phases/06-track-hardening-structured-logging/06-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

This plan contributes:
- `main.go` — `--log-level`/`--log-file` flag declarations, `diag.ParseLevel` call (no local wrapper — single canonical path), two `resolveString` calls (CRUNCHYROLL_LOG_LEVEL/CRUNCHYROLL_LOG_FILE env names), `diag.Init(diag.ParseLevel(resolvedLogLevel), resolvedLogFile)` call at the correct init-order position, `isAllNilConfig` extended with `cfg.LogLevel == nil && cfg.LogFile == nil`
- `main_test.go` — `TestResolveString` with four-tier precedence coverage for log-level and log-file (env-var isolation via t.Cleanup)

Phase-wide new artifacts (across plans 06-01..06-06) listed in 06-01-PLAN.md §"Artifacts this phase produces".
</content>