---
phase: 06-track-hardening-structured-logging
plan: 07
type: execute
wave: 4
depends_on: [06-01, 06-03, 06-04, 06-05, 06-06]
files_modified:
  - main.go
  - main_test.go
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [ERR-01, ERR-02, ERR-04]
must_haves:
  truths:
    - "D-01/D-02: Config.AudioLang and Config.SubsLang arrays feed runtime language selection when the matching CLI flag is not explicit."
    - "D-01: The first selected audio/subtitle language remains the primary protected track passed to download.Episode and processURL."
    - "Explicit --audio-lang and --subs-lang values override config arrays; absent config arrays fall back to ja-JP and en-US defaults."
    - "ERR-04 remains marked complete in requirements metadata because mux validation is already verified by Phase 06."
  artifacts:
    - "main.go contains a small language-list resolver used before processURL calls."
    - "main_test.go proves flag/config/default precedence for audio and subtitle language lists."
    - ".planning/REQUIREMENTS.md marks ERR-04 Complete in the checkbox list and traceability table."
  key_links:
    - "Config.AudioLang/Config.SubsLang -> main.go resolver -> audioLangs/subsLangs -> processURL -> download.Episode"
    - "processURL primaryAudio/primarySubs use the first element of the resolved lists for series API calls."
    - "REQUIREMENTS.md ERR-04 metadata matches verified mux behavior from 06-05 and 06-VERIFICATION.md."
---

<objective>
Close the Phase 06 verifier gap where config language arrays load correctly but do not affect runtime language selection.

Purpose: Make the D-02 config schema migration operational by using config audio_lang/subs_lang arrays unless the user explicitly supplies the matching CLI flag.
Output: A targeted wiring/test/docs update that lets /gsd-execute-phase 06 close the failed verifier truth without replanning completed Phase 06 work.
</objective>

<execution_context>
@/home/fmoura/Documents/Applications/crunchyroll-downloader/.codex/gsd-core/workflows/execute-plan.md
@/home/fmoura/Documents/Applications/crunchyroll-downloader/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-track-hardening-structured-logging/06-CONTEXT.md
@.planning/phases/06-track-hardening-structured-logging/06-PATTERNS.md
@.planning/phases/06-track-hardening-structured-logging/06-VERIFICATION.md
@.planning/phases/06-track-hardening-structured-logging/06-01-SUMMARY.md
@.planning/phases/06-track-hardening-structured-logging/06-03-SUMMARY.md
@.planning/phases/06-track-hardening-structured-logging/06-04-SUMMARY.md
@.planning/phases/06-track-hardening-structured-logging/06-05-SUMMARY.md
@main.go
@main_test.go
@internal/config/config.go
</context>

## Artifacts This Phase Produces

- New helper symbol in `main.go`: `resolveLangs(explicitFlags map[string]bool, flagName string, flagVal string, configVal []string, defaultVal []string) []string`.
- New or extended table-driven tests in `main_test.go` for language-list precedence and primary ordering.
- Updated metadata in `.planning/REQUIREMENTS.md` for ERR-04.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tests for config-backed language selection</name>
  <files>main_test.go</files>
  <read_first>
    - `main_test.go` — existing `TestParseLangs`, `TestResolveString`, and `TestIsAllNilConfig` patterns.
    - `main.go` — current `parseLangs` and the `audioLangs := parseLangs(*audioLang)` / `subsLangs := parseLangs(*subtitlesLang)` call site.
    - `internal/config/config.go` — nil-vs-empty slice semantics for `Config.AudioLang` and `Config.SubsLang`.
  </read_first>
  <behavior>
    - Explicit `audio-lang` flag value like `pt-BR,en-US` returns `[]string{"pt-BR", "en-US"}` even when config has `[]string{"ja-JP"}`.
    - Non-explicit audio flag with config `[]string{"en-US", "ja-JP"}` returns that config slice in the same order so `en-US` is primary per D-01.
    - Non-explicit subtitle flag with config `[]string{"pt-BR", "en-US"}` returns that config slice in the same order so `pt-BR` is primary per D-01.
    - Nil config language slices fall back to `[]string{"ja-JP"}` for audio and `[]string{"en-US"}` for subtitles.
    - Explicit empty CLI input and explicit empty config arrays produce an empty slice, preserving the existing downstream empty-audio hard-error path from ERR-03.
  </behavior>
  <action>
    Add a table-driven `TestResolveLangs` in `main_test.go` before or near `TestParseLangs`. The test should call a new production helper named `resolveLangs` that does not exist yet at RED time. Use `reflect.DeepEqual` like `TestParseLangs`.

    Cover both audio and subtitle cases rather than testing only one field. Include cases that prove config order is preserved because the first element is the protected primary track per D-01/D-02, and cases that prove explicit CLI flags override config arrays.

    Do not add network, FFmpeg, or API-client setup to this test. This is a pure resolver test so it should run in the root package with `go test ./ -run TestResolveLangs -count=1`.
  </action>
  <verify>
    <automated>rtk go test ./ -run TestResolveLangs -count=1</automated>
  </verify>
  <acceptance_criteria>
    - RED state: `go test ./ -run TestResolveLangs -count=1` fails because `resolveLangs` is undefined or not yet correct.
    - Test table includes explicit flag, config fallback, default fallback, and order-preservation cases for D-01/D-02.
    - Test table includes both `audio-lang` and `subs-lang` flag names.
  </acceptance_criteria>
  <done>`main_test.go` contains deterministic resolver expectations that would fail against the current verifier-gap behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire config language arrays into main runtime selection</name>
  <files>main.go, main_test.go</files>
  <read_first>
    - `main.go` — `parseLangs`, `resolveString`, config load block, and the current language parsing immediately before Widevine setup.
    - `main_test.go` — `TestResolveLangs` from Task 1 and existing `TestParseLangs`.
    - `.planning/phases/06-track-hardening-structured-logging/06-VERIFICATION.md` — failed truth and missing work for config language arrays.
  </read_first>
  <action>
    Implement `resolveLangs(explicitFlags map[string]bool, flagName string, flagVal string, configVal []string, defaultVal []string) []string` in `main.go`.

    Required behavior: if `explicitFlags[flagName]` is true, return `parseLangs(flagVal)`; else if `configVal != nil`, return `configVal`; else return `defaultVal`. Preserve the slice order exactly so index 0 stays primary per D-01. Do not read environment variables for language lists because no `CRUNCHYROLL_AUDIO_LANG` or `CRUNCHYROLL_SUBS_LANG` decision exists in Phase 06.

    Replace the current runtime parsing with `audioLangs := resolveLangs(explicitFlags, "audio-lang", *audioLang, cfg.AudioLang, []string{"ja-JP"})` and `subsLangs := resolveLangs(explicitFlags, "subs-lang", *subtitlesLang, cfg.SubsLang, []string{"en-US"})`. Keep the downstream `processURL` fallback guards unchanged unless a test proves they must be adjusted; those guards protect direct `processURL` callers.

    This implements D-01/D-02 and closes the verifier truth that `Config.AudioLang` / `Config.SubsLang` feed runtime primary-element-first language selection. Do not change `download.Episode`, `internal/output/ndjson.go`, or config JSON unmarshalling in this gap task.
  </action>
  <verify>
    <automated>rtk go test ./ -run 'TestResolveLangs|TestParseLangs|TestIsAllNilConfig' -count=1</automated>
    <automated>rtk go test ./internal/config ./internal/download -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `main.go` no longer constructs runtime `audioLangs` and `subsLangs` solely with `parseLangs(*audioLang)` and `parseLangs(*subtitlesLang)`.
    - `cfg.AudioLang` and `cfg.SubsLang` appear in the runtime language-selection call site, not only in `isAllNilConfig`.
    - Explicit CLI language flags win over config arrays; config arrays win over defaults; order is preserved.
    - Existing Phase 06 missing-track tests still pass, proving primary/secondary semantics remain compatible with `download.Episode`.
  </acceptance_criteria>
  <done>Runtime calls to `processURL` and `download.Episode` receive CLI/config/default-resolved language lists with the intended primary track at index 0.</done>
</task>

<task type="auto">
  <name>Task 3: Correct stale ERR-04 requirement metadata</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` — ERR-04 checkbox and traceability table rows.
    - `.planning/phases/06-track-hardening-structured-logging/06-VERIFICATION.md` — warning stating ERR-04 is satisfied in code/tests but metadata is stale.
    - `.planning/phases/06-track-hardening-structured-logging/06-05-SUMMARY.md` — evidence for mux input validation completion.
  </read_first>
  <action>
    Update only the ERR-04 status metadata in `.planning/REQUIREMENTS.md`: mark the Error Handling checkbox complete and change the traceability table status for `ERR-04 | Phase 6` from Pending to Complete.

    Do not edit unrelated pending requirements for later phases. Do not change requirement text, roadmap scope, or Phase 7-10 status.
  </action>
  <verify>
    <automated>rtk grep \"ERR-04\" .planning/REQUIREMENTS.md</automated>
    <automated>rtk go test ./internal/mux -count=1</automated>
  </verify>
  <acceptance_criteria>
    - The ERR-04 checkbox line starts with `- [x]`.
    - The traceability row for `ERR-04 | Phase 6` says `Complete`.
    - No OUT, META, COMP, or TUI requirement statuses are changed.
    - `go test ./internal/mux -count=1` still passes, tying the metadata update to verified behavior.
  </acceptance_criteria>
  <done>Phase 06 verifier warning for stale ERR-04 metadata is closed without changing future-phase metadata.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config file -> runtime download selection | User-controlled `config.json` language arrays determine requested tracks and primary-track semantics. |
| CLI flags -> runtime download selection | User-controlled flags override config and can intentionally request empty language lists. |
| Planning metadata -> workflow status | Requirements status influences GSD verification and future phase routing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-07-01 | Tampering | `resolveLangs` in `main.go` | medium | mitigate | Preserve explicit CLI precedence and table-test the flag/config/default order so config cannot unexpectedly override an explicit request. |
| T-06-07-02 | Denial of Service | Empty config language arrays | low | accept | Empty arrays remain explicit and flow to existing ERR-03 hard-error guards; no silent empty MKV is produced. |
| T-06-07-03 | Repudiation | `.planning/REQUIREMENTS.md` ERR-04 status | low | mitigate | Update only ERR-04 status and verify mux tests so metadata reflects already-executed behavior. |
| T-06-07-SC | Tampering | npm/pip/cargo installs | low | accept | This gap plan performs no package-manager installs. |
</threat_model>

<verification>
Run the narrow tests first:

- `rtk go test ./ -run 'TestResolveLangs|TestParseLangs|TestIsAllNilConfig' -count=1`
- `rtk go test ./internal/config ./internal/download ./internal/mux -count=1`

Then run a source assertion for the failed verifier truth:

- `rtk grep "resolveLangs" main.go main_test.go`
- `rtk grep "cfg.AudioLang\\|cfg.SubsLang" main.go`

If sandboxing blocks Go build-cache writes, rerun the same Go commands with approval; do not substitute source grep for tests.
</verification>

<success_criteria>
- The failed verifier truth is true: config `audio_lang` / `subs_lang` arrays affect the language slices passed into runtime download flow when CLI flags are not explicit.
- Explicit CLI language flags still win and comma-separated flag parsing remains backward compatible.
- First-element primary semantics from D-01 are preserved for both episode downloads and series primary audio/subtitle API queries.
- ERR-04 metadata warning is closed in `.planning/REQUIREMENTS.md`.
- No completed Phase 06 plans or summaries are rewritten.
</success_criteria>

<source_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | Phase 06 verifier gap | Graceful track selection must honor config primary/secondary language arrays | 06-07 | COVERED | Task 1 and Task 2 close the failed truth. |
| REQ | ERR-01 | Non-primary audio missing warns/skips with primary audio protected | 06-07 | COVERED | Resolver preserves audio language order before existing 06-04 behavior. |
| REQ | ERR-02 | Non-primary subtitle missing warns/skips with primary subtitle protected | 06-07 | COVERED | Resolver preserves subtitle language order before existing 06-04 behavior. |
| REQ | ERR-04 | Muxer rejects empty or mismatched inputs before FFmpeg | 06-07 | COVERED | Task 3 fixes stale metadata; implementation already delivered by 06-05. |
| CONTEXT | D-01 | Primary track is first element of language list | 06-07 | COVERED | Task 1/2 explicitly test and preserve ordering. |
| CONTEXT | D-02 | Config audio_lang/subs_lang arrays define unambiguous first element | 06-07 | COVERED | Task 2 wires cfg arrays into runtime language lists. |
| VERIFICATION | failed truth | Config.AudioLang / Config.SubsLang feed runtime selection | 06-07 | COVERED | Task 2 is the direct closure. |
| VERIFICATION | warning | ERR-04 metadata stale | 06-07 | COVERED | Task 3 updates requirements metadata only. |
| RESEARCH | none | No Phase 06 RESEARCH.md exists | N/A | EXCLUDED | 06-PATTERNS.md supplied pattern context. |
</source_audit>

<output>
Create `.planning/phases/06-track-hardening-structured-logging/06-07-SUMMARY.md` when done.
</output>
