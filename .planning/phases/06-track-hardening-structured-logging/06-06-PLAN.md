---
phase: 06-track-hardening-structured-logging
plan: 06
type: execute
wave: 3
depends_on:
  - 06-02
  - 06-03
  - 06-04
  - 06-05
files_modified:
  - internal/download/episode.go
  - internal/download/season.go
  - internal/mux/mux.go
  - internal/api/client.go
autonomous: true
requirements:
  - LOG-02
  - LOG-04
  - LOG-05
must_haves:
  truths:
    - "episode_start strategic event: diag.DownloadLogger.Info(\"episode_start\", \"ep\", EpisodeNumber, \"season\", SeasonNumber) is emitted alongside the existing output.Global.Info episode-start line (D-18 Info level)"
    - "episode_finish strategic event: after the success line, diag.DownloadLogger.Info(\"episode_finish\", \"ep\", EpisodeNumber, \"duration\", duration.String(), \"size_bytes\", fileSize) is emitted (D-18 Info)"
    - "Track-skip strategic event: in the D-04/D-05 soft-skip branches, diag.DownloadLogger.Warn(\"skipped track\", \"episode\", EpisodeNumber, \"kind\", \"audio\"|\"subtitle\", \"locale\", locale) is emitted alongside the output.Global.Warn user-facing line (D-18 strategic; tolerant of nil logger via nil-check)"
    - "season_failure strategic event: when the download.Season / errgroup.Wait error path returns, diag.DownloadLogger.Info(\"season_failure\", \"error\", err.Error()) is emitted at the Season loop failure site (D-18 Info)"
    - "ffmpeg finished strategic event: after a successful cmd.Run in MergeEverything, diag.MuxLogger.Info(\"ffmpeg finished\", \"stderr\", stderr.String()) is emitted before the warnRemove loop (D-18 Info — stderr excerpt as strategic, NOT per-segment noise)"
    - "token refreshed strategic event: in the api/client.go 401-refresh path (around line 103), diag.ApiLogger.Info(\"token refreshed\", \"status\", \"401-recovered\") is emitted alongside the existing output.Global.Debug user-facing line (D-18 Info)"
    - "Per-segment / per-dub download start = Debug noise (NOT Info) per D-18; episode start/finish = Info (strategic) — these distinctions are observable in the emitted slog calls"
    - "All subsystem loggers inherit redaction: even if an slog attr named token/cookie/etp_rt/client_id/private_key/authorization is passed, the value written to the log file shows the literal marker [REDACTED] (LOG-04 enforced by the redactingHandler seam from 06-02, not re-implemented here)"
  artifacts:
    - "internal/download/episode.go (modified: episode_start/episode_finish/skipped-track/skipped-accumulator Warn slog calls)"
    - "internal/download/season.go (modified: season-loop failure Info call)"
    - "internal/mux/mux.go (modified: ffmpeg finished Info call after successful cmd.Run)"
    - "internal/api/client.go (modified: token refreshed Info call in the 401-retry path)"
  key_links:
    - "All diag.*Logger vars are defined by 06-02 (Wave 1) and wired by 06-03 (Wave 2 diag.Init in main); this plan ONLY adds call sites, never defines or re-inits loggers"
    - "This plan runs in Wave 3 so the Episode flow being logged is final (06-04 track-hardening restructured the loops; 06-05 added mux validation) — no double-edits to the same logic"
---

<objective>
Emit the D-18 strategic slog events at the right points in the now-finalized download/mux/api flows: episode start/finish, track-skip, season failure (Info); ffmpeg summary stderr (Info); token refresh (Info). Per-segment noise stays Debug.

Purpose: LOG-02 (per-subsystem grouping, already provided by 06-02's WithGroup vars) becomes observable only when subsystem loggers are actually called. LOG-05 (strategic events default) requires these Info-level calls to exist. LOG-04 (PII redaction) is inherited automatically from the redactingHandler seam — no producer-side redaction.

Output: Three modified subsystem files with additive diag.*Logger.Info/Warn/Debug call sites; no new test plan (existing tests gate regressions; redaction is tested in 06-02).
</objective>

<execution_context>
@/home/fmoura/Documents/Applications/crunchyroll-downloader/.opencode/gsd-core/workflows/execute-plan.md
@/home/fmoura/Documents/Applications/crunchyroll-downloader/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-track-hardening-structured-logging/06-CONTEXT.md
@.planning/phases/06-track-hardening-structured-logging/06-PATTERNS.md
@.planning/phases/06-track-hardening-structured-logging/06-02-SUMMARY.md
@.planning/phases/06-track-hardening-structured-logging/06-04-SUMMARY.md
@.planning/phases/06-track-hardening-structured-logging/06-05-SUMMARY.md
@internal/diag/diag.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: download package strategic events (episode start/finish, skipped track, season failure) — D-18 Info/Debug</name>
  <files>internal/download/episode.go, internal/download/season.go</files>
  <read_first>
    - internal/download/episode.go (the post-06-04 version: the episode-start Info line at ~102, the audio secondary-skip Warn branches added by 06-04, the subtitle secondary-skip Warn branches added by 06-04, the success line at the bottom, the completed=true flag at line 299; also season.go for the season-loop failure point)
    - internal/diag/diag.go (06-02 output — the exported DownloadLogger *slog.Logger var and its WithGroup(\"download\") parent)
    - .planning/phases/06-track-hardening-structured-logging/06-CONTEXT.md D-17 + D-18 (subsystem grouping + strategic-vs-noise marking)
    - .planning/phases/06-track-hardening-structured-logging/06-PATTERNS.md §"`internal/api/client.go`" (lines 431-462) and §"Shared Patterns / Error Handling" for the nil-check-tolerant slog call pattern
  </read_first>
  <action>
    Add an import for `crunchyroll-downloader/internal/diag` to internal/download/episode.go alongside the existing internal imports. All slog calls must be nil-check tolerant: `if diag.DownloadLogger != nil { diag.DownloadLogger.Info(...) }` because the package globals are nil until diag.Init runs in main() (D-17 ordering safety).

    Episode start (D-18 Info): immediately after the existing `output.Global.Info("[Episode %d/%d] %s (S%02dE%02d) ...", info.EpisodeMetadata.EpisodeNumber, totalEpisodes, info.Title, info.EpisodeMetadata.SeasonNumber, info.EpisodeMetadata.EpisodeNumber)` line at episode.go ~102, add `if diag.DownloadLogger != nil { diag.DownloadLogger.Info("episode_start", "ep", info.EpisodeMetadata.EpisodeNumber, "season", info.EpisodeMetadata.SeasonNumber) }`.

    Skipped track (D-18 strategic Warn — pairing the user-facing output.Global.Warn with a subsystem diagnostic): in BOTH the audio-secondary-skip branch (06-04 added `output.Global.Warn("Skipping %s audio: not available for episode %d", locale, info.EpisodeMetadata.EpisodeNumber)`) and the subtitle-secondary-skip branch (06-04 added `output.Global.Warn("Skipping %s subtitles: not available for episode %d", locale, info.EpisodeMetadata.EpisodeNumber)`), immediately after each output.Global.Warn call, add the matching `if diag.DownloadLogger != nil { diag.DownloadLogger.Warn("skipped track", "episode", info.EpisodeMetadata.EpisodeNumber, "kind", "audio", "locale", locale) }` (use `"subtitle"` for the sub branch). The kind attr values are exactly "audio" and "subtitle".

    Episode finish (D-18 Info with duration + size summary): immediately after the success line at episode.go (the `output.Global.Info("%s[Episode %d/%d] %s ... %s%s %s%s %s", ...)` with the green ✓), add `if diag.DownloadLogger != nil { diag.DownloadLogger.Info("episode_finish", "ep", info.EpisodeMetadata.EpisodeNumber, "duration", duration.String(), "size_bytes", fi.Size()) }` — reuse the same `fi *os.FileInfo` already stat'd for the success-line fileSizeStr (do NOT stat the file twice; capture the int64 into a local var the existing scope already exposes).

    Season failure (D-18 Info — the season-level error): in season.go (or episode.go if download.Season lives there), at the point where a Season errgroup/aggregate error is returned, add `if diag.DownloadLogger != nil { diag.DownloadLogger.Info("season_failure", "error", err.Error()) }` before returning the wrapped error. The `error` attr stringifies err; if err contains a token it is caught by redaction only if logged as the redacted key — but `err.Error()` is free-form text, not a named attr key, so the redactingHandler does NOT redact concatenated message text; ensure you DO NOT pass the raw token value as a named attr. Prefer passing `status`-style attrs.

    Do NOT change Debug-level noise in this task (per-segment / per-dub start remain Debug-emitted elsewhere if added in plan 06-06 task 3 — see below). Do NOT modify output.Global emission lines (D-13 separate plane preserved).
  </action>
  <verify>
    <automated>go build ./internal/download/... && go test ./internal/download/... && go vet ./internal/download/...</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./internal/download/...` exits 0
    - `go test ./internal/download/...` exits 0 (existing tests stay green; new slog calls are side-effect-only and must not break the api.Client seam)
    - `rg -c 'diag.DownloadLogger.Info\("episode_start"' internal/download/episode.go` is 1
    - `rg -c 'diag.DownloadLogger.Info\("episode_finish"' internal/download/episode.go` is 1
    - `rg -c 'diag.DownloadLogger.Warn\("skipped track"' internal/download/episode.go` is 2 (one in audio branch, one in subtitle branch)
    - `rg -c 'diag.DownloadLogger.Info\("season_failure"' internal/download/` ≥ 1 (season.go or episode.go)
    - Every diag.DownloadLogger call is wrapped in a `if diag.DownloadLogger != nil { ... }` nil-check — `rg -c 'diag.DownloadLogger != nil' internal/download/` counts ≥ number of call sites
    - episode.go imports `crunchyroll-downloader/internal/diag`
    - No raw token/etp_rt value is passed as a named slog attr (free-text err.Error() is acceptable but no `"token", c.token` style attr)
  </acceptance_criteria>
  <done>Strategic download-plane events (episode_start/finish Info, skipped track Warn, season_failure Info) emit through diag.DownloadLogger with nil-checks; existing download tests stay green; no PII passed as named attrs.</done>
</task>

<task type="auto">
  <name>Task 2: mux package ffmpeg-finished Info event (D-18 strategic stderr excerpt)</name>
  <files>internal/mux/mux.go</files>
  <read_first>
    - internal/mux/mux.go (post-06-05 version: the validation block at the top, the cmd.Run success path between lines 91-108, the `var stderr bytes.Buffer` at ~92, the warnRemove loop at ~102-108, the existing `output.Global.Info("\nDownload finished! Output file: %s\n", outputFile)` at ~110)
    - internal/diag/diag.go (the exported MuxLogger *slog.Logger var)
  </read_first>
  <action>
    Add an import for `crunchyroll-downloader/internal/diag` to internal/mux/mux.go alongside the existing imports.

    After the successful `cmd.Run()` (the path that does NOT return an error — currently between line 100 and the start of the warnRemove loop at ~102), insert `if diag.MuxLogger != nil { diag.MuxLogger.Info("ffmpeg finished", "stderr", stderr.String()) }`. The stderr bytes.Buffer contains the ffmpeg progress/stderr log which is non-empty even on successful runs — use it as the D-18 strategic "FFmpeg summary (stderr excerpt)". This is an Info-level strategic event, NOT Debug per-segment noise.

    Do NOT add the same call in the cmd.Run() failure path (the error already returns to episode.go with the stderr excerpt wrapped — D-18 token refresh / season failure are the strategic failure events; per-ffmpeg-failure logging is not required). Do NOT modify the existing warnRemove loop or the `output.Global.Info("\nDownload finished! ...")` user-facing line (D-13 separate plane preserved). Do NOT log the ffmpegCommand test-seam or the args slice (could leak absolute paths — keep just the stderr excerpt).
  </action>
  <verify>
    <automated>go build ./internal/mux/... && go test ./internal/mux/... && go vet ./internal/mux/...</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./internal/mux/...` exits 0
    - `go test ./internal/mux/...` exits 0 (existing sentinel-ffmpeg tests stay green — the Info call only runs in the success path which the sentinel always fails, so the call site is never reached by existing failure-path tests; verify no test triggers a real ffmpeg success that hits the nil-check — if a test does reach it, ensure diag.MuxLogger is nil by default in tests and the nil-check guards it)
    - `rg -c 'diag.MuxLogger.Info\("ffmpeg finished"' internal/mux/mux.go` is 1
    - `rg -c 'if diag.MuxLogger != nil' internal/mux/mux.go` is 1 (nil-check around the call)
    - The Info call appears AFTER `cmd.Run()` returns nil and BEFORE the `warnRemove(videoFile)` loop (verify by source order)
    - mux.go imports `crunchyroll-downloader/internal/diag`
    - No new external package imported (slog is already transitively available)
  </acceptance_criteria>
  <done>Successful ffmpeg run emits the D-18 strategic "ffmpeg finished" Info event with the stderr excerpt via diag.MuxLogger (nil-check-guarded); existing mux tests stay green; output.Global user-facing plane unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: api package token-refresh strategic Info event (D-18)</name>
  <files>internal/api/client.go</files>
  <read_first>
    - internal/api/client.go (the Do method 401-refresh path around lines 91-130 — the `output.Global.Debug("Access token expired. Refetching one...")` at ~103, the `c.fetchAccessToken` call, the retry request `"Bearer "+c.token` at ~122, the `return nil, fmt.Errorf("refreshing token: %w", err)` and `return nil, fmt.Errorf("unauthorized after token refresh retry")` lines)
    - internal/diag/diag.go (the exported ApiLogger *slog.Logger var)
    - .planning/phases/06-track-hardening-structured-logging/06-PATTERNS.md §"`internal/api/client.go`" (lines 431-462) for the strategic-event insertion pattern and the Bearer redaction note
  </read_first>
  <action>
    Add an import for `crunchyroll-downloader/internal/diag` to internal/api/client.go.

    Token refreshed (D-18 Info — strategic recovery event): immediately after the existing `output.Global.Debug("Access token expired. Refetching one...")` line (~103) AND after the successful `c.fetchAccessToken` call sets the new token, add `if diag.ApiLogger != nil { diag.ApiLogger.Info("token refreshed", "status", "401-recovered") }`. Place it BEFORE the retry `retryReq.Header.Set("Authorization", "Bearer "+c.token)` line (~122) so the event records the recovery regardless of whether the retry succeeds. Do NOT pass the token value as a named attr — the redactingHandler only intercepts the named keys token/cookie/etp_rt/etc.; passing the raw token as a string attr key like `"token", c.token` WOULD be redacted (because the key is "token"), but since the message already says "token refreshed", there is no need to attach the secret and it is safer to omit it (defense in depth — never log the actual secret even if it would be redacted).

    Do NOT add a Debug-level event here (the existing `output.Global.Debug` is the user-facing plane already). Do NOT modify the Bearer header line itself (the redaction target is only reached if explicitly logged as a named attr, which this task does not do). Do NOT add a slog call in auth.go — auth.go's Basic Auth / cookies are PII redaction targets and the redactingHandler catch them only if logged; the strategic auth events (token refresh) all live in client.go per D-18.
  </action>
  <verify>
    <automated>go build ./internal/api/... && go test ./internal/api/... && go vet ./internal/api/...</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./internal/api/...` exits 0
    - `go test ./internal/api/...` exits 0 (existing api tests stay green — the Info call is side-effect-only)
    - `rg -c 'diag.ApiLogger.Info\("token refreshed"' internal/api/client.go` is 1
    - `rg -c 'if diag.ApiLogger != nil' internal/api/client.go` is 1 (nil-check guard)
    - The Info call appears after the `output.Global.Debug("Access token expired. Refetching one...")` line and after the new token is set — NOT in the error-return branches
    - client.go imports `crunchyroll-downloader/internal/diag`
    - No raw token value passed as any slog attr (neither key "token" nor any other key)
    - internal/api/auth.go is NOT modified in this task — `git diff --name-only` excludes internal/api/auth.go (auth's redaction is automatic via the handler; no slog source there per D-18)
  </acceptance_criteria>
  <done>Token refresh path emits the D-18 Info strategic event via diag.ApiLogger (nil-checked); the actual token value is never passed as an attr; existing api tests stay green; auth.go untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| subsystem slog producer → diag.Global → redactingHandler → log file on disk | any attr written here crosses the PII redaction seam; producers must NOT rely on redaction for secrets (defense in depth) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-06-01 | Information Disclosure | token refreshed Info event leaks the actual token value | high | mitigate | task instructions forbid passing the raw token as any slog attr; redaction is a second line (the redactingHandler from 06-02 still catches any token-keyed attr), but producers do not attach it |
| T-06-06-02 | Information Disclosure | ffmpeg stderr excerpt contains secrets (URLs with tokens) | medium | mitigate | stderr excerpts are ffmpeg-internal logs and typically contain only stream/frame counts; if a token URL appears it does so in free-text stderr, not as a named redact-key, so it would NOT be auto-redacted — minimize by logging only stderr.String() excerpt (truncation not required for v1.1 but acceptable); accept the residual risk per D-18 strategic-value tradeoff |
| T-06-06-03 | Denial of Service | diag.MuxLogger / ApiLogger nil during tests causing panic | medium | mitigate | all call sites are nil-check-guarded (`if diag.XxxLogger != nil { ... }`); verified by acceptance criteria |
| T-06-06-04 | Tampering / Integrity | subsystem logger not initialized before first use (init order bug) | low | mitigate | 06-03 enforces diag.Init after config load before checkFFmpeg; nil-check tolerates the pre-Init window for any early subsystem path |
| T-06-06-SC | Tampering | no package install in this plan | low | accept | no go.mod change |
</threat_model>

<verification>
- go build ./... exits 0
- go test ./internal/download/... ./internal/mux/... ./internal/api/... exits 0
- go vet ./... exits 0
- Every diag.*Logger call site in the three modified files is wrapped in `if diag.XxxLogger != nil { ... }`
- The api token refreshed event is emitted without the raw token as an attr
- The mux ffmpeg finished event is emitted only on the success path
- Episode start/finish/skipped/season-failure events emit through diag.DownloadLogger
</verification>

<success_criteria>
- D-18 Info-level strategic events emitted at episode start/finish (download), skipped track (download Warn), season failure (download Info), ffmpeg finished (mux), token refreshed (api)
- Per-segment / per-dub noise remains Debug-level (no Debug→Info promotion)
- Every subsystem logger call is nil-check-guarded (D-17 init-order safety)
- The user-facing output.Global plane (D-13) is unchanged in every modified file
- No raw PII value passed as any slog attr (defense in depth; the redactingHandler from 06-02 is the second line)
- All existing tests stay green
</success_criteria>

<output>
Create `.planning/phases/06-track-hardening-structured-logging/06-06-SUMMARY.md` when done
</output>

## Artifacts this phase produces

This plan contributes:
- `internal/download/episode.go` — strategic slog call sites: episode_start, episode_finish, two skipped-track Warn call sites (audio + sub), season_failure Info; nil-check guards on every call
- `internal/mux/mux.go` — "ffmpeg finished" Info call (stderr excerpt) on the successful cmd.Run path, nil-check guarded
- `internal/api/client.go` — "token refreshed" Info call (status 401-recovered) in the 401-refresh path, nil-check guarded, no raw token attr

Phase-wide new artifacts (across plans 06-01..06-06) listed in 06-01-PLAN.md §"Artifacts this phase produces".
</content>
