---
status: complete
quick_id: 260711-1651
slug: fix-urls-file-alias
---

# Quick Task 260711-1651 Summary

## Changes

- Added `--urls` as an alias for `--file` in the CLI.
- Added `resolveInputTargets` to normalize `--file` and `--urls` and reject conflicting file paths.
- Added tests for accepting `--urls` and rejecting conflicting aliases.
- Updated README and configuration docs to list `--urls`.

## Verification

- `rtk go test ./...` — passed, 354 tests across 12 packages.
- `rtk go vet ./...` — passed.
- `rtk go run . --help` — confirmed `-urls` appears in CLI usage.

## Notes

No commit was created because the working tree already contained unrelated user changes, including edits in the same files touched by this fix.
